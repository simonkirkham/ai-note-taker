# Phase Bugs — Defect backlog

**Goal:** A standing, unnumbered phase that captures bugs found in the deployed app and tracks them to a fix. Unlike numbered phases, this has no learning theme and no fixed slice sequence — items are added as defects surface and removed (marked Done) as they are fixed. Each bug is still fixed the normal way: a failing spec/test that reproduces it first, then the fix.

**What belongs here:** defects — behaviour that is wrong, broken, or crashes. If it's a small adjustment to working behaviour it's a **minor change** ([docs/phases/phase-minor-changes.md](phase-minor-changes.md)); a new capability is a **feature** ([docs/future-features.md](../future-features.md) → a numbered phase); a refactor/upgrade/CI item is a **technical improvement** ([docs/technical-improvements.md](../technical-improvements.md)).

**Learning surface:** none specific — this is maintenance work. The discipline is reproduce-before-fix (the Prove-It pattern) and guarding against regression.

---

## Summary

| Item | Summary | Status | Depends on |
|------|---------|--------|------------|
| BUG-1 | Blank screen presented when 401 returned from API | Done | — |
| BUG-2 | favicon.ico request 404s / errors on every page load | Done | — |
| BUG-3 | Data Protection warnings on every Lambda cold start (log noise) | Done | — |
| BUG-4 | ConcurrencyException surfaces as unhandled 500 on note writes | Done | — |
| BUG-5 | Renaming a deleted note throws unhandled 500 instead of 404 | Done | — |
| BUG-6 | CloudWatch RUM receives no data — loader CDN host is regional, doesn't resolve | Done | 12-F |
| BUG-7 | _Withdrawn — "empty notes created and left behind"; removed as not-a-defect (85eba79). Number retained, not reused._ | Withdrawn | — |
| BUG-8 | `x-correlation-id` returned to clients is never logged — a user-quoted ID can't be found in logs | Done | 12-A |
| BUG-9 | Note tab panels (Transcript/Final notes) stack below Quick notes instead of replacing it | Done | 15-B |
| BUG-10 | Live transcription falls behind realtime — audio streamed in ~8ms chunks (~125 events/sec) | Done | — |
| BUG-11 | Signed out ~hourly — iframe silent refresh fails under third-party-cookie blocking; switch to backend refresh-token flow | Done | — |
| BUG-12 | `DynamoDbNoteSearchViewStore.GetByNoteIdAsync` omits `ConsistentRead = true` — stale read on the inline read-modify-write can clobber a just-written field | Done | 22-A |
| BUG-13 | Search bar shows two clear `✕` — the native `<input type="search">` cancel button on top of the custom clear button | Done | 22-B |
| BUG-14 | Pasting space-separated tags drops a pill — optimistic patch no-ops when the note isn't cached yet (initial GET in flight) | Done | 20-E |
| BUG-15 | Forced through full Google sign-in on cold load — bootstrap never uses the `rt` refresh cookie, so the session is only ~1h not 30 days | Done | BUG-11 |
| BUG-16 | Google emails the user on every login — `prompt=consent` forces a fresh consent grant on each sign-in | Done | BUG-11 |
| BUG-17 | Concurrent multi-word tag add drops a tag — second append loses the optimistic-concurrency race and is silently dropped (no handler retry) | Done | BUG-4, BUG-14 |
| BUG-18 | Removing an inline image (or any edit) is silently not persisted — note content saves only on editor `onBlur`; the Save button navigates without flushing the draft | Done | 25-D |
| BUG-19 | Inline image flashes a 403 on every open — `ImageNodeView` renders the raw S3 key as a relative `<img src>` before `resolveImages` swaps in the presigned URL | Done | 25-B, 25-D |
| BUG-20 | Workspace-switcher popover overlaps the main content when open (widened in 23-E) — keep it within the sidebar width and reveal rename/delete on row hover so names still fit. | Done | 23-E |
| BUG-21 | Note title silently lost on navigate in/out — the title field is never reconciled with `detail.title` (no draft-pattern sync) so it can show empty, and a blur then persists that empty value. | Done | — |
| BUG-22 | Multi-tag add drops a pill under RYW-2 async reads — the per-stream consistency-token slot is last-writer-wins; the older `@N` can win and the next gated read releases before the second tag folds. | Done | TI-19, RYW-2 |
| BUG-23 | `POST /admin/projections/rebuild` returns an unhandled 500 when a DynamoDB call inside the rebuild times out — the whole rebuild aborts mid-flight with no retry. | Done | TI-16 |
| BUG-24 | Inline image 403s on every note open — `tiptap-markdown` parse-time fetch of the bare S3 key (relative to the SPA route) races before `ImageNodeView`'s guard exists. | Done | BUG-19, TI-37 |
| BUG-25 | `ActionItemJourney.Action_items_persist_across_navigation` E2E flakes under async-projector lag — a plain 5 s wait with no reload-tolerance. | Done | RYW-3a, BUG-26 |
| BUG-26 | Deploy E2E gate intermittently red — post-navigation visibility asserts race the async projector (systemic; fixed 2026-06-13 via TI-39 warm-up/drain + reload-tolerant asserts). | Done | RYW-2 |
| BUG-27 | Concurrent multi-tag add silently drops a tag — exhausted-retry contention returns 409 (treated as a duplicate no-op) instead of a retriable 503. | Done | RYW-2, BUG-17, BUG-26 |
| BUG-28 | Concurrent multi-tag add-then-remove drops a write — DynamoDB `TransactionConflict` escaped as an unhandled 500 (only `ConditionalCheckFailed` was retried), silently dropped. | Done | BUG-27, RYW-2, 27-D |
| BUG-29 | Projector can't purge note images on delete — role missing `s3:ListBucket`, orphaning deleted notes' images in S3 indefinitely. | Done | 27-D |
| BUG-30 | Note-scoped handlers authorized against the async NoteDetail projection → 404 right after create; auth moved to the strongly-consistent event stream (largest residual deploy-gate flake). | Done | 27-RYW, 27-D |
| BUG-31 | A browser test (remove an image, then reopen the note) randomly fails during deploys and blocks releases. Root-caused: the #697 flake was the note going **blank** at the post-removal save ([BUG-48]) which flips Save→Cancel by design. **Fixed + re-enabled (PR #397, deploy #701):** the test keeps durable body text so `hasContent` stays true and the Save button is present. Fix is **deterministic** (the stale-refetch race can no longer change the outcome). Confirmed **6/6 clean** deploy-gate passes (#701–#706, `Passed: 27, Skipped: 0` each). Removing an image works correctly for real users. | Done | BUG-30, 27-RYW, BUG-48 |
| BUG-32 | A just-typed `/ai` instruction is missed on Generate/Re-process — analyse raced the fire-and-forget content save; now flushes + awaits it first. (Residual: analyse still reads content from the async projection.) | Done | 29-A |
| BUG-33 | Forced through full Google consent after inactivity — the warm-tab refresh paths sign out an expired token without trying the valid `rt` cookie (and clear the established flag → `prompt=consent`). | Done | BUG-11, BUG-15, BUG-16 |
| BUG-34 | In-progress transcript lost on browser-back (Alt+←) and a re-record can't recover it — popstate is unguarded so the leave-commit is aborted; "Continue" only continues a *committed* transcript, never a draft; and starting a fresh recording overwrites then deletes the note-keyed draft. | Done | BUG-18, ADR-0011 |
| BUG-35 | Search over-matches — `NoteSearchRanker` scored whole fields with FuzzySharp `PartialRatio`/`TokenSetRatio`, which match any shared substring window, so "Andrew" matched the word "and" across unrelated notes. Replaced with word-level matching (exact / prefix / tight whole-token fuzzy). | Done | 22-A |
| BUG-36 | `npm run update` crashes on Windows with "the term 'silent' is not recognized" — `update.ps1` had UTF-8 em-dashes/arrows but no BOM, so Windows PowerShell 5.1 read it as Windows-1252; the em-dash byte `0x94` decoded to a `"` that closed a string early and broke parsing. Rewrote the script ASCII-only; added a `publish.spec.ts` guard that fails on any non-ASCII byte. | Done | 31-E |
| BUG-37 | The ✓ "Mark as discussed" tick on a note heading no longer works — clicking it does not toggle strikethrough on the topic/heading. | Done | 7-B |
| BUG-38 | **`TagsJourney` deploy-gate flake — ROOT-CAUSED 2026-08-06 after ~4 months of wrong theories.** **Measured scale:** a per-attempt scan of every re-run/failed deploy in #627–#719 (93 deploys) found **58 failing-test occurrences**, of which **26 (45%) are `TagsJourney`** — nearly 4× the next worst journey — spread over 15 deploys and hitting **all six** of its tests (`AddTag_PillAppearsOnNoteScreen` 8, `AddTag_PersistsAfterNavigation` 7, `AddMultipleTags_SpaceSeparated` 4, `AddTag_PillAppearsOnHomeCard` 3, `RemoveTag_PillDisappears` 2, `RemoveTag_GoneAfterNavigation` 2). The doc previously recorded only 6 recurrences — the 4× undercount is failures that were re-run into green and never written down. **Root cause (`web/src/components/NoteView.tsx:267-269`):** `useEffect(() => { if (!loadingDetail && !notFound) inputRef.current?.focus(); }, [loadingDetail, notFound])` focuses the **title** input. It does not fire on mount — it fires when the **RYW-gated `GET /notes/{id}` resolves**, i.e. at an arbitrary wall-clock moment (~150 ms to the 8 s gate cap) while the whole screen, `CommandBar` included, has been interactive the entire time. The tag combobox dismisses itself on blur (`TagCombobox.tsx:107` → `CommandBar.tsx:81` `setAddingTag(false)` → the input is **unmounted**), so the focus grab destroys it mid-interaction. `AddTagAsync` fills then presses Enter as two Playwright round trips; a steal landing between them detaches the element Playwright already resolved, and since `addingTag` is now false `tag-input` never returns → 30 s timeout. Playwright's `press` checks connectedness *before* dispatching the key, so the detach necessarily preceded the keystroke — Enter is not the trigger. **This is a real user bug, not a test artefact:** start typing a tag on a fresh note and the box can be ripped away with your half-typed text silently submitted. **Every previous theory is retired:** *cold start / idle gap* (#649/#652/#654/#656 are same-or-next-day, and #719 failed 43 s into a run where 27 tests had already passed — warm), *projector lag / stale gated read* (lag cannot detach a DOM node; the failing call is an **action**, and the assertions were already reload-tolerant), *the app never loaded* (the #719 log shows the input **resolved** with `value="1:1s"`), and the *`notFound`-bounce* shape this row previously theorised. Lag is the trigger's timing source, never the fault — which is why every consistency-token and reload-tolerance fix looked relevant and none worked. **Method note worth keeping:** the breakthrough came from the deploy #719 call log, the first with a `locator resolved to …` line. Prior recurrences gave bare timeouts because the enriched diagnostic (#372) wrapped only `FillAsync`, not `PressAsync` — an action this row flagged as outstanding for months and which cost the root cause every time. Confirmed independently by three separate root-cause agents plus review, each reproducing the unmount in jsdom. **Fix (PR #418):** make the autofocus mount-only (once, and only when nothing else holds focus) so no network-timed `focus()` remains on the note screen; widen the `AddTagAsync` catch to cover `PressAsync` + `TimeoutException`; ship a regression test that fails against the old effect. Same mechanism also explains `ActionEditJourney` (5 occurrences) — `ActionsSection` has the identical blur-to-unmount shape. **Status 2026-08-06:** PR #418 CI all green and `MERGEABLE`/`CLEAN`, **awaiting merge** (a later commit dropped the `activeElement` guard, which broke the new-note flow). Stays `In Progress` after merge regardless — per the flake-fix bar this needs **10 clean deploy runs counted per-attempt** with `scripts/flake-watch.sh`, not a green merge. **Shipped 2026-08-07 (#418).** Its own deploy #720 red-gated on two UNRELATED failures the fix had un-masked ([BUG-61]/[BUG-62]), so `deploy-production` was skipped and the fix only reached prod on deploy #722. **Flake bar: 7/10 clean runs** (#722-#727 plus 43-F's #724), counted per-attempt with `scripts/flake-watch.sh 722 '[A-Za-z]+Journey'` — zero `TagsJourney` hits since the fix. Stays `In Progress` until the tenth. **DONE 2026-08-07 — the 10-clean-run bar is met.** `scripts/flake-watch.sh 722 '[A-Za-z]+Journey'` reports **10 completed clean deploys (#722-#731), zero hits**, counted per-attempt across every attempt of every run — not by reading conclusions, which is what produced the original 6-vs-26 undercount. No `TagsJourney` or `ActionEditJourney` failure has occurred since the fix went live. The runs span real feature deploys (CHANGE-30/31/32/34, 50-A, BUG-56, TI-58, 43-F, BUG-63) rather than repeated no-op triggers, so they cover varied timing, cold/warm projector state and different code — the conditions that exposed the flake in the first place.| Done | TI-39 |
| BUG-39 | `TodoReorderJourney` reorder reverts after reload (deployed, deterministic 5/5). **Real root cause (test-env projector logs): `clear-test-data` wiped `notetaker-events` + projection tables but NOT `notetaker-proj-position`** → `Projector skip todo-order#__default__ at 1: position_guard`: the default workspace's stable-id order stream is reused every run; a prior run's processed-position (seq 1) survives the clear, so the next run's re-appended reorder (seq 1, events re-numbered from 0) is `≤` the stale mark and skipped as a duplicate → positions never applied. Stable-id streams collide; `note#`/`todo#<guid>` dodge it (fresh guids). **Not a product bug, not 36-B** (the 36-B correlation was coincidental — #655 first set the mark). Fix: `clear-test-data` now clears `proj-position` + all projection tables; journey **un-quarantined**. The per-item-`Position` fragilities the 37-A session noted (ConditionalCheckFailed swallow, PutItem clobber, cross-stream order) are real but latent — not the trigger here — tracked as an order-snapshot redesign follow-up (see detail). | Done | — |
| BUG-40 | Blank lines a user adds for structure are stripped on save — note content is stored as markdown, whose serializer collapsed every run of consecutive blank lines / empty paragraphs to one, condensing the note. Fixed by a `BlankLineParagraph` extension that serializes a *non-trailing* empty paragraph as a U+00A0 line so the structure survives the markdown round-trip (#363, deploy #666 `70c87c4`). | Done | 25-D |
| BUG-41 | HTTP action-item endpoints have an object-level auth gap (IDOR) — `POST /notes/{noteId}/actions/{actionId}/complete\|reopen`, `PATCH …/edit`, `DELETE …` authorize the **route `noteId`** via `OwnsNoteAsync` but never bind it to the action: owning *any* note + knowing a foreign `actionId` lets you mutate that action (it stamps your `sub`). Gated by the unguessable random `actionId` (not enumerable — `get_actions`/`GetActionItems` filter to your own), so low exploitability, but it is broken object-level auth. Fix: authorize the action's own owner via the new `IActionItemAuthorizer.OwnsActionAsync` (added in 41-B for the MCP tools). Fixed (#370, deploy #674): the 4 mutation handlers authorize `OwnsActionAsync` (Command Lambda, event-stream read) instead of the route `noteId`; a foreign or deleted action returns 404 (the latter was 409). | Done | 41-B |
| BUG-42 | `CreateAndListNoteJourney.Create_a_note_name_it_and_see_it_in_the_list` E2E flake — the post-create assert (the new note card visible in the list) races the async projector with a bare 15 s `ToBeVisible` and **no reload-tolerance**; failed deploy #667 attempt 1, passed on rerun. Same class as [BUG-25]/[BUG-26] (post-write visibility vs projector lag), not the BUG-38 cold-start app-load stall. Fixed (#371, deploy #673): both create→list asserts (`CreateAndListNoteJourney` + `NoteDeleteJourney`, the latter red-gated #672 the same way) now use the reload-tolerant `AssertNoteVisibleInListAfterReloadAsync`; the bare racy helper was removed. | Done | BUG-26 |
| BUG-43 | Query Lambda 500s on the first request after a SnapStart restore — the AWS SDK's cached credentials are captured in the snapshot and are stale on restore (`AmazonDynamoDBException: "The security token included in the request is invalid"`, `cold_start: true`). The priming hook registers `RegisterBeforeSnapshot` but no `RegisterAfterRestore`. 1× in 30 days (GET `/w/__default__/todos`), transient (next request succeeds). **Likely the common root of the cold-start E2E gate flakes ([BUG-38] etc.)** — a cold restore 500s the first reads, so pages don't load. **Correction (verified 2026-06-30, AWSSDK v4): the proposed `FallbackCredentialsFactory.Reset()` fix is likely ineffective** — that class is `[Obsolete]` in SDK v4 (clients resolve via `DefaultAWSCredentialsIdentityResolver`), so resetting it clears a cache the live clients no longer use; and it does not address the documented SnapStart root — under SnapStart the runtime serves *container* creds (`AWS_CONTAINER_CREDENTIALS_FULL_URI`) but the SDK cached the access-key *env-var* creds frozen in the snapshot. Real fix = stop the env-var provider winning over container creds after restore. **Fixed (#379, deploy #682): the Query Lambda's DynamoDB client binds a swappable credentials holder that a `RegisterAfterRestore` hook flips to fresh `GenericContainerCredentials`; gated Query-only via `SNAPSTART_CONTAINER_CREDS` (Command untouched).** Prod-verified: flag live on the Query function, no `security token` errors in the Query log group post-deploy. 1×/30d rarity → monitor ~30 days for definitive non-recurrence. | Done | 27-D |
| BUG-44 | A deleted note reappears in the list and stays openable → opening + saving it 404s ("couldn't be saved") and it then vanishes. Root cause: `deleteNote` never captured the delete's `X-Consistency-Token` (the endpoint didn't even emit one), so `useDeleteNote`'s `onSettled` cards refetch ran *ungated* against the async projector before `NoteDeleted` was applied → the optimistically-removed card came back. Fixed (#382, deploy #685): `DeleteNote` emits the token and `deleteNote` threads it via `captureNoteToken`, so the cards refetch gates on the projector dropping the note. Secondary (save-against-deleted shows a *retriable* "try again"): out of scope, not carried. | Done | RYW-2, BUG-30 |
| BUG-45 | A note moved to another workspace can reappear in the source workspace's list (and folder moves can flicker) — same ungated-cards-refetch defect class as [BUG-44], different trigger. The `moveNoteToWorkspace`/`moveNoteToFolder`/`unfileNote` api fns used plain `requestVoid` and the handlers never emitted a token, so `useMoveNote*`'s `onSettled` cards refetch ran ungated against the async projector. Found by Hawk reviewing #382. Fixed (#384, deploy #689): all three handlers emit `X-Consistency-Token` and the api fns thread it via `captureNoteToken`, so the refetch gates on the projector applying the move. | Done | BUG-44, RYW-2 |
| BUG-46 | Deleting a **folder** can briefly leave its notes shown under the (now-gone) folder in the cards list — the same ungated-cards-refetch class as [BUG-44]/[BUG-45], on the folder-delete cascade. `FolderCommandHandler.DeleteFolder` cascades `UnfileNote` (note-stream writes) to each contained note, but `deleteFolder`/`useDeleteFolder` only capture the folder-tree token (`captureFolderToken`/`FOLDERS_SCOPE`), not the note-cards token — so the `onSettled` `getNoteCards` refetch is ungated w.r.t. those unfile writes. Low severity (the folder is correctly gone from the tree; only the notes' placement in the cards list lags). Found by Hawk reviewing #384. **Nuance:** a folder delete is *N* note-stream writes, and the single-token list-gate (design #7) can't cleanly cover all N — the fix is not a straight copy of BUG-44/45 (needs the highest/last unfile token, or a per-list drain).  **Done — PR #437, deploy #739 (2026-08-08), live in prod.** Residual, deliberately accepted: the cards list gate holds a SINGLE stream token (design #7), so this is exact for a folder holding **one** note and covers **1 of N** otherwise — a strict improvement, never worse than before, but the other N-1 streams can still race. And the gated stream is **arbitrary**: notes are iterated in the cards projection's SCAN order, so "the last unfile write" means last *appended* in an order we do not control (an earlier version of the fix, the PR body and the review all wrongly claimed it was the last note created; writing the discriminating test falsified that). Options (b)/(c) from this entry remain if the N>1 case resurfaces. | Done | BUG-45, RYW-2 |
| BUG-47 | **Data loss** — returning to a just-edited note within the projector-lag window loads **stale-empty** content; the uncontrolled editor seeds from it on remount and the next save silently overwrites the real note with **no conflict raised**. Reproduced in prod: note `c7b3b612` wiped 2134→30 chars (v5→v6, 8 s apart); the user, seeing an empty note, retyped a remembered fragment which became the overwrite. Content recovered from the event stream (v5) and restored (v11). Root: async-projection read can return stale (8 s gate cap / 2-retry client / ungated when no token) **+** editor seeds `detail?.content` at remount (`NoteView.tsx:131`, `NoteEditor.tsx:117`) **+** no version guard on `EditContentCmd`. Fix: **hash-based optimistic concurrency on content edits** — client sends the SHA-256 of the content it loaded; server 409s `stale_content` on a mismatch (never blocks a real edit/delete, which carry the matching hash) — plus a non-destructive 409 conflict banner (keeps typed text, offers "Load latest content"). Fixed (#392, deploy #696). | Done | BUG-30, 27-RYW |
| BUG-48 | A stale gated **refetch** of note detail can transiently blank an existing note (title/content) by overwriting good cached data — the title/whole-detail analogue of [BUG-47]'s content stale-overwrite. `content = contentDraft ?? detail.content`; a background refetch (fires on reopen; note stale after a prior save) can return stale projector data (empty title) and React Query stores it → the note flashes blank. Low severity (read-only display; self-corrects on the next fresh read; no data loss — writes are hash-guarded by BUG-47). Surfaced while diagnosing [BUG-31] (the blank flip is what tripped its E2E). Candidate fix: version-guard the `keys.note` cache so a late stale refetch can't regress freshly-cached detail (BUG-47's own write-up lists this as prevention — verify what shipped).  **Done — PR #436, deploy #740 (2026-08-08), live in prod.** Took three attempts; the first two are recorded in the commit history because each looked right: (1) a boolean `if (stale && cached)` pinned the cache to the FIRST stale body until a fully fresh read — reachable on the cold path, since the RYW token outlives a reload but the query cache does not, so it *lengthened* the blank window it was meant to shorten; (2) a flag tracking "was MY last write stale" is not the cache's provenance, so any mutation patching `keys.note` (12 call sites) was unprotected and the next stale read clobbered the user's own edit — BUG-48's exact symptom. Shipped shape: remember the stale body last stored and compare the cache against it **structurally** (a reference compare would miss, because `replaceEqualDeep` rebuilds the cached object), with the hold bounded at 3 so an unreachable token cannot freeze the note for the session. Residual: scope is **note detail only** — see [TI-65] for `getNoteCards`/`getActions`/`getFolders`/`getWorkspaces`. Known unfalsifiable in the suite: swapping the value compare for `===` leaves every spec green, because under jsdom/msw a fetched body is cross-realm and structural sharing is silently off — correct for the browser, unprovable here. | Done | BUG-47 |
| BUG-49 | `NoteDeleteJourney.Deleted_note_is_not_in_list_after_navigating_back_manually` E2E flaked the **deploy #703** gate (48-A merge, `ff622875`) — the **setup** assert `AssertNoteVisibleInListAfterReloadAsync` (the reload-tolerant helper [BUG-42] added) timed out at 30 s with `rendered cards(0)=[]`, `X-Consistency=none/fresh`: **that diagnosis is WITHDRAWN (2026-08-08)** — the attempt-1 log disproves it three ways: the pre-E2E warm/drain ran and logged `projector caught up to head after 1 poll(s)`; all five cards reads were **gated and fresh** (token `note#751199a4…@4` injected), so the server confirmed the projector had applied the write; and `page.Url` at failure was the **note-detail route**, so the `note-cards` surface was never rendered — `cards(0)=[]` is an absent list, not a lagging one. **Not caused by 48-A** — that slice is desktop-only and inert in the browser (`window.desktop` undefined → toggle renders null, `useTranscription` takes the unchanged cloud path); the other 26 journeys passed and surrounding deploys (#701/#702) were green. Re-run of #703's failed jobs kicked off (fast-follow). **The filed fix direction is a no-op — do not implement it:** that warm/drain shipped in **#277** (TI-39, 2026-06-13), a month before this deploy, and ran successfully here. Open question for the slice: how the page is on `/notes/{id}` when `SaveAndReturnAsync` already awaited the URL leaving `/notes/` — suspect the `navigate(-1)` history traversal vs the helper's `ReloadAsync` hard-load (the interaction BUG-38's note already warns about). Reproduce that first. **ANSWERED + CLOSED 2026-08-09 (TI review):** it hadn't awaited it. `SaveAndReturnAsync`'s `WaitForURLAsync(u => !u.Contains("/notes/"))` landed in [BUG-38] **#418 on 2026-08-07**; before it the helper returned on the Sidebar-mounted `new-note-button`, visible on **every** route — so it could return while still on the note screen, which is exactly `page.Url=/notes/{id}` with a fresh-but-unrendered cards list. #703 (2026-07-23) predates that fix by two weeks. **Second occurrence found:** deploy **#714** (2026-07-29) failed `FilterBackNavigationJourney` with the identical signature (gated+fresh reads, `page.Url` on the note route, `cards(0)=[]`) — same defect, different journey, also pre-#418. Deploy **#720 is #418's own sha**; **21 consecutive clean deploys #720–#740**, counted per-attempt (`scripts/flake-watch.sh 695 'FilterBackNavigationJourney\|NoteDeleteJourney\|NoteReadYourWritesJourney'`) — the 10-run bar is met twice over. Fixed by [BUG-38] #418 + [BUG-61] #425; no separate fix needed. | Done | BUG-42, 27-RYW, BUG-38, BUG-61 |
| BUG-50 | **Creating a note from the meeting side-panel consistently errors "couldn't be created" and bounces the user to home — but the note WAS created.** Reproduced by report (2026-07-24), consistent. Root cause = the [BUG-44]/[BUG-45] read-after-write class: `POST /notes/from-meeting` (`CalendarHandlers.CreateNoteFromMeeting`) creates the note (4 appends: Create/Rename/SetDate/LinkToCalendar) and returns a plain **`201 Created` with no `X-Consistency-Token`** header; the client `createNoteFromMeeting` (`web/src/api/meetings.ts`) uses `request()` and captures **no** token. So `MeetingsSection.handleCreateNote` → `onOpenNote(noteId, …, isNew)` → `NoteView`'s gated `useNoteDetail`/`getNoteDetail` runs **ungated** (no captured token for the new note) against the async **NoteDetail projection**, which hasn't caught up → **404** → `is404` + `onNotFound` → redirect home (+ error). Consistent because the navigate always beats the projector. **Same gap in `CreateNoteFromNextOccurrence`** (`:138`, also plain `Created`, and `createNoteFromNextOccurrence` captures no token). **Fix (mirror BUG-44):** both endpoints emit `X-Consistency-Token = {noteId.ToStreamId()}@{version}` (the post-`LinkNoteToCalendarEvent` note-stream version, exactly as `NoteRecordingHandlers.cs:64` does), and the two client fns switch to `requestWithResponse` + `captureNoteToken(noteId, response)` so the open-after-create read gates on the projector reaching that write. High priority — user-facing data-confusion (looks like a failure but the note exists, cluttering with duplicates on retry). In-process `Api.Integration` uses the sync-projecting store so it can't reproduce the 404 (assert the header is emitted instead); real proof is the E2E/prod behaviour. **Fixed (#407, deploy 58a63b06):** both endpoints emit the token, both client fns capture it. | Done | BUG-44, BUG-30, 27-RYW |
| BUG-51 | **"Create note for next occurrence" (recurring meetings) never worked — always reports "no future occurrences".** Found by Hawk reviewing the BUG-50 fix. `createNoteFromNextOccurrence` (`web/src/api/meetings.ts`) called `apiFetch(\`${base}/notes/from-next-occurrence\`)` **raw**, bypassing `scopedPath()` — but the route is mounted workspace-scoped at `/w/{workspaceId}/notes/from-next-occurrence` (`CalendarEndpoints.cs:16`). So the unscoped `/api/notes/from-next-occurrence` fell through to `/{proxy+}` → **404**, which the client mapped to `throw new Error("no_future_occurrences")` — the feature failed identically whether or not a future occurrence existed. Masked in tests because `getWorkspaceId()` is null there → `scopedPath` is a no-op → the unscoped test mocks matched. **Fixed (this PR, with BUG-50):** exported `scopedPath` and scoped the call, mirroring `createNoteFromMeeting` (which goes via `request()`). The BUG-50 token-capture on this path now actually runs. Not E2E-provable (the recurring/linked-meeting flow needs Google OAuth, absent from the E2E gate — see 44-B). | Done | BUG-50 |
| BUG-52 | **Local transcription pegs the CPU and a whisper process keeps running after you stop/close the app.** Reported 2026-07-26 (real Windows testing — the earlier "latency fine" test was actually the cloud path). Three faults in the 48-A/B/C engine: (1) whisper children spawned via `child_process.spawn` were **never killed** on stop or app-quit → on Windows they **orphan** and run to completion in the background (a stopped recording still pegged ~50% CPU); (2) every pass ran at **8 threads**, oversubscribing a 4-core laptop → thrash; (3) the stop-time pass used **medium.en** (1.5 GB, ~1.5x realtime) — minutes of full CPU, **doubled** for 1:1 diarization (two passes). **Fixed (Step 1, this PR):** track every whisper child and `killActiveWhisper()` on new-recording start, on discard/stop, and on `app.before-quit` (proven to terminate a real running process); cap threads at `pickThreads` = half the cores (min 1); switch the final/diarization model to **small.en** (~466 MB, ~2.3x realtime). The live path still spawns per 5 s window — its **latency** (~5-7 s) is fixed separately in Step 2. **Done (#409, deploy 58a4837c; installer `1.0.0-20260726.152`):** kill proven against a real process; threads capped; small.en shipped. | Done | 48-A, 48-C |
| BUG-53 | **Local live transcript latency is ~5-7 s (bad).** The 48-A live path waits for a full 5 s window to fill, then spawns a fresh `whisper-cli` (reloading the model + grabbing threads) per window — so a word doesn't appear for ~5-7 s, and the repeated spawns churn CPU. Only surfaced on real local testing (2026-07-26). **Fixed (Step 2):** replaced spawn-per-window with a **resident `whisper-server`** (loads the model once, answers `/inference` over HTTP) + a pure sliding-window reducer that folds overlapping re-transcriptions into a growing transcript, emitting the live text ~every 1.5 s (~3-4 s perceived latency). The native-binding route (`smart-whisper`) was **de-risked and rejected** — its prebuilt lacked CPU-feature dispatch → 15× too slow; the official whisper.cpp server binary keeps runtime dispatch. Silent-failure hardening: server-start failure + a sustained streaming-failure threshold + a mid-recording crash all surface the on-device-failed banner; `/inference` has a 20 s abort; a runaway (never-settling) window is force-committed. Not E2E-provable (needs real capture on Windows — MANUAL-VERIFICATION §BUG-53). **Done (#413, deploy a98c0d9b / run #715):** live latency + resident-process behaviour pending your Windows validation on the next installer. | Done | BUG-52, 48-A |
| BUG-54 | **Every sidebar/home navigation silently kills an in-progress recording.** Clicking Home, a folder, or Unfiled while recording unmounts `NoteView` with no confirmation — the BUG-34 popstate trap only catches browser-Back, and `handleHome` / `handleFolderSelect` / `handleUnfiledSelect` (`web/src/App.tsx`) call `navigate` directly. Pre-existing, **not** a 49-A regression; found by Hawk reviewing 49-A, which added the `requestLeave` / `onRegisterLeaveGuard` seam that makes the fix a one-line wrap per call site (49-A already routes tab switch/close and `openNote` through it). High priority — silent transcript loss, the exact failure BUG-34 exists to prevent. **A FOURTH site was found while fixing: the workspace switcher** (`WorkspaceSwitcher.switchTo`) navigates from its own `useNavigate`, so prop-drilling the guard wouldn't reach it — hence `LeaveGuardContext` (`web/src/components/leaveGuardContext.ts`), which any navigating descendant consults via `useRequestLeave()` (defaulting to "just proceed" with no provider, so component tests are unaffected). All four sites now hand their continuation to the recording note, which runs it after the existing confirm; declining leaves the user exactly where they were, popover and all. Destination side effects (closing the sidebar, opening the folder preview) moved inside the continuation so they don't fire on a declined leave. Review of the fix found **three more** unguarded sites beyond the four: `WorkspaceSwitcher.submitCreate` (create-and-switch, ten lines below `switchTo`), **sign-out** (clears the token → unmounts everything), and **move-to-workspace** (unlike delete the note *survives* the move, so losing the transcript is pure loss) — plus a side effect (`setPreviewFolderId`) still firing outside the continuation, and a stranded confirm when a recording ends by itself mid-prompt. Sign-out additionally forced `handleConfirmedLeave` to **await** the content flush before handing over, or the save 401s against a cleared token. Closing the sidebar deliberately stays *outside* the continuation: on mobile it is an overlay whose scrim would otherwise dim and block the confirm it just raised. **Reproduce-before-fix:** 13 App-level specs in `web/src/__tests__/RecordingNavigationGuard.test.tsx` — 11 red first (the confirm appeared on none of the paths); the 2 negatives are green pre-fix by design. Includes a *stale-guard* negative (record → stop → navigate → no prompt), which the first-draft negative could not catch because it never registered a guard at all. Not E2E-provable (the gate has no microphone). **Fixed (#412, deploy run 30481183307 — green on re-run after a [BUG-38] flake; `deploy-production` succeeded, so it is live).** **Deliberate UX call:** pressing Stop while the confirm is showing is read as consent and completes the navigation (nothing is left to protect); the transcript relies on the existing unmount flush. **Residual — see [BUG-55]:** in *local* transcription mode the transcript commit runs after a minutes-long finalise, so signing out mid-recording can still 401 that POST; every other destination outlives the navigation. | Done | BUG-34, 49-A |
| BUG-55 | **Signing out mid-recording in LOCAL transcription mode still loses the transcript, despite the [BUG-54] confirm.** "Leave & save" awaits the *content* save before proceeding, but the *transcript* commit is fired asynchronously by `stopRecording()`: in cloud mode `commitTranscript()` dispatches immediately (safe), while in local mode it waits on `window.desktop.local.diarize()/finish()` — the stop-time small.en pass at ~2.3× realtime, doubled for 1:1 diarization, so minutes on a long meeting ([BUG-52]; note [BUG-53] fixed the *live* path's latency, not this finalise) — before an authenticated `completeTranscription` POST (`web/src/hooks/useTranscription.ts`). With sign-out as the continuation the token is cleared long before that POST, so it 401s, `committedRef` is released and nothing retries. Every other destination is unaffected (the request outlives a route change); only sign-out clears auth. Found by Hawk reviewing the BUG-54 fix. **Fix direction:** gate the sign-out continuation on the commit landing (or on `status !== 'finalising'`), rather than only on the content flush. Not E2E-provable (needs local capture on Windows). **Done (#447, merged 2026-08-10).** The sign-out continuation now awaits `awaitCommit()` (the stop sequence, then the commit POST); every other destination is unchanged, because awaiting on all of them would hang ordinary navigation for the whole finalise. `stopRecording` had to be made **idempotent** first — `handleConfirmedLeave` calls it unconditionally and `isRecording` includes `'finalising'`, so Stop-then-sign-out called it twice and the second call reassigned the promise `awaitCommit` waits on, leaving the bug unfixed on exactly the configuration it targets. The wait is **derived**, not a flat constant: `clamp(recordedMs × 1.95, 2 min, 60 min)`, where 1.95 is 50% over the true worst case (BUG-52: `small.en` at ~2.3× realtime, run twice for 1:1 diarization, plus the single-stream fallback when `diarize()` returns null ⇒ ~1.31 × audio). An earlier revision used a flat 5 minutes, which is *below* the finalise time for any meeting over ~4 minutes and so was a no-op on the primary case. **Residual:** above ~31 minutes of audio the 60-minute ceiling binds before the derived deadline, so a very long local 1:1 can still expire mid-pass and lose the transcript; the ceiling is deliberate (a wedged engine must not strand the user forever) and a "Sign out now" escape during the wait would remove the trade — see [BUG-73]. Expiry logs and emits `transcriptCommitDeadlineExpired` rather than failing silently. | Done | BUG-54, 48-A |
| BUG-56 | **On-device transcription shows nothing at all while recording, then takes a very long time to appear after you stop.** Reported 2026-08-06 on real Windows testing — the [BUG-53] Windows validation coming back negative. **Root-caused 2026-08-07 from the code: three stacked faults, and the originally-filed silent-stall hypothesis was the third, not the cause.** (1) The resident server has **never started since BUG-53 shipped** — `WhisperServer` was spawned with `whisperBinPath()` = **`whisper-cli.exe`**, but `whisper-server` is a *different binary* in the same bundle and only it accepts `--host`/`--port`; the CLI exits on the unknown argument. `whisper-server.exe` ships in the installer already; nothing pointed at it. (2) The failure was **invisible** because `RecordControl.tsx:120` rendered the banner only when `status === "error"`, while the on-device handler deliberately leaves `status === "recording"` — so `local:error` fired, `setError` ran, and the message was never displayed. (3) `StreamingSession.step()` returns quietly while `!server.ready`, so *process alive, never ready* was silent forever. The slow-after-stop half is the unchanged [BUG-52] `small.en` CLI pass over the whole recording (~2.3× realtime) — expected cost, not a defect. Every test stayed green because `whisperServer.integration.spec.ts` takes its binary from a **different** env var (`WHISPER_SERVER_BIN`) supplied by hand — nothing asserted production wires the same one. **Fix (PR #426):** a `whisperServerBinPath()` resolver used by `local:start`; the banner renders whenever `error` is set; a 45 s ready deadline in `StreamingSession` (deliberately BELOW `WhisperServer`'s 60 s start timeout, which kills the child on give-up — a deadline above it can only ever observe a dead process, and the first draft shipped at 75 s as exactly that dead code, caught on review); a `ChildProcess` `'error'` handler in `WhisperServer.start()` (a missing binary would otherwise throw inside the Electron main process); packaging fails unless **both** binaries staged, and stages only the two binaries actually invoked. **Done (#426, deploy #729; installer republished).** Three adversarial review rounds each caught the fix reproducing the bug's own shape — a spec that silently skipped the only real-binary proof, a ready deadline that could never fire, and an error sent to a detached listener; see [phase-bug56-the-live-path-that-never-ran](../learnings/phase-bug56-the-live-path-that-never-ran.md). **The live path has still never been observed working on Windows** — MANUAL-VERIFICATION §BUG-56 rows 2-3 are the first real evidence. | Done | BUG-53, BUG-52, 48-A |
| BUG-57 | **`OpenNoteTabsJourney` red-gated deploy #717 with an empty cards list — ROOT-CAUSED: a vacuous test assertion, not a data problem.** The diagnostic read `rendered cards(0)=[]` with all five `/notes/cards` reads `200 X-Consistency=none/fresh`, and — the tell everyone including me missed — **`page.Url=…/notes/ffbc9c94-…`**. The test was still on the **note screen**; the cards list rendered nothing because it was never on the page. Cause: `data-testid="new-note-button"` lives in the **Sidebar**, which `App.tsx:406` renders *outside* `<Routes>`, so it is visible on every route including a note. `AppPage.AssertHomeLoadedAsync` and `SaveAndReturnAsync` waited only on that button, so both returned **without the navigation having happened**. Compounding it, `handleBackFromNote` uses `navigate(-1)` — a history traversal the spec queues as a later task — so a caller's next `ReloadAsync()` hard-loaded the address bar's current URL (still the note) and discarded the pending traversal; the reload loop then hunted `note-cards` on the note page for 30 s. **All three hypotheses originally filed here — stale `proj-position`, a cold/lagging projector, and a workspace-scoping mismatch — are wrong**, and no amount of consistency-token work could have fixed it. Fix (PR #418): both helpers `WaitForURLAsync` off the note route before asserting; consider a home-only test id. Found by independent root-cause analysis, not by re-running. **Fixed and shipped 2026-08-07 (#418, live on deploy #722).** `AssertHomeLoadedAsync`/`SaveAndReturnAsync` now `WaitForURLAsync` off the note route before asserting. The home-only test id this row asked for landed as [BUG-61]'s `home-view`. No `OpenNoteTabsJourney` recurrence in #722-#727.| Done | 49-A |
| BUG-58 | **"Generate final notes" hangs for 29 s and then fails — the Bedrock analysis call has no timeout, so the Command Lambda is killed instead.** 4 occurrences in 14 days of prod (obs-review 2026-08-06), every one a `Duration: 29000.00 ms … Status: timeout` on the Command Lambda. X-Ray shows the analyse request's three DynamoDB reads finishing inside 150 ms and then **28.9 s of nothing** — `BedrockAnalysisService.ConverseAsync` is awaited with no client-side deadline, and the Lambda's 29 s limit fires first. The user gets a spinner that dies at ~29 s with no honest error, and the note is never analysed. **The failure is nearly invisible:** the process is killed mid-call, so there is no exception, no `Error`/`Warning` log line, no `CommandFailed`/analysis metric, and `notetaker-analysis-failed` stays OK — the "All errors" dashboard widget shows nothing. Only the `notetaker-error-rate` alarm noticed (it did fire, twice). Fix direction: a client-side deadline on the analyse path so exhaustion returns the existing `AnalysisOutcome.ServiceUnavailable` → 503 + a logged Error + metric; plus an alarm on Lambda timeouts so a silent kill is never invisible again. **In progress (PR #424):** client-side deadline + Lambda-timeout alarm, Hawk findings addressed; PR CI all green and `MERGEABLE`/`CLEAN`, awaiting merge. **Sized from prod data (2026-08-07), not guessed:** 90d of command-hosted Converse calls (n=16) are median 2.6s but 17.9 / 21.5 / 23.6 / 26.4s in the tail; pairing each against its invocation gives a post-Bedrock tail of **3.0-5.9s** (the sequential `TagNote`-per-tag and `AddActionItem`-per-action appends). 29s Lambda minus a 6s worst tail = **23s deadline** (the first draft's 20s would have turned the 21.5s and 23.6s calls — which succeeded at 24.5s and 27.7s total — into 503s). **Residual, accepted:** an analysis whose Bedrock call lands just under 23s can still be killed if its tail runs long, and the kill would leave a note with a new summary and tags but no action items, since the appends are sequential and nothing marks the analysis incomplete. The new `notetaker-command-lambda-timeout` alarm makes that visible rather than silent. The durable fix is moving analysis off the synchronous request path (filed as TI-63), not a larger number — anything needing >23s+tail cannot fit a 29s Lambda at all. **Done — PR #424, deploy #734 (2026-08-07), `deploy-production` confirmed.** | Done | — |
| BUG-59 | **Typing into a note that has been deleted tells you to "try again" forever — every save is rejected and the text only ever lives in the browser tab.** Prod evidence (obs-review 2026-08-06): note `b721c995…` was created 2026-07-24 14:34:09, deleted at 14:34:21 (`NoteDeleted` v7), and then took a `RenameNote` at 14:34:33 and five `EditContent` saves at 14:37 → 15:05 — **31 minutes of a real browser session**, all 404. `NoteView.handleSaveContent` maps every save failure to the retriable "Couldn't save your note. We kept your text — try again." toast, but a deleted note is **terminal**: retrying can never succeed, the view never reconciles (the cached detail is not invalidated, so the existing `is404 → onNotFound` bounce never fires), and closing the tab loses the work. Explicitly deferred as out-of-scope by [BUG-44]; now has prod evidence. Fix direction: treat a 404 from a note write as terminal — invalidate the note query and show a "this note was deleted" state that offers the kept text for recovery, instead of "try again". **Done (#439, deploy #746).** The rescued text lives in a module-level store rendered by `App` **above the router**, so the note unmounting — now the correct response to a deleted note — cannot destroy it, and no `keys.note` invalidation can evict it. The doomed writes stop (a latched ref refuses every later save). Every note-level 404 from `EditContent` is byte-identical via one `NoteGone()` helper: an earlier revision split "deleted" from "not yours" so the message would never be false, but that hands back a reliable existence oracle, and not leaking existence is the stronger property. An API Gateway route miss never reaches the handler, so it stays bare and the 34-B skew case is unaffected. | Done | BUG-44 |
| BUG-60 | **A browser that refuses storage crashes the app on the auth/calendar paths.** `localStorage`/`sessionStorage` access THROWS (rather than returning null) in private mode or at quota, and four sites are unguarded: `web/src/auth/AuthContext.tsx:42` — evaluated **during render** on any OAuth return, so it takes the whole tree down — plus `:148-160`, `:176-182`, and `signIn` at `:222-228`. Found while building 49-B, whose acceptance criterion is exactly "storage unavailable leaves the app fully working"; that slice fixed the one site it touched (`AppGate`'s `postLoginRedirect`) and left these, since they are auth-path changes deserving their own review. **Fix direction:** reuse the existing safe-accessor shape in `web/src/api/consistencyTokens.ts` rather than inlining a fifth try/catch. Not E2E-provable (the gate's browser allows storage); a vitest spec stubbing `Storage.prototype` to throw reproduces it, as 49-B's does for the `AppGate` site. **Attempt 1 (PR #440) was NOT merged** — it fixed the render-phase crash but made `signIn` worse: the mechanical guard dropped the PKCE verifier write and **still redirected**, so the OAuth return found no verifier and dead-ended on the sign-in page with a spent `?code=`, an unbounded silent loop. **Done (#440, deploy #748).** `safeStorage.verifiedSet` writes then **reads back** (a `setItem` that does not throw is not evidence the value is there — Safari private mode discarded writes silently for years), and `signIn`/`startCalendarConnect` refuse to navigate when it fails, saying why. A callback carrying a code with no verifier strips `?code=&state=` instead of leaving it to be replayed. `pkce.ts`'s four calendar writes and `chunkReload.ts` are guarded too — the latter needed more than a swallow, since its flag is the only thing bounding the self-heal to one reload, so an unpersistable flag now means **do not reload**. `store()`'s try/catch was **removed**: deleting it left every test green because each accessor already wraps the property access — a guard that cannot fire. The redirect goes through `lib/hardRedirect` so a test can assert the non-event (jsdom refuses to stub `window.location`), enforced by an eslint rule. **Residual:** the `authStorageBlocked` RUM events are inert until [TI-67] enables custom events on the app monitor. | Done | 49-B |
| BUG-61 | **Deploy #720 red-gated on `AssertHomeLoadedAsync` — [BUG-38]'s new home assertion is absent on an empty home.** [BUG-38]/[BUG-57] (PR #418) replaced the helper's vacuous `new-note-button` wait (Sidebar-mounted, so visible on *every* route — it proved nothing) with `WaitForURLAsync` + `note-cards`. The route wait is correct and stays. `note-cards` is not: `ListView` renders it **only** when the list is non-empty (`homeCards.length > 0` at `ListView.tsx:564`, `filteredCards.length > 0` at `:401`), so a legitimately empty home has no such element. `WorkspaceThemeJourney.SetWorkspaceTheme_PersistsAcrossReload` failed at `AppPage.cs:112` for exactly this reason (16 s, `ToBeVisibleAsync` on an empty default workspace). **Fix:** a `data-testid="home-view"` on `ListView`'s root `<main>` — present whether or not the workspace has notes, and `ListView` renders only on the home/folder route, so the assertion still proves the route rather than merely the app shell. Applied to `SaveAndReturnAsync` too, which had the same Sidebar-`new-note-button` vacuity. **Not a regression in #418** — #418 exposed it; the previous assertion passed vacuously in both states. **Second, SEPARATE failure in the same run, still open — see [BUG-62]:** `DeepLinkJourney.Hard_loading_a_missing_note_url_recovers_to_home` timed out at `AppPage.cs:109`, the **URL wait**, not the visibility assert — so it is not explained by this fix. **Confirmed fixed — deploy #721** (the first after PR #425) ran 27 passed / 1 failed: `WorkspaceThemeJourney` passed and the only remaining failure was `DeepLinkJourney`, isolating it cleanly to [BUG-62]. Stays `In Progress` until the gate is fully green and the flake-fix bar is met. **Confirmed fixed and live** — deploy #721 (first after PR #425) isolated it exactly as intended (27 passed / 1 failed, `WorkspaceThemeJourney` green, only `DeepLinkJourney` left), and #722 onward are fully green. Shipped to prod on #722.| Done | BUG-38, BUG-57 |
| BUG-62 | **A malformed note link dead-ends on a broken note screen instead of recovering to home — and its E2E has been passing vacuously.** In deploy #720 `DeepLinkJourney.Hard_loading_a_missing_note_url_recovers_to_home` timed out after 30 s at `AppPage.cs:109` — the `WaitForURLAsync` — so the app **stayed on** `/w/__default__/notes/missing-…` and the 21-C recovery never fired. **Root cause (confirmed by code reading):** `NoteHandlers.GetNote` binds its route parameter as a **`Guid`** (`src/Api/Handlers/NoteHandlers.cs:89`). The journey's id is `missing-{Guid:N}` — the literal prefix `missing-` makes it unparseable — so minimal-API parameter binding fails and returns **400**, never reaching the handler's `Results.NotFound()`. The client throws `GET /notes/… failed: 400` (`web/src/api/client.ts:139`), and `NoteView.tsx:184` gates recovery on `is404 = error.message.includes("404")`, so `is404` is false, `handleNotFound` (`App.tsx:507-511`) never runs, `NoteView` stays mounted and the URL never changes. **A valid-but-missing id is fine** — `ApiIntegrationTests.WhenNoNoteExists_GetNoteDetails_ReturnsNotFound` uses a real `Guid.NewGuid()`, gets a true 404, and recovers. Only the **malformed** id path is broken, and nothing tested it: before [BUG-38]'s PR #418 this journey asserted only the always-visible Sidebar `new-note-button`, so it passed whether or not the redirect fired. Query retry is `retry: 1` (`main.tsx:30`), so backoff does not explain the 30 s. **User impact:** a truncated, corrupted or hand-edited note link (the realistic shared-link failure) dead-ends on a broken note screen with no way back, instead of bouncing home with "That note no longer exists." **Fix direction:** treat any terminal client error on the note-detail read as not-found — widen the `is404` gate to the response **status** rather than substring-matching the message (the substring test is itself fragile: a note id containing "404" would false-positive). Add an `Api.Integration` case for a malformed id and keep the E2E on the malformed id so the path stays covered. **Fixed (PR #426):** `ensureOk` now throws an `ApiError` carrying `status` (message unchanged), and `NoteView`'s dead-link gate reads that status, treating **400 and 404** alike — both mean the id in the URL is unusable. The substring test is gone. Regression test `DeepLinkEdges > deep-linking a malformed note id redirects home with a toast` stubs **400** and was confirmed red against the old gate before the fix; the pre-existing sibling case stubs 404, which is exactly why this suite stayed green while the E2E red-gated — the msw double hard-coded the friendlier status the real Guid-binding path never returns. **Fixed and live** — PR #427, deploy #722: E2E 28/28, smoke 11/11, `deploy-production` succeeded (frontend-only change, so `backend=false` is expected and the Build/Deploy frontend + CloudFront invalidation steps all ran).| Done | BUG-61 |
| BUG-63 | **Two quick "send to top/bottom" clicks can leave the To Do list showing a different order than the server has.** Found by review of [CHANGE-34](phase-minor-changes.md) (PR #422) but **not fixed before it merged** (b436cd77). `sendTo` in `TodoSection.tsx` never calls `addBusy`, and the buttons are disabled only by position, so two *different* rows' buttons are clickable back-to-back. React Query runs mutations in parallel and `useReorderTodos` snapshots `previous` per-mutation, so with list `[A,B,C]`: send-to-top on C (R1 snapshots `[A,B,C]`, cache `[C,A,B]`), then immediately on B (R2 snapshots `[C,A,B]`, cache `[B,C,A]`). If R1 fails and R2 succeeds, the rollback restores R1's snapshot `[A,B,C]` while the **server holds `[B,C,A]`** — and nothing reconciles: `useReorderTodos` has no `onSettled` invalidate, `staleTime` is 30s and `refetchOnWindowFocus` is false. Fix: serialise with `scope: { id: 'todos-reorder' }`, add an `onSettled` invalidate, or disable every row's send buttons while `reorder.isPending`. **Related, same PR:** (a) `TodoOrderCommandHandler` has no `ConcurrencyException` retry loop (unlike `NoteCommandHandler`), so a concurrent reorder 409s for a retriable condition — two clicks land ~200ms apart where two drags were seconds apart; (b) activating a send button moves the row to an end, the control becomes `disabled`, and the browser blurs it — focus falls to `document.body`, on what the code comments call the *only* keyboard reorder path since CHANGE-29 removed the arrows; (c) a reorder during an in-flight add posts a `temp-…` id into the order snapshot, so the just-added to-do gets no Position and drops to the bottom. **Fixed (PR #431, merged 2026-08-07).** Guarded the `reorderTo` **choke point** on `reorder.isPending`, which covers every entry point at once — send-to-top/bottom, drag-drop onto a row, and drop onto the Today line — plus `reorder.isPending` on the buttons so the lock is visible. **The per-item `busy` fix does NOT work** and was tried first: order is a property of the WHOLE list, so row A being busy says nothing about row B, leaving exactly this hole; the regression test still showed two POSTs. An `onSettled` invalidate was also tried and dropped — with overlap structurally impossible it only adds a refetch per reorder, and it broke 6 tests whose msw doubles always replay the original order. Regression test `ignores a second send while the first is still saving`, confirmed red first. **Was briefly double-filed as BUG-65** by the other session working this queue; that duplicate row is removed and BUG-63 is canonical.| Done | CHANGE-34 |
| BUG-65 | **On-device live transcription works but is very slow.** Confirmed 2026-08-07 on Windows: after [BUG-56], `whisper-server.exe` is running and live text does appear — but far behind [BUG-53]'s ~3-4 s target. **Diagnosed from the code + the pinned v1.9.1 server source; not yet measured on the machine.** Four compounding causes, none exotic: (1) the live server is spawned with **no streaming flags** — `--audio-ctx` unset, so every `/inference` pays the **full 30 s padded encoder cost** no matter how short the window is (whisper always encodes a padded 30 s mel; this is why "a short window is fast" was never quite true); (2) `--no-fallback` unset, so temperature fallback silently retries a messy segment up to ~6× (0.0 → 1.0 in 0.2 steps) — and a sliding window's clipped boundary words are exactly what trips the fallback heuristics; (3) `maxWindowMs = 8000` means the reducer does not commit until the window exceeds 8 s, so the steady-state re-transcribed window is **8-16 s**, not the ~3 s `whisperServer.ts`'s header comment assumes; (4) threads are `floor(cores/2)` capped at 8 (from [BUG-52]'s CPU-peg fix) — 2 threads on a 4-core laptop. Beam search was **ruled out** by reading the pinned source: `beam_size = -1` → greedy already. **Key asymmetry making this cheap to fix:** the live transcript is *disposable* — 48-B re-transcribes the whole recording with `small.en` at stop and replaces it — so live accuracy can be traded hard for latency with **no effect on the saved transcript**. **Fixed (PR #435):** `--audio-ctx 768` on the live server only, `maxWindowMs`/`hardWindowMs` halved to 4 s/8 s, a **send-time clamp** so the window handed to the engine can never exceed the encoder context (overshooting made whisper truncate the mel while still seeking a full 30 s, skipping audio outright — the old `hardWindowMs` "invariant" bounded nothing, since `finalizedMs` only advances after an inference completes), and a `<userData>/local-transcription.log` recording per-step `window`/`infer`/**rtf**/`dropped`/`clamped` plus a header with the machine's core count — an installed build had no observable channel at all, which is why BUG-56 and BUG-65 both had to be diagnosed by reading code. Threads deliberately **not** raised (that would re-open [BUG-52]'s CPU-peg complaint; reduce work before spending CPU). Expected several-fold, **unmeasured** — the log is what will say. **Shipped #435, deploy #737, installer `1.0.0-20260808.182`. Deliberately NOT Done:** the fix is unverified on hardware, and this bug is a *speed* complaint, so only the `rtf` figure from MANUAL-VERIFICATION §BUG-65 can close it. **One command scores it:** `scripts/check-local-transcription-log.sh` (2026-08-10) reads the log and prints the verdict. **Measured 2026-08-10 on the real log** (2 sessions, 16 cores, `audioCtx=768`/`maxWindow=4000ms` confirmed applied): `rtf` median **0.30** and **0.26**, max 0.78 in the clean session, **0 of 44** and **1 of 25** steps at or above 1.0 — the tuning holds on hardware and the §BUG-65 row-3 criterion is met. The single 1.84 outlier was one 6.5 s stall that recovered on the next step, not a pattern. **CLOSED 2026-08-10** on a third, purpose-made recording (48 steps, 16 cores, with a deliberate mid-recording silence): `rtf` median **0.13** — about 8x faster than realtime — max 1.39 on the very first step only (`committed=0`, model warm-up), **1 of 48** steps at or above 1.0, **1** dropped tick, **0** clamped, **0** failed. Successive sessions ran 0.30 -> 0.26 -> 0.13 as the machine warmed. §BUG-65 rows 2-5 scored by `check-local-transcription-log.sh`; rows 1 and 6 observed by the human during the same recording. Two Hawk rounds each found the fix reproducing the bug's own shape — a no-op flag asserted as working, and a runaway guard the new clamp could pin below its threshold; see [phase-bug65-guards-that-cannot-fire](../learnings/phase-bug65-guards-that-cannot-fire.md). | Done | BUG-56, BUG-53, BUG-52 |
| BUG-66 | **`agendaEditorApi.test.ts` red-gates PR CI on a ProseMirror teardown race — every test passes.** Surfaced 2026-08-08 on **two unrelated branches within minutes**: `slice/bug-48-stale-detail-refetch` (run 31267893979) and `slice/43-h1-agenda-migration` (run 31263151771). Both report **all tests green** (952/952 and 941/941) and then fail the job on a vitest *unhandled error*: `ReferenceError: document is not defined` at `EditorView.get root` → `domSelection` → `DOMObserver.flush` → `Timeout._onTimeout` (prosemirror-view). ~~A ProseMirror `DOMObserver` timer outlives the jsdom environment and fires while a **node-environment** file (`agendaEditorApi.test.ts`) is running, so `document` is gone.~~ **Superseded — that reading was wrong:** `agendaEditorApi.test.ts` carries no `@vitest-environment` docblock (`Favicon.test.ts` is the repo's only node-environment file); the timer outlives the environment of its OWN file. Real mechanism below. **Non-deterministic** — the same file was green on run 31252043731 for the same branch two commits earlier — so a re-run unblocks, but it will recur and it red-gates *every* PR. **Root cause confirmed in the dependency source (2026-08-09), and independently re-verified on review:** `DOMObserver.stop()` (`prosemirror-view/dist/index.js:4644`) arms `window.setTimeout(() => this.flush(), 20)` whenever mutation records are pending, and `stop()` runs on **every** state update, not just teardown. That timer bypasses `flushSoon()`, so `flushingSoon` stays `-1` and `!view.docView` — which `EditorView.destroy()` sets — is the **only** guard between it and the crash. Two conditions decide exposure, and `agendaEditorApi.test.ts` was the only place both held: (a) content applied **after** construction queues records, whereas the three round-trip suites pass `content` to the constructor, before `start()`, and never queue any; (b) the editors are **detached** (no `element`), so `EditorView.get root`'s ancestor walk finds no document node and falls through to the bare global — a *mounted* editor caches the real `document` and structurally cannot throw this, which is why the app itself never sees it. It created **19** editors and destroyed none; the **five** other editor suites all already destroy (`blankLinePreservation` and `emojiShortcode` also queue records, so the pattern was understood, just not applied here). **Fixed:** `makeEditor` registers each editor and an `afterEach` destroys them; `emojiShortcode.test.ts` moved its three end-of-body destroys into an `afterEach` too, since an assertion failure skipped them and stacked a spurious ReferenceError on the real failure. Isolation (`poolMatchGlobs`/`environmentMatchGlobs`) is **not** needed and was not used — it would cost CI time for a defect the destroy removes at source. **Proof:** an `afterAll` asserts both that the registry is non-empty and that every editor in it is destroyed — review demonstrated that the destroyed-check *alone* stays green if the `push` in `makeEditor` is dropped, the vacuous-guard shape of [phase-bug65-guards-that-cannot-fire.md](../learnings/phase-bug65-guards-that-cannot-fire.md). Deleting the `afterEach` is red (verified). A teardown simulation was tried first and rejected — `document` is non-configurable under vitest, so it cannot be deleted from a test, and the fix rests on the source reading rather than on a local green run (local runs use `vmThreads`; CI uses `forks`, a different teardown). **Closure bar (per [BUG-64]'s precedent, not `flake-watch.sh`, which greps *deploy* logs for a journey regex):** 10 clean runs of the **`frontend` job on the PR Checks workflow**, counted **per-attempt**, each run confirmed to contain the fix commit via `git merge-base --is-ancestor`. **BAR MET — 10/10 (2026-08-10 04:00Z).** Clean and confirmed to contain fix commit `38613c3b`: PR-CI runs **#759, #761, #763, #764, #766, #767, #768, #769, #770, #771** — every one single-attempt, `frontend` green, with **zero** `document is not defined` unhandled errors since the fix. Runs #755-758, #760, #762, #765 were excluded throughout: a parallel session's branches cut *before* the fix, where `merge-base --is-ancestor` is false and a green run is not evidence. The ten span four different slices (BUG-59, BUG-60, BUG-55 and docs-only pushes), so they cover varied timing and code rather than repeated no-op triggers. Note the bar counts *PR Checks* runs, not deploys — a merge does not add one. Runs #755-758, #760, #762, #765 are excluded: they are a parallel session's branches cut *before* the fix, so `merge-base --is-ancestor` is false and a green run on them is not evidence. Reproduce the count with: `gh run list --workflow "PR Checks" --limit 40 --json databaseId,number,headSha,createdAt,conclusion`, filter to `createdAt > 2026-08-09T22:19:04Z`, then per run `git merge-base --is-ancestor 38613c3b <headSha>` and `gh api repos/simonkirkham/ai-note-taker/actions/runs/<id>/attempts/<n>/jobs --jq '.jobs[]|select(.name=="frontend")|.conclusion'` across **every** attempt. **Severity:** High — a shared red gate blocks every slice and every parallel session. | Done | — |
| BUG-68 | **Opening and saving a note whose bullet lists are separated by blank lines merges them into one list and loses the separators.** Found while verifying [43-H1]'s prepend against the real prod notes (2026-08-09), by round-tripping stored content through the actual `NoteEditor` extension set (`StarterKit` + `BlankLineParagraph` + `Markdown` + `MarkdownTaskList` + `TaskItem`). Stored body `- a\n- b\n\n \n\n- c` — two bullet lists with a [BUG-40] blank-line paragraph between them, which is what the editor itself writes — parses back as a **single** list `- a\n- b\n- c`: the blank-line paragraphs between list items are dropped and the lists merge. The next save persists the merged version, so the note is permanently reflowed. Round-trip is stable after that (the second pass is a no-op), so this is a one-time loss per note, on first open-and-save. **Pre-existing and independent of 43-H1** — the baseline body alone reproduces it with no migration involved; 43-H1's prepend does not cause it and does not make it worse (the prepended checklist stays its own `taskList`, pinned by a new case in `taskListMarkdownRoundTrip.test.ts`). **Confirmed real prod data:** note `d591ed55` ("Acquire Teams Check in") has exactly this shape. **Evidence is a vitest round-trip, not a click-through** — reproduce in the running app before fixing. **Fix direction:** [BUG-40]'s `blankLineParagraph.ts` preserves a blank line between *blocks*; the gap is a blank line between two *lists*, where markdown-it's list continuation rules swallow it. Likely needs the serializer to emit a separator markdown-it cannot absorb (an empty HTML comment, or alternating bullet markers) — check what the parse actually does before choosing. | Open | — |
| BUG-69 | **`OpenNoteTabs.test.tsx` aborts the pre-commit hook under parallel-session load — every test passes in isolation.** Hit 2026-08-09 committing [43-H1]: the hook's frontend gate reported **952 passed / 1 failed**, the failure being `findAllByTestId('note-card')` timing out in `openFromList` (`OpenNoteTabs.test.tsx:123`, from `:180`). Re-run alone: **15/15 pass**. **Diagnosis — a default-timeout asymmetry exposed by load, the same shape as [BUG-64].** The run's own figures give it away: `tests 210s` but `environment 3111s` and `setup 2196s`, because three worktrees were compiling and running jsdom suites at once (`bug-66-editor-teardown`, `slice-50b-move-across-line`). testing-library's `findBy*` defaults to a **1000 ms** timeout, which is ample idle and hopeless when jsdom construction alone is taking minutes. Nothing about the assertion is wrong; only its budget is. **Not caused by 43-H1** — that slice touches `AgendaFromContent`, the migration handler and one taskList round-trip test; `OpenNoteTabs` covers note tabs and shares no code. **Not yet seen in CI**, which runs on native Linux with the `forks` pool and no competing worktrees, so the practical cost today is a ~25-minute local commit that aborts on an unrelated test and tempts a `--no-verify` habit. **Fix direction:** give `openFromList`'s `findAllByTestId` an explicit timeout the way [BUG-64] gave the Electron waits a shared `APP_BOOT_TIMEOUT_MS`, rather than raising vitest's global — a global bump hides genuinely slow tests. Check the other `findBy*` calls in the file for the same bare default while there. **Widened 2026-08-10 — it is not one test, and not only the frontend.** Committing 43-H2 ran the hook twice and it failed both times on DISJOINT sets of unrelated BACKEND tests: first `ExceptionMappingTests` + `GetNoteTests` + `McpAuthorizationServerTests`, then `AuthObservabilityTests` x2. All 707 passed standalone in **53 s**; under the hook the same suite took **7 m 57 s** then **17 m 24 s**, at machine load **22 rising to 90** with four sibling worktrees running suites concurrently (`slice-51b-open-note-bar`, `prototype-tabs-redesign`, `bug-71-stale-calendar-state`, `bug-74-finalise-wedge`). Disjoint failure sets that each pass in isolation in 6 s is the signature of resource starvation, not a defect in any of those tests. **Consequence:** the local pre-commit gate is effectively unusable whenever peers are active, which is most of the time in this repo's parallel-session workflow — and CLAUDE.md now forbids `--no-verify`, so an agent is stuck between a rule and an unusable gate. **Fix direction is no longer 'raise one timeout':** either bound test concurrency (xUnit `maxParallelThreads` / vitest `poolOptions` sized to available CPU rather than nominal cores), or have the hook detect load and refuse fast with a clear message instead of failing 17 minutes later on a random test, or move the heavy suites out of the hook and lean on CI. Worth deciding deliberately — the current state trains agents to bypass the gate, which is exactly what the rule is trying to prevent. | Open | — |
| BUG-70 | **"+ New Note" while recording leaves an orphan empty note behind when you decline the leave.** `handleNewNote` (`web/src/App.tsx`) creates the note **server-side first** — `createNote` → `setNoteDate` → optional `moveNote` — and only then calls `openNote`, which is where the [BUG-54] recording guard runs. So clicking "+ New Note" mid-recording creates a real, dated, empty note; choosing "Keep recording" abandons the navigation but not the note, which then shows on the home list as an untitled blank card the user never made. Pre-existing since [BUG-54] added the guard; surfaced by [CHANGE-33] review, whose new "Still recording — open the new note?" copy makes the promise explicit (it correctly names a note that does exist). **Fix direction:** ask before creating, not after — route the guard around the whole of `handleNewNote` (`requestLeave(() => void handleNewNote(), "open the new note")`) so a declined leave never reaches the create. Check `handleOpenNextOccurrence` and the `/ai` create-note path in `NoteView` for the same create-then-guard ordering. Frontend-only; no event, projection or endpoint change. | Open | BUG-54 |
| BUG-71 | **The app never finishes loading — a spinner that never resolves, on every visit, with no way out but clearing your browser data.** It happens to anyone who started connecting a calendar and abandoned it partway; the next ordinary sign-in strands, and nothing on screen explains why or offers a route back. **Mechanism:** `AuthContext`'s `isCalendarConnectReturn` checks only that `calendar_state` is non-null — not that the returned `state` matches it. So an ABANDONED calendar connect (user starts it, never completes) leaves `calendar_state` in `sessionStorage`; a later ordinary sign-in return then satisfies `isCalendarConnectReturn`, seeds `authLoading = true`, and takes the sign-in exchange path — which never clears `authLoading`. The gate shows its loading state indefinitely with no way forward but a manual storage clear. Found by review of [BUG-60], which touches that exact derivation but does not introduce this. **Fix direction:** require `returnedState === calState` in the derivation (the effect at `:151` already does exactly that check before acting on it — the derivation is simply weaker than the branch it predicts), and clear `calendar_*` on a sign-in return. **Severity:** Medium — recoverable only by clearing site data, which is precisely what the affected user is least likely to try. **Fix in PR #448 — status stays `Open` until the deploy lands (Scribe closes it).** `isCalendarConnectReturn` now carries the calendar arm's FULL precondition set, including its two early returns: `!initialToken && clientId && code && calState && calVerifier && returnedState === calState`. Two earlier revisions each claimed to match the branch and were disproved by probe — first the missing `calVerifier`, then `has('code')` where the branch needs `code` truthy plus the ignored `initialToken` guard. Each gap stranded the gate identically. Five of the six conjuncts are pinned by a test that reddens when it alone is removed — including `clientId`, which review found unpinned on the round that claimed otherwise. `calState` is pinned too, after review showed it guards `null === null` (a verifier surviving without its state, and a callback carrying no `state` param, would otherwise derive true and strand identically). The derivation seeds `authLoading` and only that arm ever clears it, so predicting an arm that then declines to run is what strands the gate. Review reproduced a SECOND strand through the omitted `calVerifier` clause, with BUG-60's storage-blocked message suppressed by the same true value; both gaps are now pinned. A sign-in return also clears every `calendar_*` marker (hygiene, not load-bearing — a stale marker can no longer match a freshly-minted state). | Open | — |
| BUG-72 | **Navigating away mid-finalise commits the LIVE transcript and silently discards the better on-device one.** `useTranscription`'s unmount effect calls `commitTranscript()` unconditionally. In local mode the stop-time `medium.en` pass (plus 1:1 diarization) runs for minutes after Stop; leaving the note during it commits whatever the live `base.en` path produced, sets the one-shot `committedRef`, and when the higher-quality result lands its `commitTranscript()` early-returns — so the transcript the user keeps is the worse one, with no indication the better one existed. Found by review of [BUG-55], which fixes the *auth* half of the same race (sign-out clearing the token) but not the *quality* half; "every other destination is safe" is true for authentication and not for transcript quality. Same root cause as BUG-55's critical: `commitTranscript` racing the local finalise. **Fix direction:** on unmount, if a stop sequence is in flight, let it finish rather than committing the interim text — the POST already outlives the route change (that is why only sign-out needed the wait). **Severity:** Medium — silent quality loss, local mode only, and only when the user leaves during the finalise window. **Fix in PR #449 — status stays `Open` until the deploy lands (Scribe closes it).** The unmount effect now CHAINS on the in-flight stop sequence rather than committing immediately: it awaits the sequence, then calls `commitTranscript()`. On the happy path that is a no-op (the sequence already latched the one-shot commit with the better text); on a sequence that fails outright it commits the live text, i.e. the pre-fix behaviour. An earlier revision simply *skipped*, which made the sequence the sole commit owner — and it has two ways to never commit (a throw escaping its inner try, and the [BUG-56] hang), so that lost the transcript outright, worse than the bug. That POST outlives a route change; sign-out is the one destination that does not, which is what [BUG-55] waits for. Pinned by a spec that drives the real hook, unmounts mid-finalise, and asserts the committed body is the small.en text and NOT the live one; a second test pins the ordinary case, since a guard that swallowed every unmount commit would lose the transcript outright. | Open | BUG-55 |
| BUG-73 | **A confirmed sign-out can park for up to an hour with no way out.** [BUG-55] makes the sign-out continuation wait for the on-device transcript commit, which is correct — an un-awaited POST 401s and the transcript is lost. But the wait is `clamp(recordedMs × 1.95, 2 min, 60 min)`, so on a long local recording the user who has already confirmed "Leave & save" sees "Finishing the transcript…" and has no exit until it lands or the deadline expires. On a shared machine that is a real problem: they cannot sign out. The same ceiling also caps the protection — above ~31 minutes of audio it binds before the derived deadline, so a very long 1:1 can still expire mid-pass and lose the transcript, which is BUG-55 recurring at a higher threshold. **Fix direction:** a "Sign out now" control in the parked banner that abandons the wait deliberately (warning that the transcript may not save), which removes the trade entirely — the deadline no longer has to be both short enough to be escapable and long enough to be sufficient. Found by review of BUG-55. **Severity:** Medium — no data loss on the common path, but no escape either. | Open | BUG-55 |
| BUG-74 | **The note sticks on "Finalising…" and never moves, and the recording you just made is gone — for anyone who waits on the note rather than navigating away.** No error, no way to retry, and the Stop button will not help. Local transcription only, when the on-device engine fails while shutting down. **Mechanism:** Found by probe during review of [BUG-72]: make `window.desktop.local.discard()` throw (it is called from the local branch's `finally`, so the throw REPLACES the completion and rejects the whole stop sequence) and the sequence never reaches `commitTranscript()` at all, never reaches `setStatus('stopped')`, and surfaces an unhandled rejection. The UI sits on `finalising` indefinitely with no error and no recovery. [BUG-72] fixes exactly this outcome for the user who LEAVES the note (the unmount chains and commits the live text as a fallback) — the stay-mounted half is untouched, so the same failure has two very different endings depending on whether you navigate away. **Fix direction:** wrap the local branch's tail (`localCleanupRef`, `discard()`) so a throw cannot pre-empt the commit + `setStatus`, and surface the failure rather than leaving the status stuck. **Severity:** Medium — silent total transcript loss, local mode only, on an engine fault [BUG-56] documents as real. **Fix in PR #450 — status stays `Open` until the deploy lands (Scribe closes it).** Guarded at the SHAPE, not per call site, after review showed a site-local fix leaves the others: the listener-detach closure is guarded where it is built (so the stop sequence, `cleanup()` and the cloud-fallback path are all covered by one change, and a failing first detach no longer strands the second), `discard()` is guarded in its `finally`, and `cleanup()`'s capture-track teardown is guarded because it runs inside React's effect teardown — a throw there escapes the unmount itself. That third site was found by this bug's own test throwing out of `view.unmount()`. A terminal `.catch` on the sequence closes the class for any site nobody has named, converting a rejection into a settle so the status always reaches `stopped`. Each guard is pinned by a test that reddens when it alone is removed; a fourth pins that the happy path still commits the finalised text, not the live one. Failures now emit a signal rather than only a console warning — though see [TI-67], which is why that signal does not reach production yet. Review then found the fix's own diagnostic was the last unguarded pre-commit throw: `recordRumEvent` calls a third-party global and this bug had placed it inside every catch it added, so a failing telemetry call re-created the exact fault being reported. Guarded once at source, which also covers ~14 call sites elsewhere including corrupt-storage handling. The terminal handler now commits and cleans up as well as restoring the status — without that, an unnamed throw left the user on `stopped` with the transcript uncommitted, the microphone still live and a retry blocked by the idempotency guard. Those lines are **unpinned by design**: with every named site guarded nothing reaches them, which is what a terminal handler is for, and is recorded here rather than left to be rediscovered. The same is true of [BUG-72]'s chain-vs-skip choice — no test now distinguishes them, because this fix removed the only reachable throw that could. Its fallback test was deleted rather than contrived; the guard it protected (`if (stopInFlightRef.current)`) is still pinned by the surviving tests. | Done | BUG-72, BUG-56 |
| BUG-75 | **A note reopened during the on-device finalise shows no transcript until a later navigation or reload.** `commitTranscript` only POSTs — it never invalidates `keys.note` — and the query client runs `staleTime: 30_000` with `refetchOnWindowFocus: false`. Reopening the note before the commit lands therefore serves a cached detail with no transcript, and nothing refetches when the commit completes. The mechanism predates [BUG-72], but that fix **widens the window** on the leave-mid-finalise path from roughly zero (the commit used to fire during the navigation) to the whole finalise duration — minutes — which is why it surfaces now. Mitigated by `detail.transcriptDraft`, which still offers the live text for recovery, and a later real commit supersedes a recovered draft. **Fix direction:** invalidate `keys.note(noteId)` after a successful commit. **Severity:** Low — recoverable and self-correcting on the next navigation. | Open | BUG-72 |
| BUG-76 | **Three ways a note's agenda count disagrees with what the header shows.** All three were found in [43-G](phase-43.md)'s review and carried on the 43-H2 checklist; 43-H2 shipped the removal without them, and with Phase 43 now Done they need their own row or they are lost inside a closed phase. Each is a mismatch between the server's parse of the note body and the editor's live read, and **since 43-H2 the body is the only source**, so a divergence is now the only way the two can disagree — it used to be masked by the legacy union. **(a)** `collectTaskItems` (`web/src/lib/agendaEditorApi.ts:62`) recurses only into `taskList` children, so a tick reachable through a NON-checklist list — `- Shopping` / `  - [ ] Milk`, or a bulleted child under a task item — is counted server-side but invisible in the header. Widening the recursion to a list-type set closes it and cannot reintroduce the blockquote exclusion (a blockquote is not a list). **(b)** A paragraph holding only a non-text inline node (`- [ ] ![shot](key)`) reads as empty client-side and is skipped, while `AgendaFromContent.Parse` counts it. **(c)** The BUG-24 image-resolve calls `setContent` with `emitUpdate: false`, so the editor's live topic list is not republished for it; one extra `publish()` in that `.then` makes the live list unconditionally authoritative. **User impact:** the "X / Y" pill disagrees with the ticks visible in the note, and a topic that is counted but not shown cannot be ticked from the header. Low frequency — each needs a specific note shape — but (a) is the likeliest, since indenting a checklist under a plain bullet is ordinary note-taking. | Open | 43-G, 43-H2 |
| BUG-78 | **You are dropped at the sign-in screen instead of straight back into your notes, and told your browser is blocking storage — a message that returns on every reload, for as long as you keep that link.** Reachable from a hand-edited or truncated sign-in link. The session that would have restored you silently never runs. **Mechanism:** `AuthContext` derives `hasOAuthCode` from `searchParams.has('code')`, but the exchange effect requires a TRUTHY `code` at every branch. The two derivations that consume `hasOAuthCode` therefore diverge from the effect they predict. (1) `shouldBootstrapRefresh` is suppressed, so a user arriving at `/?code=` with a valid refresh cookie is shown the sign-in screen instead of being restored — the [BUG-15] regression that derivation exists to prevent (reproduced: zero refresh calls). (2) [BUG-60]'s `storageBlocked` seed renders its "browser is blocking storage" message, while the effect arm that would strip `?code=` and emit the signal declines to run — **so the code stays in the address bar and the message returns on every reload**, which is exactly the indefinite loop BUG-60's strip exists to break. A third consumer diverges the same way: with an `initialToken` present the effect returns before any arm, so the message shows and nothing is stripped — E2E-only (`window.__E2E_AUTH_TOKEN`), no real user reaches it. All found by review of [BUG-71]; all **pre-existing** and not regressed by it, which is why that PR keeps `has('code')` rather than folding a session-restore change into a gate-strand fix. **Fix direction:** derive all three consumers from a truthy `code`, and note that `staleCalendarState.test.tsx`'s empty-code case asserts today's behaviour — this fix owns flipping it. **Severity:** Low — needs a hand-edited or truncated address to reach. | Open | BUG-71, BUG-60, BUG-15 |
| BUG-67 | **After you press Stop, the live on-device engine keeps re-transcribing the same stale audio — competing for CPU with the pass you are waiting for.** Found 2026-08-09 in the first real [BUG-65] log, not by report. Evidence: 12 consecutive steps with `window=3810ms` and `committed=628` **both frozen** while inference climbed 1.1 s → 3.0 s over ~30 s. **Two causes compounding.** (1) `StreamingSession.step()` has no *new-audio* check: `MIN_NEW_MS` gates on `byteLen - startByte`, which measures the WINDOW SIZE, not arrivals since the last step — so once PCM stops, any window over 500 ms re-runs forever. (2) `stopRecording` sets `stoppedRef` (halting PCM) and then awaits `local:diarize` — two `whisper-cli` passes at ~2.3× realtime — **before** anything stops the live session; `local:finish` does call `s.stop()` but is only reached afterwards, and the 48-C path may call `discard` instead. So the live engine spins on stale audio for the whole diarize pass, on the same cores. **A contributing cause of the "very slow after stop" half of [BUG-56]**, which was attributed wholly to `small.en`'s expected cost. No data loss (the tail still renders live, and the final pass replaces it) — wasted CPU that directly slows the thing the user is waiting for. **Fixed (PR #443):** `step()` returns early when the audio position it would consume has not advanced since the last step actually run — idle detection, not a latch, and it re-arms after a pause. Crucially it records what the step **consumed**, not what was buffered: a clamped step (BUG-65's send cap) deliberately leaves a tail waiting, and marking that consumed would have meant the tail was never transcribed live once audio stopped — the two fixes silently cancelling at exactly the end-of-audio case the clamp exists for. Caught on review, reproduced as a spec first. **Halting the session at `local:diarize` was tried and dropped:** it bought about one step over the guard while costing the final in-flight window's follow-up, and made a handler documented as stateless depend on the live session. **Shipped #443, deploy #741, installer `1.0.0-20260809.186`. Not Done:** the symptom is CPU burn with nothing on screen, so only the log closes it — MANUAL-VERIFICATION §BUG-67, scored by `scripts/check-local-transcription-log.sh` alongside [BUG-65] from the same recording. **Confirmed fixed on hardware 2026-08-10**, with the pre-fix control in the same log: the session an hour BEFORE #443 shipped shows a **14-step** frozen run (window and committed both static — the reported shape), the session after shows **1**. It also confirms this bug's own causal claim that the spin was stealing cores from the pass being waited on: steps dropping a tick fell from **12 of 44 (27%)** to **2 of 25 (8%)**. **CLOSED 2026-08-10** — the re-arm was proved on a recording with a deliberate silence: the transcript stood still at `committed=352` for **24 s** across 17 steps while the window kept cycling 3000-9000 ms, then grew again (396 -> 670) the moment speech resumed. So the guard suppresses work during silence and does **not** latch off. **A person going quiet does not stop the step lines** — the microphone keeps delivering audio, so the engine keeps running and transcribes nothing; the idle guard only fires when the stream truly stops, i.e. at Stop. The first version of the checker looked for a gap BETWEEN log lines and therefore returned a false negative on a recording that had in fact demonstrated the behaviour. Review also caught the *spec* passing on the very spin it forbade (cap set below `hardWindowMs`, so the runaway guard could not fire and the assertion was satisfied BY the spin); corrected and verified by reverting the production fix to confirm it fails. | Done | BUG-65, 48-C |
| BUG-77 | **You finish a recording, the note is never analysed, and all you are told is "Analysis failed. Please try again."** Hit live 2026-08-10 ~14:50Z on the desktop app (`1.0.0-20260810.196`); the same note analysed fine 30 min later. **CONFIRMED DEFECT — the mis-reporting.** `RecordControl.tsx:73` catches with a bare `catch {}` and discards the error, so a dead network, an expired sign-in, a refused request and a parse error all print one sentence that names the wrong subsystem and advises a retry that may be incapable of working. Compounded by [TI-67] (RUM `CustomEvents: DISABLED` in prod) so nothing client-side is recorded either. **This is fixable now and independently of the trigger:** keep the status and message, and let an unauthenticated result say *sign in again* rather than blaming analysis. **TRIGGER — STILL OPEN.** **A previous version of this row asserted the cause was a terminally expired sign-in. That was WRONG and is retracted.** The reasoning was: `apiFetch` (`client.ts:112-118`) pre-flights every call and, if the JWT is expired and the silent refresh fails, returns a synthetic `new Response(null, {status:401})` **without sending** — which would produce exactly this silent, traceless failure; and the refresh endpoint was observed failing every time. **What killed it:** the session was never invalid. `CompleteTranscription` succeeded at 14:49:22.190Z on a valid token, and analysis succeeded on the **same note** at 15:19:31Z (`RecordAnalysisSummary`, Version=5) **with no re-sign-in** — while the refresh endpoint went on failing at 15:02, 15:07 and 15:15. **"Session refresh: no rt cookie present" is continuous background noise here, not a cause:** 16+ occurrences on 2026-08-10, always in pairs ~1.2 s apart, never once succeeding, and never once preventing an authenticated call. That is [BUG-33]'s signature (warm-tab refresh not trying the cookie), not an expiry. The synthetic-401-without-sending path remains real code and a real candidate, but **a continuously-failing refresh that usually breaks nothing cannot on its own explain a failure that happens only sometimes** — anything proposed as the trigger must explain the intermittency. **Established facts to build on.** No `/analyse` request appears in gateway `Count` at 14:49-14:58Z (per-minute 14:46-14:49Z = 12/15/3/3, then **zero** 14:50-14:57Z); the 2 `4xx` at 14:58:19.4/20.7Z are exactly the two refresh calls behind the human's two "try again" clicks. The Command Lambda was **cold** at 14:49:21 (`cold_start: true`), and the analyse path is slow — the 15:19 success took ~15 s end to end including a **4.4 s** ICS calendar fetch before Bedrock. Whether a client-side deadline interacts with that is untested. **Unresolvable with current instrumentation:** access logging is `None` on `$default` and X-Ray's Lambda segments carry no HTTP URL, so the single `4xx` at 14:49Z cannot be attributed. **Do instrumentation before any further theory** — widen the catch and land [TI-67]; a second occurrence is otherwise as blind as the first. [TI-63] and [BUG-58] are not ruled out as cleanly as first recorded: both would log *if the request arrived*, and arrival is exactly what is unestablished. **Process note:** two sessions in sequence stated a cause with more confidence than the evidence carried, and it reached the human as fact. The disconfirming datum — an authenticated call succeeding inside the same window — was present in the log that was being read at the time. | Open | TI-67, BUG-33 |
| BUG-64 | **`shell.e2e.ts` flakes the new desktop PR gate on a cold Electron launch.** Surfaced 2026-08-07 on the very first PR gated by [TI-58] (#426, run 31171890278): `launches and renders the bundled frontend from local assets` failed on `getByRole('sign in with google')` after 5 s, while `renderer has no Node integration` passed on the **same** launch. Not a product fault and not caused by the slice — a timeout asymmetry between the two tests. The failing one asserts with `expect(...).toBeVisible()`, which uses Playwright's **5 s expect timeout**; the passing one waits with `locator.waitFor()`, which defaults to **30 s**. A cold CI Electron (fresh process + xvfb + the SPA's first paint) routinely exceeds 5 s, so only the assertion-style wait was ever at risk. Re-run went green, confirming non-determinism. **Fixed (in #426, to unblock the shared gate):** both waits now share an explicit `APP_BOOT_TIMEOUT_MS = 30_000`. Per the flake-fix proof bar this needed 10 clean runs of the desktop job. Counted per-attempt — a job re-run into green still contains the failure and does not count — and each run was additionally confirmed to CONTAIN the fix commit `b320f3b2` (`git merge-base --is-ancestor`), since a green run on a branch cut before the fix is not evidence for it. **Bar MET at 10/10 (2026-08-08).** `flake-watch.sh` does not apply here — it greps *deploy* logs for a journey regex, whereas this needs the `desktop` **job conclusion** on the **PR Checks** workflow. Reproduce with: `gh run list --workflow "PR Checks" --limit 60 --json databaseId,number,createdAt,conclusion` filtered to runs after the fix commit `b320f3b2` (2026-08-07T12:03Z), then for each run id `gh api repos/simonkirkham/ai-note-taker/actions/runs/<id>/attempts/<n>/jobs --jq '.jobs[]|select(.name=="desktop")|.conclusion'` across **every** attempt. Clean so far: PR-CI runs #718, #719, #720, #722, #723, #724, #725, #726, #727 — all single-attempt, `desktop` green. **One more clean run closes it.** Run #721 was cancelled (superseded by #722 on the same branch) and counts neither way. | Done | TI-58 |

Further bugs will be appended as they are identified.

---

> **Fixed bugs are condensed in [phase-bugs-archive.md](phase-bugs-archive.md)** (one terse entry each, anchors preserved). The Summary table above stays the full index; only **open** and **in-progress** defects keep a detailed section below.

---

## BUG-38 — `TagsJourney` deploy-gate flake — a network-timed autofocus steals focus and unmounts the tag input

**Status:** In Progress — root-caused 2026-08-06 after ~4 months of wrong theories; fix in PR #418, awaiting the clean-run bar. **Severity:** High — the worst single deploy-gate flake by a wide margin, **and a real user bug**, not a test artefact.

**Measured scale (per-attempt scan of deploys #627–#719, 93 deploys):** 58 failing-test occurrences, of which **26 (45%) are `TagsJourney`** — nearly 4× the next worst journey — across 15 deploys and hitting **all six** of its tests (`AddTag_PillAppearsOnNoteScreen` 8, `AddTag_PersistsAfterNavigation` 7, `AddMultipleTags_SpaceSeparated` 4, `AddTag_PillAppearsOnHomeCard` 3, `RemoveTag_PillDisappears` 2, `RemoveTag_GoneAfterNavigation` 2). This doc previously recorded **6** recurrences — the 4× undercount is failures that were re-run into green and never written down, which is why the standing rule is to count per-attempt with `scripts/flake-watch.sh`, never by reading run conclusions.

**Symptom:** a `TagsJourney` test times out at 30 s waiting for `GetByTestId("tag-input")` in `AppPage.AddTagAsync`.

**Root cause (`web/src/components/NoteView.tsx:267-269`):**
```
useEffect(() => { if (!loadingDetail && !notFound) inputRef.current?.focus(); }, [loadingDetail, notFound])
```
This focuses the **title** input. It does **not** fire on mount — it fires when the RYW-gated `GET /notes/{id}` resolves, i.e. at an arbitrary wall-clock moment (~150 ms to the 8 s gate cap) while the whole screen, `CommandBar` included, has been interactive the entire time. The tag combobox dismisses itself on blur (`TagCombobox.tsx:107` → `CommandBar.tsx:81` `setAddingTag(false)`), which **unmounts** the input — so the focus grab destroys it mid-interaction. `AddTagAsync` fills then presses Enter as two Playwright round trips; a steal landing between them detaches the element Playwright already resolved, and because `addingTag` is now false, `tag-input` never returns.

**Enter is not the trigger** — Playwright's `press` checks connectedness *before* dispatching the key, so the detach necessarily preceded the keystroke.

**Real user impact:** start typing a tag on a freshly-opened note and the box can be ripped away, with your half-typed text silently submitted.

**Every previous theory is retired:**
| Retired theory | Disproved by |
|---|---|
| Cold start / idle gap | #649/#652/#654/#656 are same-or-next-day; #719 failed 43 s in, after 27 tests had already passed (warm) |
| Projector lag / stale gated read | Lag cannot detach a DOM node; the failing call is an **action**, and the assertions were already reload-tolerant |
| The app never loaded | The #719 log shows the input **resolved**, with `value="1:1s"` |
| A `notFound` bounce | Not reachable on a note that had already rendered |

Lag is the trigger's **timing source**, never the fault — which is why every consistency-token and reload-tolerance fix looked relevant and none worked.

**Method note worth keeping:** the breakthrough came from the deploy #719 call log, the first to carry a `locator resolved to …` line. Prior recurrences gave bare timeouts because the enriched diagnostic (#372) wrapped only `FillAsync`, **not** `PressAsync` — an action this row flagged as outstanding for months, and which cost the root cause every time. Confirmed independently by three root-cause agents plus review, each reproducing the unmount in jsdom.

**Fix (PR #418):**
1. Make the autofocus **mount-only** — once, and only when nothing else holds focus — so no network-timed `focus()` remains on the note screen.
2. Widen the `AddTagAsync` catch to cover `PressAsync` + `TimeoutException`.
3. A regression test that fails against the old effect.

**Same mechanism explains `ActionEditJourney`** (5 occurrences) — `ActionsSection` has the identical blur-to-unmount shape.

**Bar for Done:** per the flake-fix rule, 10 clean deploy runs counted per-attempt with `scripts/flake-watch.sh`, not on merge.

**Key files:** `web/src/components/NoteView.tsx:267-269`, `web/src/components/TagCombobox.tsx:107`, `web/src/components/CommandBar.tsx:81`, `tests/Browser.E2E/AppPage.cs` (`AddTagAsync`).

---

## BUG-46 — Folder delete not note-cards-gated → contained notes briefly shown under the deleted folder

**Status:** **Done** — PR #437, deploy #739 (2026-08-08), live in prod. Exact for one note per folder; 1-of-N and an arbitrary (scan-order) stream otherwise — see the summary row. **Severity:** Low — the folder is correctly removed from the tree (gated by `FOLDERS_SCOPE`); only the notes' placement in the cards list lags the projector. No data loss. Found by Hawk reviewing #384.

**Root cause:** same ungated-cards-refetch class as [BUG-44]/[BUG-45], on the folder-delete cascade. `FolderCommandHandler.DeleteFolder` (`UnfileNotesInFolderAsync`, `src/Api/CommandHandlers/FolderCommandHandler.cs:97-102`) appends an `UnfileNote` **note-stream** write per contained note. `useDeleteFolder` invalidates `keys.noteCards`, but `deleteFolder` (`web/src/api/folders.ts`) captures only the folder-tree token (`captureFolderToken`/`FOLDERS_SCOPE`), never a note-cards token — so the `onSettled` `getNoteCards` refetch is ungated w.r.t. those unfile writes and can still show the notes under the (now-gone) folder until the projector catches up.

**Why not a straight BUG-44/45 copy:** a folder delete produces *N* note-stream writes, but the cards LIST gate holds a **single** stream token (design #7 — "list read waits on the single most-recently-written stream"). Capturing one unfile token gates on one note; the others can still lag. Options: (a) capture the **last/highest** unfile token and accept single-stream gating (matches existing list-gate semantics — cheapest); (b) a per-list projector-drain for the folder-delete case; (c) return the notes' post-cascade positions from the delete response and reconcile the cache directly (no refetch). Decide during the slice.

**Reproduce-before-fix:** `Api.Integration` — `DELETE /folders/{id}` with contained notes returns a note-cards `X-Consistency-Token`, and a token-gated `/notes/cards` shows the contained notes unfiled (not under the deleted folder); frontend — after `deleteFolder`, the cards refetch carries a note-stream token in `If-Consistent-With`.

---

## BUG-48 — Stale gated refetch can transiently blank an existing note (title/detail regression)

**Status:** **Done** — PR #436, deploy #740 (2026-08-08), live in prod. Three attempts; scope is note-detail only ([TI-65] carries the rest) — see the summary row. **Severity:** Low — read-only display glitch; self-corrects on the next fresh read; **no data loss** (content *writes* are hash-guarded by [BUG-47]). Surfaced while diagnosing [BUG-31].

**Symptom:** An existing note briefly renders **blank** (empty title/content) on reopen, then corrects itself. When it coincides with an in-editor edit that empties the body (e.g. removing the note's only image), the blank state flips the note header from **Save** to **Cancel** (`hasContent` false) — which is what tripped the BUG-31 E2E (deploy #697 trace: `note-title-input` `value=""` + `cancel-button` present at the failure).

**Root cause:** `content = contentDraft ?? detail?.content` and `title = titleDraft ?? detail?.title ?? initialTitle` (`NoteView.tsx`). A background note-detail **refetch** fires on reopen (React Query `refetchOnMount`; the note is stale after a prior save). On a lagging projector the gated read returns **stale** data (empty title) after the 8s cap, and React Query **stores** it — overwriting the good cached detail. With no draft set, the displayed title/content follow the stale value → the note flashes blank. This is the read-side analogue of [BUG-47] (which guarded content *writes* with a hash and prevents *seeding* the editor from a stale read, but does not stop a stale refetch from regressing the cached `detail` the header's `hasContent` reads).

**Note:** [BUG-47]'s write-up lists "version-guard the `keys.note` cache so a late stale refetch can't regress freshly-cached content" as prevention — **verify whether that shipped and, if so, why it did not cover the title** (the #697 blank was after BUG-47's deploy #696). The fix likely needs to guard the whole `detail` object (a monotonic version/token on the cache), not just content.

**Reproduce-before-fix:** a frontend test — seed `keys.note(id)` with a full note, then resolve a *stale* gated refetch returning an empty-title note; assert the cached/displayed title does **not** regress to empty.

**Fix direction:** version/token-guard the `keys.note` cache write so a refetch older than what is cached is dropped (mirror the RYW token already threaded through `gatedRead`). Out of scope for [BUG-31] (which stabilised its E2E by keeping durable local draft content); file-and-defer.

---

## BUG-49 — `NoteDeleteJourney` setup assert times out; the reload loop is stuck on the note route

**Status:** Open. Red-gated the **deploy #703** E2E gate (48-A merge, `ff622875`); re-run of the failed jobs kicked off as the immediate unblock. **Severity:** High (deploy-gate flake — a red shared gate blocks every slice, and #703 skipped `deploy-production`).

**Symptom:** `NoteDeleteJourney.Deleted_note_is_not_in_list_after_navigating_back_manually` fails in its **setup**, not its assertion: `AssertNoteVisibleInListAfterReloadAsync` (the reload-tolerant helper [BUG-42] added) times out at 30 s with the thrown-message diagnostic `rendered cards(0)=[]`, `X-Consistency=none/fresh`. The just-created "Del …" note never lands in the cards projection inside the reload window.

**Not caused by 48-A.** That slice is desktop-only and inert in a browser (`window.desktop` undefined → the toggle renders null, `useTranscription` takes the unchanged cloud path). The other 26 journeys passed and the surrounding deploys (#701, #702) were green.

**~~Root cause (class): a cold/lagging projector reload-tolerance cannot beat.~~ WITHDRAWN 2026-08-08 — disproved from the attempt-1 log.** The three facts below come from `runs/29990621282/attempts/1`, read per-attempt as the deflake guardrail requires:

| # | Evidence (deploy #703, attempt 1) | What it rules out |
|---|---|---|
| 1 | Step 23 *"Warm the API + projector before E2E"* ran and logged `warm-up POST … -> HTTP 200, token: todo#f4859b41…@1` then **`projector caught up to head after 1 poll(s)`** | The projector was **warm and at head** before the suite. The proposed fix was already shipped in **#277** (TI-39, 2026-06-13) — a month before this deploy. |
| 2 | All 5 logged cards reads: `200 X-Consistency=none/fresh`, with `injectedToken=note#751199a4…@4` present | The reads **were gated** (`RegateCardsRead` injects when the token is non-empty) and the server answered **fresh** — i.e. it confirmed the projector had applied `@4`. Not projector lag. |
| 3 | `page.Url=https://…/w/__default__/notes/751199a4-7214-40ed-91ed-5b9d31446f37` at failure | The reload loop was sitting on the **note-detail route**, not the home list — so the `note-cards` surface the assert targets was never the rendered view. `rendered cards(0)=[]` is an absent list, not an empty one. |

**So the filed fix direction is a no-op** — do not implement it. The warm/drain exists and worked here.

**Open question for the slice:** how the page came to be on `/notes/{id}` when `SaveAndReturnAsync` had already awaited `WaitForURLAsync(u => !u.Contains("/notes/"))` and asserted `home-view` visible. `handleBackFromNote` uses `navigate(-1)` (a history traversal), and the helper's `page.ReloadAsync()` hard-loads the address bar — the interaction BUG-38's note in `SaveAndReturnAsync` already warns about. Reproduce that before writing any fix; the consistency diagnostics on this failure are a red herring and the reload loop makes it worse by re-loading the note route until the deadline.

**Key files:** `tests/Browser.E2E/Journeys/NoteDeleteJourney.cs`, `tests/Browser.E2E/Pages/AppPage.cs` (`AssertNoteVisibleInListAfterReloadAsync`, `SaveAndReturnAsync`). **Not** the deploy workflow.

---

## BUG-55 — Signing out mid-recording in local transcription mode still loses the transcript

**Status:** Open. Found by Hawk reviewing the [BUG-54] fix. **Severity:** Medium — silent loss of a recorded transcript, but only on the sign-out destination and only in local mode.

**Symptom:** With [BUG-54]'s leave-confirm in place, choosing **"Leave & save"** while recording and then signing out still loses the transcript. Every other destination is fine.

**Root cause:** "Leave & save" awaits the **content** save before proceeding, but the **transcript** commit is fired asynchronously by `stopRecording()`. In cloud mode `commitTranscript()` dispatches immediately (so the request outlives the route change — safe). In local mode it first waits on `window.desktop.local.diarize()/finish()` — the stop-time `small.en` pass at ~2.3× realtime, doubled for 1:1 diarization, so minutes on a long meeting ([BUG-52]; note [BUG-53] fixed the *live* path's latency, not this finalise) — and only then issues an authenticated `completeTranscription` POST. Sign-out clears the token long before that POST, so it **401s**, `committedRef` is released, and nothing retries. Sign-out is the only continuation that clears auth, which is why no other destination reproduces it.

**Fix direction:** gate the sign-out continuation on the transcript commit landing (or on `status !== 'finalising'`), not only on the content flush.

**Worked design (2026-08-08, from reading the code — not yet implemented).** The awkward part is that awaiting unconditionally is wrong: in local mode `stopRecording` runs the `medium.en` final pass (plus 1:1 diarization) before it ever calls `commitTranscript`, which is minutes on a long meeting — so awaiting on *every* destination would hang ordinary navigation. Only sign-out needs it, because only sign-out clears the token. Four steps:

| # | File | Change |
|---|---|---|
| 1 | `web/src/hooks/useTranscription.ts` | `stopRecording` currently fires `void (async () => {…})()` (`:605`) and `commitTranscript` fires `void completeTranscription(…)` (`:203`). Capture both promises in refs and expose `awaitCommit(): Promise<void>` on `UseTranscriptionResult` that awaits the stop sequence **then** the commit POST. Cloud mode resolves almost immediately; local mode resolves after the final pass. |
| 2 | `web/src/components/leaveGuardContext.tsx` | Widen to `requestLeave(proceed, opts?: { awaitTranscript?: boolean })` and pass `opts` through to the registered guard. |
| 3 | `web/src/App.tsx:494` | `onSignOut={() => requestLeave(signOut, { awaitTranscript: true })}` — the only caller that sets it. |
| 4 | `web/src/components/NoteView.tsx` (`handleConfirmedLeave`, `:580`) | It already awaits `pendingContentSaveRef`; also `await transcription.awaitCommit()` when the flag is set, before calling `proceed()`. |

**Not E2E-provable** (needs real local audio capture on Windows) — guard with a unit test over the continuation's await set: a fake `awaitCommit` that resolves only after a tick, asserting `signOut` is not called until it has. Mutation-check it by removing the await; the test must go red.

**Key files:** `web/src/hooks/useTranscription.ts` (`stopRecording`, `commitTranscript`), `web/src/components/leaveGuardContext.tsx`, `web/src/App.tsx`, the [BUG-54] leave-confirm continuation in `NoteView.tsx`.

**Not E2E-provable** — reproducing it needs real local audio capture on Windows. Guard with a unit test over the sign-out continuation's await set.

**Key files:** `web/src/hooks/useTranscription.ts` (`stopRecording`, `commitTranscript`), the [BUG-54] leave-confirm continuation in `NoteView.tsx`.

---

## BUG-56 — On-device transcription: nothing live while recording, very slow after stop

**Status:** **Done** — #426, deploy #729. Verified headlessly; the Windows checklist (MANUAL-VERIFICATION §BUG-56) is the remaining real-world proof. **Severity:** High — the headline benefit of 48-A / [BUG-52] / [BUG-53] is absent in the shipped installer, and it fails **silently** (no banner, no log the user can see).

**Reported:** 2026-08-06, real Windows testing. This is the [BUG-53] Windows validation coming back negative — BUG-53 shipped (#413, deploy #715) marked Done *pending* that validation.

**Symptom:**
- Nothing appears in the transcript for the whole recording.
- After Stop, text eventually appears — after a long wait.
- No on-device-failed banner at any point.

**Root cause — THREE stacked faults, root-caused from the code (2026-08-07), not from the installer.** The originally-filed "prime hypothesis" (the never-ready silent stall) is real but is the *third* layer, not the cause.

1. **The resident server has never once started — the wrong binary is spawned.** `WhisperServer` is constructed with `whisperBinPath()`, which returns **`whisper-cli.exe`** (`desktop/src/modelStore.ts:142`); no server-binary resolver existed. `whisper-server` and `whisper-cli` are **different binaries** in the same whisper.cpp bundle: only the server accepts `--host`/`--port` and serves `/inference`. The CLI exits immediately on the unknown argument, so `start()` never reaches ready and the live path produced nothing from the moment [BUG-53] shipped. `whisper-server.exe` **is** in the pinned v1.9.1 release zip and **is** staged by `fetch-whisper-bin.mjs` (it copies every `.exe`) — nothing ever pointed at it. **Confirmed on Windows 2026-08-07:** no `whisper-server.exe` in Task Manager during a recording; `resources/whisper/` present.
2. **The failure was invisible because the banner is unreachable during a recording.** `local:error` *does* fire and `useTranscription` *does* `setError(...)` — but `RecordControl.tsx:120` rendered the banner only when `status === "error"`, and the local-error handler deliberately leaves `status === "recording"` (audio is still captured for the stop-time pass). So the message was stored in React state and never rendered. This is why the user saw silence rather than the start-failure banner that was, in fact, being sent.
3. **The silent-stall hole** as originally filed: `StreamingSession.step()` returns quietly while `!server.ready`, before the `FAIL_THRESHOLD` accounting, so *process alive, never ready* is silent forever. Not the cause here, but it is the reason fault 1 could hide at all.

The post-stop delay is the unchanged [BUG-52] final pass: `small.en` over the **entire** recording via the CLI at ~2.3× realtime — ~13 min for a 30 min meeting. Nothing is wrong with it; it was just the only path still producing output. **Expected cost, not a defect.**

**Why every test was green:** `whisperServer.integration.spec.ts` takes its binary from **`WHISPER_SERVER_BIN`** — an env var supplied by hand — while production resolved `whisperBinPath`. The spec proved `WhisperServer` works against the *server* binary; nothing asserted that production wires that same binary. Same class as the [BUG-53] learning "a replaced component's half-wired contract is a lie until you re-check every consumer" — here the unchecked consumer was the *binary path*, not the error channel.

**Reproduce-before-fix:** not E2E-provable (needs real capture on Windows — MANUAL-VERIFICATION §BUG-53). Headless coverage added, red before the fix:
- `desktop/tests/whisperPaths.spec.ts` — the server path resolves to `whisper-server`, is a sibling of the CLI, and `WHISPER_BIN` must not redirect it.
- `desktop/tests/streamingSession.spec.ts` — a stub server stuck `running && !ready` past the deadline calls `onError` exactly once; one that becomes ready in time reports nothing.
- `web/src/__tests__/RecordControlLocalError.test.tsx` — the banner renders while `status === "recording"`/`"finalising"` when `error` is set.
- `desktop/tests/packaging.spec.ts` — packaging verifies **both** binaries were staged.

**Fix (this PR):**
1. `whisperServerBinPath()` in `modelStore.ts` resolves `whisper-server(.exe)`, overridden by `WHISPER_SERVER_BIN` (deliberately *not* `WHISPER_BIN`); `local:start` passes it to `ensureServer`, keeping `whisperBinPath` for the batch passes.
2. `RecordControl` renders the banner whenever `error` is set, not only on `status === "error"`.
3. A ready deadline armed in `StreamingSession.start()` (45 s, one-shot, independent of the step cadence) fires `onError` if the server never reaches ready. It must sit **below** `WhisperServer`'s 60 s start timeout: `start()` kills the child and nulls `proc` when it gives up, so a deadline at or above that can only ever observe a dead process. The first draft was 75 s and was therefore unreachable dead code — a mechanism that read as a live safety net and was not, which is the very shape of this bug. `streamingSession.spec.ts` now asserts the ordering so it cannot silently invert again.
4. The same check runs on `stop()`, so a recording that ends before the deadline does not finish silently — with a 20 s grace period, since the first recording after launch legitimately spends seconds loading `base.en` and a banner there would sit beside a perfectly good transcript. A start failure that lands *after* the renderer stops listening is held and replayed on the next recording, so a permanently-stalled engine cannot stay silent for someone who only records briefly.
5. `WhisperServer.start()` handles the `ChildProcess` `'error'` event — an ENOENT on a missing binary previously had **no listener**, so it would have thrown inside the Electron main process instead of rejecting `start()`.
6. `fetch-whisper-bin.mjs` fails packaging unless **both** `whisper-cli.exe` and `whisper-server.exe` staged, and stages only the two binaries actually invoked rather than all 23 `.exe` files in the release zip.

**Deploy-time:** desktop-shell only — no backend, no CDK, no web deploy path touched → neutral.

---

## BUG-65 — on-device live transcription runs, but far slower than the ~3-4 s target

**Status:** In Progress — PR #435. **Severity:** Medium-High — [BUG-56] made the live path *exist*; this is what stands between it and being usable. No data loss (the stop-time transcript is unaffected).

**Reported:** 2026-08-07, Windows, on the installer carrying the BUG-56 fix. `whisper-server.exe` visible in Task Manager, live text appearing — "very slow".

**Diagnosis (from code + the pinned whisper.cpp v1.9.1 server source). Not yet measured on the machine.**

| # | Cause | Where | Why it costs |
|---|---|---|---|
| 1 | `--audio-ctx` never passed | `whisperServer.ts` `start()` args | whisper encodes a **padded 30 s mel every time**, so a 3 s window costs nearly what 30 s does. Likely the dominant cost. **Correction:** an earlier version of this row claimed whisper.cpp's `stream` example lowers this for streaming — it does **not** (`stream.cpp` defaults `audio_ctx` to 0, the full context). The 768 figure comes from the `command` example, a short-utterance workload, and `whisper.h` labels the option EXPERIMENTAL and quality-reducing. |
| 2 | temperature fallback | `server.cpp` | `temperature_inc = 0.20f` default → a segment failing the logprob/compression heuristics retries at 0.2, 0.4 … 1.0, up to ~6 decodes, and a sliding window's clipped boundary words are what trip those heuristics. **NOT FIXED, deliberately.** `--no-fallback` is parsed by the server and never read (`cli.cpp`/`stream.cpp` do `no_fallback ? 0.0f : …`; the server assigns unconditionally) — dead in every release through v1.9.1. The only working lever is a per-request `temperature_inc`, which also disables whisper's repetition guard — and a reduced `audio_ctx` is documented to *induce* repetition loops, so it would trade latency for a hallucination risk this change itself creates. Left in place; measure first. |
| 3 | `maxWindowMs = 8000` | `streamingTranscript.ts` `DEFAULT_STREAM_CONFIG` | the reducer commits nothing until the window exceeds 8 s, so steady state re-transcribes **8-16 s** every 1.5 s tick — 3-5× the ~3 s window `whisperServer.ts`'s header comment claims. |
| 4 | `floor(cores/2)`, cap 8 | `localEngine.ts` `pickThreads` | correct for [BUG-52]'s CPU-peg complaint, but it halves the latency-critical path too. whisper-server's own default is `min(4, hw)`. |

**Ruled out:** beam search. The pinned `server.cpp` has `beam_size = -1`, and `strategy = beam_size > 1 ? BEAM_SEARCH : GREEDY`, so it is already greedy. (Checked rather than assumed — this was the first hypothesis and it was wrong.)

**Why (1) and (2) hit the live path specifically:** both defaults are tuned for *batch* transcription of complete audio. The live path re-transcribes a short, boundary-clipped, overlapping window many times per minute — the workload whisper.cpp ships a separate `stream` example for.

**The asymmetry that makes this cheap:** the live transcript is **disposable**. 48-B re-transcribes the entire recording with `small.en` on stop and replaces it. Live accuracy can therefore be traded aggressively for latency with **zero effect on the saved note** — the opposite of the trade-off that governs the final pass.

**Instrument before tuning.** Right now `whisper-server`'s stdout is not captured and `StreamingSession` logs no per-step timing, so there is no way to tell which cause dominates or to prove an improvement. This is the [BUG-56] lesson applied: do not ship a mechanism whose effect cannot be observed. Log per step: window ms, inference ms, and whether the step was dropped by the busy-guard.

**Fix (PR #435) — instrument and tune together, so one installer round-trip both improves it and reveals what is left:**
1. Pass `--audio-ctx 768` to the live server only (`--no-fallback` is dead in the server and `--flash-attn` already defaults true — neither is passed). Verified from the pinned `server.cpp` parser, not from the binary's symbol table — a symbol table shows a flag is *present*, never that it is *read*, which is exactly how the dead `--no-fallback` nearly shipped as a fix. The batch/diarize CLI path stays untouched.
2. `maxWindowMs`/`hardWindowMs` halved to 4 s/8 s, so the steady-state window is a few seconds.
3. Threads left alone. The live server is latency-critical, but raising them re-opens [BUG-52]'s CPU-peg complaint — reduce work before spending CPU. This is the next lever if the log's `rtf` stays above 1.0.

**Verified about the shipped binary before changing any flag** (an unknown argument makes whisper-server print usage and `exit(0)` — the exact mechanism that killed the live path in BUG-56):
- `--audio-ctx` consumes a value and is read. `--no-fallback` parses but is **never read** by the server — parsing is not the same as taking effect, and checking only the former is what nearly shipped a no-op as a fix.
- `--flash-attn` is **already `true` by default** — passing it would be a no-op, so it is not passed.
- `beam_size = -1` already selects greedy, so beam search was never the problem (the first hypothesis, checked and discarded).

**Not done, deliberately:** raising the thread count. It would re-open [BUG-52]'s CPU-peg complaint, and every change here reduces *work* rather than spending more CPU. If the log shows it is still short, threads are the next lever with data behind them.

**Deploy-time:** none — desktop-shell only.

---

## BUG-64 — `shell.e2e.ts` flakes the desktop PR gate on a cold Electron launch

**Status:** **Done** — fixed in #426; proof bar met 2026-08-08 at **10/10**. Counted properly: each of the 10 `desktop` jobs was verified `attempt=1` (so no re-run-into-green is hiding a failure) **and** confirmed to contain the fix commit `b320f3b2` via `git merge-base --is-ancestor` — a green run on a branch cut before the fix would not be evidence for it. **Severity:** Medium — no product impact, but it red-gates *every* PR, since [TI-58] made the desktop job a required check.

**Surfaced:** 2026-08-07, on the first PR to run the gate TI-58 added (#426, run 31171890278, job 92845302393). 92 passed, 1 failed.

**Symptom:** `tests/shell.e2e.ts:26` — `expect(getByRole('button', { name: /sign in with google/i })).toBeVisible()` timed out at 5000 ms. The second test in the same file, asserting the same button on its own fresh launch, passed.

**Root cause — a timeout asymmetry between two tests that look equivalent:**

| Test | Wait style | Effective timeout |
|---|---|---|
| `:26` launches and renders the bundled frontend | `expect(locator).toBeVisible()` | **5 s** (Playwright's `expect` default) |
| `:37` renderer has no Node integration | `locator.waitFor()` | **30 s** (Playwright's action default) |

`playwright.config.ts` sets a per-**test** `timeout: 60_000`, which does not raise the per-**assertion** expect timeout — a distinction that is easy to miss, and the reason the config looked generous while one assertion was not. A cold Electron launch on a CI runner (fresh process, xvfb, the SPA's first paint) routinely exceeds 5 s, so only the assertion-style wait was ever exposed. Each test gets its own launch via `beforeEach`, so the *first* test also pays the coldest start.

**Why it appeared now, not before:** nothing ran `desktop/tests/` in CI until [TI-58]. Locally the file fails for want of a display, so the asymmetry had never been observed under a real cold launch.

**Fix (in #426):** both waits share an explicit `APP_BOOT_TIMEOUT_MS = 30_000`. Folded into #426 rather than a separate PR because a red shared gate blocks every slice and every parallel session — the standing "act on red builds" rule.

**Proof bar:** a flake fix is not Done on merge. Needs **10 clean runs** of the `desktop` job, counted **per attempt** — a job re-run into green still contains the failure and does not count. Runs may come from any PR's gate, not just this one.

**Deploy-time:** none — PR-side test only.

---

## BUG-57 — `OpenNoteTabsJourney` red-gated deploy #717 on an empty cards list — a vacuous test assertion

**Status:** In Progress — root-caused; fix in PR #418, awaiting the clean-run bar. **Severity:** High (deploy-gate flake) — but **not** a product defect.

**Symptom:** `OpenNoteTabsJourney` failed the deploy #717 gate hunting `note-cards` for 30 s. The thrown-message diagnostic read `rendered cards(0)=[]` with all five `/notes/cards` reads `200 X-Consistency=none/fresh` — a healthy backend serving a list the test could not see.

**Root cause (the tell everyone missed): `page.Url=…/notes/ffbc9c94-…`.** The test was still on the **note screen**. The cards list rendered nothing because it was never on the page. Two compounding causes:

1. **The wait was vacuous.** `data-testid="new-note-button"` lives in the **Sidebar**, which `App.tsx:406` renders *outside* `<Routes>` — so it is visible on **every** route, including a note. `AppPage.AssertHomeLoadedAsync` and `SaveAndReturnAsync` waited only on that button, so both returned without the navigation having happened.
2. **The navigation was discarded.** `handleBackFromNote` uses `navigate(-1)`, a history traversal the spec queues as a later task; a caller's next `ReloadAsync()` hard-loaded the address bar's **current** URL (still the note) and dropped the pending traversal. The reload loop then hunted `note-cards` on the note page until the deadline.

**All three hypotheses originally filed here are wrong** — a stale `proj-position`, a cold/lagging projector, and a workspace-scoping mismatch. No amount of consistency-token work could have fixed this; the assertion never tested what it claimed to.

**Fix (PR #418):** both helpers `WaitForURLAsync` off the note route before asserting. Follow-up worth taking: a **home-only** test id, so an always-rendered chrome element can never again stand in for "the home screen loaded".

**Durable lesson:** an always-visible element is not a route assertion. Root-cause analysis found this; re-running never would have — and the diagnostic that solved it was already in the failure message.

**Key files:** `tests/Browser.E2E/AppPage.cs` (`AssertHomeLoadedAsync`, `SaveAndReturnAsync`), `web/src/App.tsx:406`, `web/src/components/NoteView.tsx` (`handleBackFromNote`).

---

## BUG-58 — Analyse hangs to the 29 s Lambda timeout; the Bedrock call has no deadline

**Status:** Open. **Severity:** Medium-High — a user-facing action ("Generate final notes") dies after ~29 s with no honest error, and the failure is almost invisible to monitoring.

**Found:** observability-review 2026-08-06, 14-day sweep (window 2026-07-23 → 2026-08-06).

**Symptom:** the analyse spinner runs ~29 s and the note is never analysed. No 503, no "Analysis service unavailable" — the invocation is killed mid-flight.

**Prod evidence** — 4 timeouts in 14 days, all Command Lambda, all exactly at the configured limit:

| UTC | RequestId | REPORT |
|-----|-----------|--------|
| 2026-07-30 09:33:41 | `3f703d9c…` | `Duration: 29000.00 ms  Status: timeout` |
| 2026-07-30 09:37:55 | `322b8dab…` | `Duration: 29000.00 ms  Status: timeout` |
| 2026-08-04 07:56:28 | `0c747ce2…` | `Duration: 29000.00 ms  Status: timeout` |
| 2026-08-04 08:03:47 | `a32187bc…` | `Duration: 29000.00 ms  Status: timeout` |

X-Ray (`1-6a719b0f-613f8cff4a03ad2c7def9a63`) fingerprints the request as the analyse route and shows where the time goes:

- `Scan notetaker-proj-workspacelist` (workspace-scope middleware) — 65 ms
- `GetItem notetaker-proj-notedetail` — 22 ms, **89 908 bytes**
- `GetItem notetaker-proj-notedetail` — 22 ms, **89 908 bytes** (the same item, twice)
- then **28.9 s with no subsegment at all**, then the kill.

The two identical note-detail reads are the signature of `POST /w/{ws}/notes/{noteId}/analyse`: `TranscriptionHandlers.AnalyseNote` reads the detail for the ownership check, then `NoteAnalysisService.cs:34` reads it again. The in-flight Bedrock subsegment is never emitted because an unfinished subsegment is not flushed when the process is killed.

**Root cause:** `BedrockAnalysisService.cs:47` awaits `_bedrock.ConverseAsync(converseRequest, ct)` with **no client-side timeout** — `Builder.cs:225` registers `AddAWSService<IAmazonBedrockRuntime>()` on SDK defaults, whose read timeout far exceeds the Lambda's `Timeout = Duration.Seconds(29)` (`NoteTakerStack.cs:453`). A slow or hung Converse therefore always resolves as a Lambda kill rather than as a handled failure. Why Converse stalled on those four calls is **unknown** — no response was ever received, so there is no upstream status to read.

**Observable?** Barely, and that is half the finding:

- No exception, no `Error`/`Warning` log line, no `CommandFailed` or analysis metric — the kill pre-empts every failure path, so `notetaker-analysis-failed` stayed `OK` throughout.
- The dashboard "All errors (backend + frontend)" widget shows **nothing** for these four requests.
- The only two signals are the raw `REPORT … Status: timeout` line and the Lambda `Errors` metric — which did trip `notetaker-error-rate` (ALARM 2026-07-30 10:39 BST, and 2026-08-04 09:04 + 09:07 BST) with nothing in the logs to explain it.

**Reproduce-before-fix:** a spec that stubs `IAmazonBedrockRuntime.ConverseAsync` to block past the deadline and asserts the handler returns 503 `AnalysisOutcome.ServiceUnavailable` (rather than hanging) is the regression guard; the prod stall itself is not reproducible on demand.

**Fix direction:**
1. **Give the analyse path a deadline** — a linked `CancellationTokenSource` (~20 s, comfortably inside the 29 s Lambda limit), or a `Timeout` on the Bedrock client config. An exhausted call then flows through the existing `AnalysisOutcome.ServiceUnavailable` → 503 "Analysis service unavailable", with a logged Error and a failure metric, so the user gets a real message and the dashboard shows the fault.
2. **Make a silent kill visible** — a metric filter on `Status: timeout` in the Lambda log groups plus an alarm, so a future timeout is not diagnosable only by hand-reading REPORT lines.
3. **Drop the duplicate read** (minor, do it in the same slice) — `AnalyseNote` and `NoteAnalysisService` each `GetAsync` the same ~90 KB `NoteDetail`; pass the already-loaded detail through.

**Deploy-time:** neutral — a cancellation deadline plus one metric filter/alarm; no traffic-shifting, no IAM change. Note the guardrail on alarms: adding one changes the alarm-count infra assertion, so update `InfraAssertionsTests` in the same slice.

---

## BUG-59 — A save into a deleted note says "try again" forever

**Status:** Open. **Severity:** Medium — no server-side data loss, but the user's typed text is held only in the tab and is lost when it closes, with the UI insisting a retry will work.

**Found:** observability-review 2026-08-06. Explicitly deferred as secondary by [BUG-44] ("save-against-deleted shows a *retriable* 'try again': out of scope, not carried") — this row carries it with prod evidence.

**Symptom:** the note editor stays open on a note that no longer exists. Every save toasts "Couldn't save your note. We kept your text — try again." The retry can never succeed.

**Prod evidence** — note `b721c995-f1de-442a-8fb9-2d4e6bde176a`, 2026-07-24, one real browser session (`Mozilla/5.0 (Windows NT 10.0)`, not the smoke suite):

| Time (UTC) | What happened |
|------------|---------------|
| 14:34:09 | `CreateNote` → v2, then `RenameNote`/`SetNoteDate`/`LinkNoteToCalendarEvent`/`SetNoteDate` → v6 (create-from-meeting flow) |
| 14:34:21 | `DeleteNote` → **`NoteDeleted` v7** (confirmed as the last event on the stream in `notetaker-events`) |
| 14:34:33 | `RenameNote` → 404 `NoteNotFoundException` |
| 14:37:06 / 14:41:34 / 14:53:28 / 14:54:27 / 15:05:51 | `EditContent` → 404 ×5 |

31 minutes of editing, six rejected writes.

**Root cause:** `NoteView.handleSaveContent` (`web/src/components/NoteView.tsx`) has exactly two failure branches — `StaleContentError` → the conflict banner, everything else → the generic retriable toast. A 404 (note deleted) lands in "everything else", which is wrong: it is terminal, not transient. Nothing invalidates the note query on that 404 either, so the cached detail keeps the editor alive and the existing `is404 && onNotFound` bounce (`NoteView.tsx:250`) never fires.

**Observable?** Yes — `Command failed EditContent Note NoteNotFoundException` at Warning. But it is **indistinguishable in the error view from the deliberate 404 probes in `tests/Api.Smoke/ErrorResponsesSpec.cs`**, which fire a `RenameNote`/`EditContent`/`SetNoteDate` triplet against random GUIDs on every deploy (45 of the window's 51 `NoteNotFoundException` warnings). Separate them on `user_agent`: the smoke probes carry `Amazon CloudFront`, a real user carries a browser UA.

**Reproduce-before-fix:** delete a note in one tab while its editor is open in another, then type and save in the second tab — expect the deleted-note state, not the retry toast.

**Fix direction:** treat a 404 from a note write as terminal. Invalidate `keys.note(noteId)` so the existing not-found path takes over, and replace the toast with a "this note was deleted" state that surfaces the kept text for recovery (copy out / recreate) rather than inviting a retry that cannot succeed.

**Attempt 1 (PR #439) — NOT merged, converted to draft 2026-08-08.** A 404 on a content save set a terminal `deleted-note-banner` holding the failed save's text. Hawk found it introduces a **regression** plus an unclosed hole; do not merge that shape.

| # | Finding | Why it matters |
|---|---|---|
| C1 | **Leaving the note on a doomed save is now silent.** `handleBack` fires the save then `onBack()` immediately, so the 404 lands after unmount and `setDeletedNote` is a no-op on a dead component. Before the change the user got the (misleading) retry toast; now they get **nothing** and walk away believing it saved. | A regression — worse than the bug being fixed, on the commonest exit. |
| C2 | The banner is component state, so **any** `keys.note` invalidation evicts it and bounces home, destroying the text. Reachable after the banner shows via `useAnalyseNote.onSettled`, `refreshNote()`, and `useTagMutations`. The deviation's premise ("we never invalidate") only held inside `handleSaveContent`. | The rescued text is lost on exactly the paths a stuck user tries. |
| I1 | Banner sits inside the `hidden` quick-notes tabpanel → invisible from Transcript/Final. `handleGenerateFinalNotes` is a Final-tab button that saves. | Fix renders where the user can't see it. |
| I2 | The doomed writes never stop: the 404 branch still restores `contentDraftRef`, so a further blur re-fires the PUT, and "Back to notes" fires one more via the unmount flush. | The 31-minute repeat-write signature is unchanged; only the toast is gone. |
| I3 | **`status === 404` is not sufficient evidence of deletion.** Also produced by not-yours (`NoteHandlers.cs:77`), never-existed (`Note.cs:138`), and — already shipped once in 34-B — an API Gateway route miss falling through to `/{proxy+}`. Under a deploy skew every user would be told their note was deleted. | Discriminate on a `{ error = "note_not_found" }` body, mirroring the existing `stale_content` 409 convention — not the bare status. |
| I5 | Five mutations left the spec green, incl. deleting the "Back to notes" button and removing the draft-ref restore. The GET stub returns 200 forever, so the spec cannot reach C2 at all. | The tests did not cover the fix's own failure modes. |

**The deviation itself was judged correct** — invalidating `keys.note` bounces the user home via `App.tsx:570-574` and destroys the text, which is the harm the bug is about. The direction stands; the implementation has to survive unmount, live outside the tab panels, stop the retries, and discriminate the 404.

**Deploy-time:** frontend-only → neutral.
