# Human-Input Log

Every point the pipeline stopped for the human, per slice. The human-cost analogue of [token-log.md](token-log.md). Goal: drive down the *avoidable* interruptions over time by feeding each one back into a concrete fix.

Recorded by the [`human-input-log`](../.claude/skills/human-input-log/SKILL.md) skill at slice end (run by Scribe). Permission prompts are captured live by a Notification hook (`scripts/log-human-input.py` → `.claude/human-input-pending.jsonl`, gitignored); clarifications, decisions, and unblocks are reconstructed from the session transcript. Avoidable rows graduate into a fix via [`process-improvements`](../.claude/skills/process-improvements/SKILL.md) in the same turn.

## Type taxonomy

| Type | What it is | Avoidable? | Where the fix lands |
|------|-----------|-----------|---------------------|
| **Gate** | A defined human gate (Scout brief, Breaker spec, Pip start, manual `cdk deploy`) | No — intended | None. Flag only if a gate fires **more than once** per slice (re-litigated) |
| **Permission** | A tool call needed manual approval (sandbox escape, non-allowlisted command) | Usually | `settings.local.json` allow-list / sandbox config |
| **Clarification** | A question raised because the spec/context left a gap | Often | Better default, doc, or a spec-template field |
| **Decision** | "Which of these approaches?" — a choice handed to the human | Sometimes | Codify the heuristic in `CLAUDE.md` |
| **Unblock** | The human had to sort something out (red build, env, token, account) | Often | Guardrail or automation |

The signal is the **avoidable** rows and their root cause. Gate counts are noise unless a gate repeats.

## Log

One row per interruption. Newest slices at the top.

| Slice | Type | What was asked | Why it stopped | Avoidable? | Fix / where it landed |
|-------|------|----------------|----------------|:----------:|-----------------------|
| 42-B | Clarification | User asked "Still going?" during a long opaque CI/merge-gate wait | GitHub **dropped the PR `synchronize` event** so the Hawk-fix commit's CI silently never ran, then main deploy #676 was red (a parallel-session flake) — a multi-minute wait with no merge progress | Partly | Detect a dropped trigger sooner (verify checks belong to the current head sha; re-trigger via close→reopen) — now in [phase-42b-calendar-note-creation](learnings/phase-42b-calendar-note-creation.md). The close/reopen re-trigger + named-check settle-gate are the fix; no new guardrail. |
| 43-B | Unblock | User flagged "It failed" when the 43-B merge deploy went red at the E2E gate | The shared deploy gate red-failed on the **BUG-38/BUG-43 cold-start flake** (different random journey each run — TagsJourney then NoteDeleteJourney; SnapStart credential 500), not a 43-B regression. I was already diagnosing; the user's nudge confirmed urgency | No (flake owned by BUG-38/43) | Diagnosed as non-deterministic flake (different test per run, 26/28 pass, agenda untouched), drove the shared gate green with a single `gh run rerun --failed` → 43-B carried live. No new fix needed (flake already tracked). |
| 44-A | Permission | User twice interrupted ("stop prompting for permissions" / "stop asking for permissions") and **rejected the `Monitor` tool** used for PR-CI polling — a **direct repeat of 43-A** | The 43-A fix ("don't use `Monitor` for CI/deploy waits") was logged but NOT codified anywhere the driver actually reads at runtime, so `Monitor` was used again | Yes | **Recurring → codified as a hard guardrail in `CLAUDE.md` (`## Guardrails`)**: never use the `Monitor` tool for CI/deploy waits; use a single `Bash` helper-script call or `run_in_background` Bash `until`-loop (both under `Bash(*)`). Also widened `sandbox` allowlist (Docker socket + registry domains) so Testcontainers `EventStore.Integration` tests don't escape-hatch-prompt either. |
| 44-A | Decision | Slice scope (A vs A+C vs A+B+C) **and** routing (draft phase now / backlog / discuss) | Net-new feature request with no phase doc — legitimate Scout-brief gate; both asks batched into **one** `AskUserQuestion` call | No | — (legitimate human gate; correctly batched into a single interruption) |
| 43-A | Permission | User twice interrupted ("stop asking for permissions" / "stopping forcing permissions") and **rejected the `Monitor` tool** used for PR-CI + main-deploy polling | The `Monitor` tool requires a per-launch approval; using it for routine CI/deploy waits in a long autonomous slice reads as "forcing permissions" | Yes | Stop using `Monitor` for CI/deploy waits — use the read-only helper scripts as **single** Bash calls (`scripts/deploy-status.sh`, `scripts/merge-gate.sh <pr>`) or a `run_in_background` Bash with an `until` loop (one notification), both covered by `Bash(*)`. Codified in [phase-43a-agenda-add](learnings/phase-43a-agenda-add.md) + run-pipeline guidance below. **(Recurred in 44-A — now a CLAUDE.md guardrail.)** |
| 42-A | Permission | Repeated tool-approval prompts during the finish (build/test/git) — user twice said "stop asking permissions" | `dotnet build`/`test` write `~/.dotnet`, `~/.aspnet`, `~/.templateengine` (first-run sentinels/telemetry) outside the worktree; not in sandbox `allowWrite` → escaped sandbox → prompted | Yes | Added those paths (+`~/.local`/`~/.config`/`~/.cache`) to `sandbox.filesystem.allowWrite` + `DOTNET_*` telemetry opt-out env in `settings.local.json`; extended [[reference_sandbox_escape_hatch_prompts]] memory |
| human-input-log (tooling) | Decision | Capture mechanism: live hook, Scribe transcript scan, or both | Genuine design fork for a net-new feature; no established default | No | — (legitimate; batched with the row below into one prompt) |
| human-input-log (tooling) | Decision | Analysis loop: auto-graduate via process-improvements, or periodic manual review | Same fork; changed what was built | No | — (legitimate; both asks resolved in a single `AskUserQuestion` call → 1 interruption, not 2) |
| MPI-10 | Decision | Fixture-data location: real-local vs anonymise-and-commit vs both | Genuine fork — the user's confidential meetings in a public repo; a real PII/cost trade-off with no safe default | No | — (legitimate; the user must own where their real data lives) |
| MPI-11/9 | Decision | Which model to ship (Opus 4.6 now vs enable/pay for the frontier) + whether to also ship `analysis@v9` | Prod cost trade-off (Opus-tier ~80–100× Nova Lite) + a subscription/access decision only the account owner can make | No | — (legitimate cost/commercial call). **One avoidable miss:** an early model-swap `AskUserQuestion` was posed before the accessible-model data was in — the user dismissed it, then decided once the Opus-4.6 numbers were shown. Lesson: don't ask a spend decision until the comparison that informs it is on the table. |
| MPI-12 | Decision | (a) v10 scope: prompt-only vs freeform-output restructure; (b) v9 vs v10 to ship, given the judge scored v10 slightly lower | (a) genuine schema-vs-prompt fork; (b) the target is the USER's own subjective style — only they can judge which reads more like them, and the metric was inside the noise | No | — (both legitimate; (b) is exactly the case where the human, not the LLM judge, is ground truth — see [mpi12-metaprompt-and-judge-vs-human](learnings/mpi12-metaprompt-and-judge-vs-human.md)). Each presented with the numbers + a side-by-side so the call was informed. |
| 49-A | Clarification | Shape of "open multiple notes": tabs vs split panes vs both vs separate browser windows; and whether the open set persists locally / per user / not at all | Genuine fork with no safe default — split panes and tabs are different products, and per-user persistence would have added an aggregate + projection + endpoints to a slice that shipped with none | No | — (legitimate; both asks batched into **one** `AskUserQuestion` with ASCII previews, so 1 interruption not 2. The answer removed a whole backend from the phase) |
| 49-A | Gate | Authorise Scout to break the feature down into a phase | Defined human gate (CLAUDE.md: never begin a pipeline role's work without authorisation) | **Yes** | The feature request was routed to `future-features.md` and *then* Scout was offered — two stops for one request. The shape questions were already being asked; **"file for later vs break it down now" belonged in that same `AskUserQuestion` batch**, collapsing two interruptions into one. Codified below |
| 49-A | Gate | Authorise the pipeline run from 49-A | Defined human gate (phase-doc approval = the spec + implementation authorisation) | No | — (legitimate; this is the review point the phase doc exists for, and the doc had just been written) |
| BUG-54 | Gate | Authorise carrying out BUG-54 | The bug was filed during 49-A's review and offered as a follow-up; the human picked it up explicitly ("Carry out Bug-54") | No | — (legitimate: choosing *which* open bug to spend a slice on is the human's call) |
| BUG-54 | — | _No interruptions during execution_ — 3 Hawk rounds, a CONFLICTING-branch merge, and a red deploy were all driven to green without stopping | The red deploy was a documented flake ([BUG-38]) on a shared gate, which CLAUDE.md says to act on rather than ask about | No | — (the autonomous-pipeline default worked as intended) |
| BUG-56 | Gate | Authorise starting BUG-56 ("Start 56") | Defined human gate — choosing which open bug gets a slice is the human's call. Asked in the **same** message as the diagnostic offer, so one stop, not two | No | — (legitimate; the status report and the "shall I start" collapsed into one exchange) |
| BUG-56 | Clarification | Two Windows observations: is `whisper-server.exe` in the install dir, and does a whisper process appear in Task Manager while recording | **Not obtainable any other way** — the failure is Windows-only and the deploy gate has no microphone or Windows runner. Deliberately chosen so the two outcomes *discriminate* between wrong-binary and missing-binary rather than merely gathering facts | No | — (legitimate. Note the code diagnosis landed **first**, so these confirmed rather than unblocked; asking them up-front was still right, since they ran in parallel with the investigation at zero cost to it) |
| BUG-56 | Decision | How far to take desktop test automation — build tier 1 now vs 1+3 vs 1+2 vs file everything | Genuine cost/benefit fork the human owns: tiers 2-4 add Windows runners and per-PR minutes. Raised by the human's own question ("how can we automate testing a desktop client?"), and answered with the four tiers, what each would have caught, and the recommendation, so the call was informed | No | — (legitimate; a spend/scope decision. Presented as one `AskUserQuestion` with the recommended option first) |
| BUG-56/TI-58 | — | _No interruptions during execution_ — 3 Hawk rounds on #426, a red shared gate on its first PR ([BUG-64]), a branch 9 commits behind main, and two merge+deploy cycles were all driven without stopping | Red shared gate = act, don't ask (CLAUDE.md); the rest are routine pipeline work | No | — (autonomous default worked; the only stops were the three above, all genuine) |
| BUG-65 | Clarification | Report that on-device transcription was live "albeit very slow" | Not obtainable otherwise — Windows-only, no mic or Windows runner in CI. Confirmed BUG-56 fixed **and** surfaced BUG-65 in one observation | No | — (legitimate; the same channel that closed BUG-56 opened its successor) |
| BUG-65 | Decision | Instrument-first vs tune-only vs both in one slice | Genuine fork the human owns, because **they** pay the cost: every iteration is a build → install → record → report cycle for them, so the round-trip count *is* the trade-off | No | — (legitimate. Presented with the cost of each in round-trips, recommendation first) |
| BUG-65 | — | _No interruptions during execution_ — 2 Hawk rounds, a dead-flag discovery that invalidated a shipped claim, a CONFLICTING-branch false green, and a duplicate-row merge were all driven without stopping | All within tooling; the doc/merge issues were self-inflicted and self-corrected | No | — (autonomous default held) |
| BUG-67 | Clarification | "What log?" — the diagnostic file was referenced repeatedly before the user had a build containing it | **Avoidable.** The log shipped one deploy earlier; referring to it as if known, without once saying what it was, where it lived, or that it did not exist until they reinstalled, cost a round-trip | **Yes** | When introducing a NEW artefact the human must retrieve, state what/where/when-it-exists **at the moment it ships**, not when you next need it. A one-line "after you install build N, this file appears at X" in the same message as the merge would have removed the exchange entirely |
| BUG-67 | Decision | Close BUG-65 now vs wait for a real meeting; and fix BUG-67 now vs park it | Both genuinely the human's: BUG-65's close criterion is subjective ("does it feel fast enough"), and BUG-67's priority competes with BUG-55/48-D. Batched into one `AskUserQuestion` | No | — (legitimate; the rtf data was presented so the BUG-65 judgement was informed) |
| BUG-67 | Clarification | "Can you speak to other local sessions?" | Reasonable question given three doc collisions in one day; answered no — the repo is the only shared channel | No | — (legitimate; and the answer explains why the collisions happen) |

## Analysis

Run by the `human-input-log` skill in **analyse** mode (on a cadence or on demand). Tallies avoidable rows by Type and root cause across the whole log, names the top recurring cost, and graduates a concrete fix. Recurring causes and their pre-emptions are codified directly in `CLAUDE.md` guardrails / skill rules — not restated here.
