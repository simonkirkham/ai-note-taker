# Agent Workflow

The pipeline is defined in [`.claude/skills/.agent/generic/agent-roles.md`](../.claude/skills/.agent/generic/agent-roles.md) — that file is the single source of truth for role remits, rules, hand-offs, the sequence diagram, and the responsibilities matrix.

Read it directly for the full pipeline detail. Updates to the pipeline belong there; Scribe's learnings hand-off points there too.

For shareable visual summaries of the pipeline — flowchart, slide-friendly linear view,
human-gated vs autonomous swimlanes, a sequence diagram, and a roles-at-a-glance table — see
[`workflow-diagram.md`](./workflow-diagram.md).
