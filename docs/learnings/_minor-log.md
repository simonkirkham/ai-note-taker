# Minor-slice log (Tier 1)

One line per trivial slice that applied a guardrail/permission entry but had no narrative worth a full doc. Tier 2 slices get their own `phase-<id>-….md`; Tier 0 slices get nothing. See `.claude/skills/process-improvements/SKILL.md` Step 2.5.

| Date | Slice | What was applied | Pointer |
|------|-------|------------------|---------|
| 2026-06-26 | 38-B paste transcript into existing note | Pivoted the 38-A import feature (per user feedback) from create-a-new-note to **paste into the open note**: note-scoped `POST /notes/{noteId}/import-transcript` (`CompleteTranscription` replace + `AnalyseAsync(transcriptOverride)`), home button removed, **Paste transcript** modal on the Transcript tab with a replace-confirm. **Reusable points:** (1) **the `transcriptOverride` is required even for an EXISTING note** — the just-appended transcript hasn't reached the async `NoteDetail` projection, so analysing the read-back would use the stale transcript (same RYW class as 38-A, not specific to create). (2) **Don't copy a handler's exception-catch blindly when the call shape changes** — the old single-purpose `CompleteTranscription` handler caught `InvalidOperationException → 404` for non-existence, but in the orchestrated import that catch is *dead* for 404 (the command handler throws the typed `NoteNotFoundException` first) and *harmful* downstream: it would swallow an analyse-phase `TagNote`-already-present race (against the lagging projection) as a misleading 404 **after the transcript was committed**. Keep only the typed `NoteNotFoundException` catch; let genuine post-commit failures surface as an honest 500. (Hawk catch.) Clean, no-churn slice (main green throughout). | PR #360 |
| 2026-06-04 | tech-remove-dead-dispatcher | Deleted dead `IDomainEventDispatcher`/`IDomainEventHandler` + 5 unused `*EventHandler` classes (never called since `9931d12` inlined projections via an unreviewed direct-to-main "reduce code" commit); corrected CLAUDE.md / architecture.md / ADR 0009 to document inline-in-handler as the real write path. *Why* already captured in `phase-10j` / `phase-12b` learnings — this slice is the resolution. | PR #171 |
| 2026-06-05 | bug-11-session-refresh-token | Merge step ran `gh pr merge` chained after the deploy-gate `echo` in one shell, so it merged while an unrelated deploy (#468) was in-progress — a gate breach (harmless only thanks to `concurrency: deploy, cancel-in-progress:false`). Fix: the merge command must parse `gh run list --json status,conclusion` and abort unless `completed`/`success`; never chain `gh pr merge` unconditionally after the gate check. Full *why* in `phase-bug-11-session-refresh-token.md`. | PR #175 |
| 2026-06-05 | 17-B link-meeting-picker | Reused the never-called `POST /notes/{id}/calendar-link` from a new frontend picker (modal reuses Phase 16 day-nav via a new shared `meetingDay.ts` extracted from `MeetingsSection`). Pure-frontend, optimistic-first. *Why* not worth a full doc; the standout (rebuildable-projection backfill) lives in `phase-17a-rebuildable-calendar-link.md`. | PR #177 |
| 2026-06-05 | 17 (process) | Scout phase doc was drafted as loose edits in the shared primary checkout, which was mid-flight on a user WIP branch — the doc edits rode along across a branch switch. Fix applied: land Scout phase docs on main via their own small PR **before** `/run-pipeline` starts, and stage explicit paths only (never `git add -A`) in a shared checkout. | PRs #174, #176, #177 |
| 2026-06-05 | 18-A transcription-draft-store | Transcription checkpoints are loss-tolerant working state, not events: new overwrite-in-place DynamoDB draft store (note-id-keyed, owning user as attribute + handler-enforced ownership, 48h TTL, least-priv Get/Put/DeleteItem) living beside the projections in `src/EventStore/`; `PUT`/`DELETE /notes/{id}/transcription/draft` emit no events; `POST /transcription` deletes the draft on commit; `GET /notes/{id}` composes `transcriptDraft` at read time (suppressed when a prefix of the committed transcript). Full *why* — the snapshot-less full-stream-replay cost that disqualifies checkpoint-events — in `adr/0011-transcription-checkpoints-draft-store.md`. | PR #179 |
| 2026-06-05 | 19-A split-api-by-domain | Pure refactor: `api.ts` → `api/<domain>.ts` + a `request<T>()`/`requestVoid()` helper (void endpoints must use `requestVoid` — calling `res.json()` on an empty body throws), no barrel, 30 import sites repointed. **Two reusable guardrails:** (1) when moving/renaming a module, grep **dynamic** `import('…')` too, not only `from '…'` — a static-only grep missed 2 `await import('../api')` in a test and the first suite run failed one file; (2) `npm run lint` can pass on a stale `.eslintcache` while the pre-commit `eslint .` catches `import-x/order` — run `lint -- --fix` / trust the pre-commit lint, don't rely on a clean `npm run lint` alone. Cohesion *why* already in `phase-19.md` + the `frontend-react` "single-domain module" smell. | PR #181 |
| 2026-06-05 | 18-B transcription-recovery | Frontend half of crash-safe transcription: 15s checkpoint retargeted from the committing `POST` to the no-event draft `PUT` (`saveCheckpoint`); clean Stop/natural-end/intentional-unmount commit once via a `committedRef` one-shot; optimistic Recover/Discard banner on reopen when `GET` exposes `transcriptDraft`. Built on the parked stopgap branch (`wip/phase-18-…`, cherry-picked). **Process note:** mid-build, the concurrent 19-A `api.ts`→`api/` split merged to main, so the slice needed a one-time `git merge origin/main` with a modify/delete resolution (drop `api.ts`, port the two new fns into `api/transcription.ts` + the `transcriptDraft` type into `api/notes.ts`, repoint imports) — clean once done, no behaviour change. *Why* for the draft-store design lives in `adr/0011-…`. | PR #182 |
| 2026-06-05 | 18-C continue-transcript | Pure-frontend "continue a transcript": Record on a note with a committed transcript prompts Continue / Re-record (inline, leave-confirm idiom); Continue seeds `useTranscription` from the prior transcript via `resumePrefixRef`, **re-prepended to every finalised result** (not a one-time seed) so a later turn can't drop the prior text, and the commit saves the concatenation via the existing `TranscriptionCompleted` — no new event. **Hawk catch (real):** seed from `displayedTranscript` (`liveTranscript ?? transcriptText`), not the committed `transcriptText`, else a *second consecutive* Continue seeds from pre-resume text and silently replaces — the exact data-loss mode the slice guards against. | PR #183 |
| 2026-06-08 | 20-A tanstack-foundation-todos | Pre-commit `vitest run` intermittently ends in `Segmentation fault (core dumped)` after all tests pass (worker-pool/jsdom teardown crash, Node 20.20.2, vitest 2.1.9) — fails the hook with exit 1 despite a green suite. Workaround: retry the commit; it passed under the default Node (24) when Node 20 segfaulted twice. Don't `--no-verify` — the suite is genuinely green, the crash is in teardown. Full design *why* for the slice in `phase-20a-tanstack-foundation-todos.md`. | PR #184 |
| 2026-06-09 | bug-14-multi-tag-tag-race | A near-deterministic "element never appears" E2E timeout (always the same shape, a pill that never renders) is a **missing render**, not latency — raising the Playwright timeout (PR #203, 15s→45s) masks it and deploy #493 still failed *with 45s applied*, disproving the latency theory. Real cause: optimistic `patchTags` is a no-op when `keys.note` isn't cached yet (`(old) => old ? … : old`), so tagging a fresh note while its initial GET is in flight drops the pill; a space-separated multi-tag paste (two concurrent mutations) widens the window. Fix: `onSettled` reconciles `keys.note` **only** when `ctx.previous === undefined` (optimism couldn't apply) — not unconditionally, which reverts the optimistic pill via a stale GET and broke 2 tests. **Guardrail:** reproduce-then-fix beats symptom-masking; an intermittent E2E that fails *the same way* each time (vs a *slow* one) is a bug, not flake. Full *why* in `phases/phase-bugs.md` BUG-14. | PRs #203, #205 |
| 2026-06-11 | 25-D remove-inline-image | Add a per-image ✕ remove control via `Image.extend({ addNodeView: () => ReactNodeViewRenderer(ImageNodeView) })` — UI only, no schema/parse/serialize change. **Reusable guardrail:** to attach a control to an existing node, extend with `addNodeView` rather than redefining the node — the node `name` ("image") is unchanged, so every doc-walk keyed on `node.type.name === 'image'` (`swapImageSrc`, `removeImage`, `extractImageKeys`) and the markdown serializer keep working untouched; the button markup lives only in the NodeView DOM, never in serialized content. Removal calls `deleteNode()` (no API; S3 blob orphaned, purged on note-delete per 25-C). **Test reality:** jsdom mocks `NoteEditor`, so the NodeView interaction is unprovable in vitest — the BDD proof is the E2E `NoteImageJourney` remove-and-reload `[Fact]` at the post-deploy gate. | PR #226 |
| 2026-06-11 | 19-F1 live-regions | Added `role="alert"` (errors) / `role="status"` (loading/empty) to 15 silent transient surfaces across 8 components. **Reusable guardrail:** an audit-derived slice list goes stale fast in a multi-session repo — **re-audit the exact surfaces immediately before building**, don't trust a multi-day-old finding list. The 2026-06-05 audit named ~10 gaps; a 2026-06-11 re-audit (Explore subagent) found ~half already fixed by intervening slices and surfaced new ones (TranscriptTab, QuickCapture), so building from the stale list would have wasted edits and missed surfaces. **Scope note:** `eslint-plugin-jsx-a11y` (added as new slice 19-F3) would *not* have caught these — it has no "dynamic content needs a live region" rule; live-region coverage stays a manual + test-asserted concern. | PR #229 |
| 2026-06-11 | 19-F2 focus-visible | 6 bare `:focus`→`:focus-visible`; consolidated `MeetingPicker`'s `document`-level Esc handler into `useFocusTrap(ref, { onClose })`. **Reusable guardrail:** before adding Esc-to-close to a `role="dialog"`, check it *has a valid dismiss action* — the original spec said add Esc to `SessionExpiredBanner`, but it's a full-screen blocking re-auth gate (`App.tsx`: `if (sessionExpired) return <SessionExpiredBanner …>`) whose only action is "Sign in again". Per ARIA APG, Esc-to-close is *optional* and wrong when dismissing isn't a choice — Esc would just hide the one recovery path. Audit "no Esc handler" findings are not automatically gaps; a blocking modal correctly omits it. Spec corrected in-slice. | PR #234 |
| 2026-06-11 | 19-F3 jsx-a11y | Adopted `eslint-plugin-jsx-a11y` as a CI gate. **Two reusable guardrails:** (1) **Peer-range gap, not a blocker** — jsx-a11y@6.10.2 caps its eslint peer at v9 but runs fine on this repo's eslint 10; resolve with a *scoped* `package.json` `overrides` (`eslint-plugin-jsx-a11y` → `eslint: "$eslint"`), verified `npm ci` green on Node 20 — **not** a repo-wide `legacy-peer-deps`, which weakens resolution for everything. (2) **A lint gate retroactively enforces on code merged after its branch point** — the gate went green on my branch (backlog cleared), then failed deploy #525 on `WorkspaceSwitcher` `autoFocus`, a component a parallel session merged to main *after* my branch point. Expected behaviour, not a regression; fix forward on main (the merge-gate can't be satisfied by a PR onto an already-red main — chicken-and-egg — so a direct scoped-disable push is the correct unblock). **JSX comment placement:** eslint-disable for an attribute-position rule (`no-autofocus`) needs the directive on the line *immediately before the reported line* — put `autoFocus` on the `<input` opening line, and use `//` in JS-expression context but `{/* */}` in JSX-children context (a `//` in JSX children renders as visible text and isn't parsed as a directive). | PRs #236, +follow-up |
| 2026-06-11 | BUG-20 switcher popover overlap | A dropdown widened to avoid text truncation (23-E: `min-width:16rem`, grew rightward) then overlapped the page content. Better pattern for a narrow-container dropdown: keep it container-width and pull the secondary actions (rename/delete) out of the flex flow (`position:absolute`, revealed on `.row:hover`/`:focus-within`) so the primary label gets full width — fixes overlap *and* truncation together. **Gotcha:** the revealed-actions backplate must be **opaque** or the label's trailing text bleeds through behind the icons — `--color-primary-bg` is a ~6% translucent wash, so composite it over surface (`linear-gradient(var(--tint),var(--tint)) var(--color-surface)`) to match a hovered row exactly. `opacity:0` (not `display:none`) keeps the buttons focusable/tab-ordered and clickable in tests (`userEvent.click` only trips on `pointer-events:none`). | PR #244 |
| 2026-06-13 | MPI-5 grounded-entity allowlist | Eval-judge fix: prompt-level note-grounding (MPI-4) still let the LLM quality judge flag note/gold entities ("Stark Industries") as fabrication, so make grounding **deterministic** — `GroundedEntities.From(fixture)` humanises the fixture's curated gold tags (kebab→spaced, dedup) and `BedrockQualityJudge.BuildPrompt` renders them as a `GROUNDED ENTITIES — NEVER flag as fabrication` block (omitted when empty). Gold tags *are* the curated grounded set; the user note already appears verbatim in the prompt, so note-NER was unnecessary (doc Step 1 reconciled "note + gold" → "gold tags" per Hawk). Harness-only, `tests/Analysis.Eval/**` is `paths-ignore`d → no deploy. **Confirming `make eval` run + report still pending (live Bedrock, eval-run skill)** — code merged but acceptance not yet proven, so MPI-5 stays *In Progress*. **Operational guardrail:** the "main's latest deploy green + idle" merge gate can **starve for hours** when many parallel sessions merge to a fast-churning main (here ~2h: a cascade of 28-A/B flaky-E2E-timeout deploys + BUG-21/22 + TI-3 kept the *latest* run perpetually in-progress or red). Don't fix another session's deploy ("one driver per slice"); instead hold + monitor, `git merge origin/main` to keep the branch CLEAN (resolves the persistent `mergeable:UNKNOWN` recompute-lag), and use a **combined** PR-checks-∥-deploy poller so you merge in the first green idle window it catches. Flaky single-E2E timeouts (30s, 1 of ~20, different test each rerun) are the dominant cause of these red latest-deploys. | PR #257 |
| 2026-06-11 | CHANGE-15 folder-preview keyboard | `FolderPreviewPanel` note rows were click/drag-only `<li>`s under a scoped `jsx-a11y` disable. **Reusable guardrail:** to make a clickable+draggable row accessible, replace the `<li role>`+`onClick` with a real `<button>` *inside* the `<li>` rather than bolting `tabIndex`/`onKeyDown`/`role="button"` onto the `<li>` — the button gives native focus + Enter/Space + correct role + accessible name for free, and the `eslint-disable` simply drops off. `draggable`/`onDragStart` move onto the button unchanged (buttons are valid drag sources); keep the row divider (`border-bottom`) on the `<li>` and the interactive styling (padding/hover/`cursor:grab`/`:focus-visible`) on the button via a full reset (`border/background/font/text-align/width`). Drag source had no test — added `expect(item).toHaveAttribute('draggable','true')` (Hawk) since the drop test fabricates `getData` and would pass even if `onDragStart` were removed. | PR #247 |
| 2026-06-13 | TI-38 error-log-level | ASP.NET's `app.UseExceptionHandler(...)` registers `ExceptionHandlerMiddleware`, which logs **every** caught exception at `Error` (category `Microsoft.AspNetCore.Diagnostics.ExceptionHandlerMiddleware`) *before* invoking your inline handler — so a domain exception you then re-map to 409/404 + Warning **double-logs**, polluting the ops "All errors" view with expected conflicts. **Reusable guardrail:** to get a single log line at a level *your* mapper decides, don't nest your handler inside `UseExceptionHandler` — replace it with a plain `try/catch` middleware that maps + writes the response itself (keep a `Response.HasStarted` guard + `Response.Clear()` to preserve the framework's implicit reset). Found via a 14-day prod log review: most `Error` lines were `ConcurrencyException` already mapped to 409. | PR #267 |
| 2026-06-13 | TI-37 frontend-error-capture | CloudWatch RUM `Telemetries: [errors, performance, http]` does **not** capture a failed `<img>`/`<script>`/`<link>` load — it fires a DOM resource `error` event (not a JS exception, not a fetch/XHR), and the asset is fetched S3→CloudFront direct so the backend never sees it either: invisible end-to-end. **Reusable guardrail:** to surface resource-load failures, add a **capture-phase** `window.addEventListener('error', h, true)` (the 3rd arg `true` is required — resource errors only surface in the capture phase), detect a resource target (`event.target` is an Element with `src`/`href`, not `window`), and forward to RUM via `cwr('recordError')` so it rides the existing `JsErrorCount` metric; skip `target === window` to avoid double-counting the `errors` telemetry. Surfaced by a user 403 report (note-image PNGs). | PR #268 |
| 2026-06-13 | BUG-23 rebuild-timeout | A transient DynamoDB `TimeoutException` mid-rebuild aborted the whole projection rebuild as an unhandled 500. **Reusable guardrail:** when adding retry-on-transient to a handler, audit **reads as well as writes** — `BoundedWrites` already retried the rebuild's writes, but the heavy `ReadAllStreamsAsync` scan + reconcile reads were unwrapped, so the asymmetry left the dominant timeout path unprotected. Wrap the reads in the same bounded retry, and map a *surviving* transient (`TimeoutException`, non-client-aborted `OperationCanceledException`) to **503 + Warning**, not an unhandled 500 + Error — a genuine client abort still maps to 500; a recovered transient returns 200 and must not trip the fault metric. | PR #269 |
| 2026-06-13 | BUG-24 image-403 (v1 reverted, v2 fixed) | Inline-image 403 on open: stored markdown carries a bare S3 key `![](notes/{id}/{img}.png)`, `tiptap-markdown` parses it to a transient `<img src="notes/…">` (relative → `…/notes/notes/…` → 403) **at parse time**, before any NodeView guard exists (BUG-19's placeholder only fixed the *rendered* node). **Two reusable guardrails:** (1) **A no-network-fetch property is unprovable in jsdom** — vitest's DOMParser doesn't fetch, so a unit seam can only prove the transform/ordering, never "no request was made"; the deploy-gate E2E (`page.Response += …`, fail on a non-presigned `/notes/` PNG ≥400) is the *only* authoritative proof, and PR CI (no E2E) will happily green a broken fix. **v1 (PR #272) shipped on unit-only confidence — a markdown-it renderer-rule that `tiptap-markdown`'s real parse bypassed — and the deploy-gate E2E caught it red, costing a merge→revert→re-fix cycle.** (2) **To stop a parse-time fetch, remove the fetchable src from the string *before* the parser sees it — don't try to intercept inside the parser.** v2 creates the editor with `content: stripImageKeys(value)` (images removed pre-parse), then resolves keys→presigned and `setContent`s the resolved markdown (`emitUpdate:false`), gating insert/paste/drop on a `canInsert` flag during the resolve window so the whole-doc `setContent` can't wipe a user edit (resolve fires once per note via a `didResolve` ref on the captured initial value — not per keystroke). Full *why* in `phases/phase-bugs.md` BUG-24. | PRs #272 (reverted), #273 |
| 2026-06-13 | CHANGE-16 pin @tiptap/extension-link | `NoteEditor.tsx` imported `@tiptap/extension-link` while it was only present transitively via `@tiptap/starter-kit`; a future StarterKit bump dropping it would break the import with no manifest signal. Fix: declare it directly at `^3.23.4`. **Reusable guardrail:** an `import 'x'` whose package is only transitively resolved is a latent break — it must be a *direct* dependency. Match the surrounding float (`^3.23.4`, like the other `@tiptap/*`) and **do not** copy the lone exact-pin on `@tiptap/extension-image` (that exact pin is the documented ERESOLVE hazard with floated `@tiptap/*` — dep-audit T6). Regenerate the lockfile **incrementally** (`npm install`, not `rm`) on an npm matching CI's (11.13.0): the diff is then a single root-`dependencies` line — the `node_modules/@tiptap/extension-link` tree node already exists, so promotion doesn't alter resolution and no package node churns. | PR #283 |
| 2026-06-13 | RYW-4 cutover-cleanup | Final slice of the read-your-writes migration — docs + dead-code cleanup (architecture.md + ADR 0009 rewritten to "async live, RYW is the read-after-write contract, projector sole writer, optimism feel-only"; removed the dead scoped `IProjectionUpdater` DI registration). **Two reusable guardrails:** (1) **A "complete the cutover / retire wording X" cleanup must grep the whole repo for the stale wording, not just the files you remember** — the first pass updated `ProjectorFunction` but missed four live "shadow mode / flows still inline" comments in `NoteTakerStack.cs` + `StreamProjector.cs` that flatly contradicted the cutover-complete claim; Hawk caught them. A cleanup slice's diff should be driven by `grep -rn "shadow mode\|still inline\|inline write"`, not memory. (2) **To prove a DI registration is dead, do static no-consumer analysis, not "the suite passes"** — the in-process `ApiFactory` rebuilds its own event store and never resolves the scoped `IProjectionUpdater`, so the 383-green suite would pass with or without the line; the real proof is that nothing in the API host injects/`GetRequiredService`s it (command handlers append-only, rebuild handler folds via its own stores, the decorator hand-builds its updater). Hawk independently confirmed via the same static trace. | PR #274 |
| 2026-06-15 | TI-41 gate presence-wait | `GetActions` had a hand-rolled `Task.Delay(100)×10` loop *beside* the consistency gate to ride out cross-stream projector lag (gate waits on the **action** stream; ownership reads the **note**'s projection — a different stream, no DynamoDB-Streams cross-key order). Folded it into a new `IConsistencyGate.WaitForPresenceAsync<T>` (bounded presence-poll sharing the version-wait's interval/cap/injected-delay/logging). **Reusable heuristic:** when a read must wait on a *second* stream than the token gated on, pick the wait by what the read needs from that stream — **existence** (presence-poll, no version/token change; right when the dependency is immutable, e.g. ownership) vs **content** (multi-position token `a@N,b@M`, only if the writer actually knows `M` — here the Command Lambda validates the note via its *projection*, not its event stream, so it doesn't) vs **denormalize** the field into the gated stream's projection (removes the 2nd wait but costs an event version + rebuild). A raw `Task.Delay` loop next to an injected, virtual-time-testable gate is the smell that flags this. Full A/B/C *why* in `technical-improvements.md` TI-41. | PR #289 |
| 2026-06-22 | 31-A electron-loopback-shell | To host an SPA that uses **relative `/api`** and `redirect_uri = window.location.origin` inside Electron, you cannot use `loadFile`/`file://` (relative `/api` → `file:///api`, dead) **or** a custom `app://` scheme (Google OAuth Web client **rejects** non-https/custom-scheme redirect URIs). **Reusable guardrail:** run a tiny **loopback HTTP server in the main process** (`127.0.0.1:<port>`) that serves the bundle (SPA fallback) and **proxies `/api/*` → prod** via `session.fetch`; the origin is then `http://localhost:<port>` — Google-acceptable as a redirect URI and same-origin to `/api`, so no CORS/SameSite breakage and `web/` needs zero change. Strip the proxied `Set-Cookie` `Domain` so the httpOnly `rt` cookie becomes **host-only** and persists across restart; rewrite `Location` redirects back to localhost; force-resolve + containment-check asset paths to block traversal (`../`, `..%2f`). Full *why* in `phases/phase-31.md` 31-A Design. **Setup gotcha (manually verified on Windows 2026-06-22):** a freshly added+saved `http://localhost:5180` redirect URI gives `Error 400: redirect_uri_mismatch` on the first attempts even when the value is exactly correct — **Google propagation lag** (~5 min to a few hours), not a config error. Confirm the running app's `window.location.origin` reads `http://localhost:5180`, then wait + retry; no code change. Captured in `desktop/MANUAL-VERIFICATION.md` Troubleshooting. | PR #311 |
| 2026-06-14 | 19-G test-quality migration | Migrated 3 testid-heavy unit files (RecordControl/FinalNotesView/ShortcutsPanel) to role/label-first queries and converted TagsSection interaction `fireEvent`s to `userEvent`. **Reusable guardrails:** (1) **A unit-test query migration must NOT remove the `data-testid` from component source** — `tests/Browser.E2E` references ~96 testid locators, a separate contract; keep the attribute, only change the unit query. (2) **Keep `getByTestId` where there is no role/accessible-name** (decorative `<span>` timer, unnamed `<p>`/`<ul>` content) — inventing a role or an ambiguous `getByRole('list')` is worse than a stable testid. (3) **Leave specific-key `fireEvent.keyDown` and a `mousedown`-submit/`click` pair raw** — `userEvent.click` fires a full pointer sequence (re-firing mousedown) and changes what a preventDefault-on-mousedown handler tests; convert only genuine change/blur/click interactions. (4) Prove behaviour-coverage is unchanged by asserting the **same passing-test count before and after** (71→71), not just "green". | PR #286 |
| 2026-06-22 | BUG-35 word-level search | Fuzzy search matched `"Andrew"` against the word `"and"` across unrelated notes. Cause: `NoteSearchRanker` scored a query against each **whole field** with FuzzySharp `Fuzz.PartialRatio`/`TokenSetRatio`. **Reusable guardrail: `PartialRatio`/`TokenSetRatio` over a whole document is substring-window matching, not word matching — near-guaranteed false positives.** `PartialRatio` slides a query-length window across the entire text and keeps the best contiguous substring; over any long body a short query finds *some* window above threshold, so it matches almost everything. These ratios are for short-string-vs-short-string (a query vs a single token/title), never query-vs-body. Fix: tokenize the field and score each query term against tokens — exact / prefix (term≥3) / tight whole-token `Fuzz.Ratio≥85` (term≥4, typo tolerance); reject substring/infix. Gate inclusion on the **best single-term** score (OR across a multi-word query — gating on the *mean* silently requires every term = AND, dropping "budget review" notes that only say "budget"); rank by the mean (rewards coverage). Query-time only (the ranker), so no projection rebuild. | PR #312 |
| 2026-06-22 | minor-18-19 tag-search + auto-older | Two frontend-only home tag-filter tweaks: a `tag-filter-search` box that narrows displayed pills when `tags.length > 8` (CHANGE-18, view-only — selection/note-filtering untouched), and auto-enabling "Show older" when the first tag is selected (CHANGE-19), reverting only the auto-enable on clear. **Reusable guardrails:** (1) **Track an auto-enabled toggle with an explicit `*AutoEnabled` flag, never a derived `userOn \|\| filterActive`** — the derived form makes the box un-untickable while filtering; the explicit flag lets the user override (manual untick respected) and preserves a pre-existing user-ON preference through apply→clear. (2) **Adjust the toggle in the user-action handlers (toggle/clear/onChange), not in a `useEffect`** — a `setState` in an effect body trips the `react-hooks/set-state-in-effect` lint gate (a hard CI gate `tsc`/`vitest` don't catch). (3) **A behavioural change can be fully green in its own test files yet break a sibling file owned by an earlier change** — CHANGE-19 broke the CHANGE-3 today/older composition test in `CollapsibleFilters.test.tsx` (which asserted the old "tag-select doesn't reveal older" behaviour); the slice's two new test files passed in isolation, only the **full suite** caught it. Run the whole frontend suite before PR, not just the changed files; update the sibling test to the new expected behaviour rather than leaving it red. | PR #313 |
| 2026-06-22 | CHANGE-20 search highlight = gate matcher | Follow-up to BUG-35: the search *inclusion gate* used word-level matching (`BestTokenScore`) but the *highlight/snippet* path still picked displayed terms via FuzzySharp `Process.ExtractTop` (substring). **Reusable guardrail: when one code path decides inclusion and a second decides what to *show* for the same decision, they must share one definition of "matched" — extract the predicate (`TermTokenScore`) and have both delegate, or the displayed reason drifts from the real one.** Second trap (Hawk-caught): the gate scored tags *tokenized* (word-by-word) while the highlight scored *whole* tags, so a note admitted on the 2nd word of a multi-word tag (`"corp"` in `"acme corp"`) showed an empty term — the display candidate set must be the same unit the gate matches on (tokenize tags in both). Query-time only, no rebuild. | PR #314 |
| 2026-06-22 | CHANGE-17 missed prod rebuild | Case-insensitive tags shipped (code merged, E2E green, deploy #607) but the slice's **mandatory post-deploy `POST /admin/projections/rebuild` was never run** — discovered ~weeks later when a tag-index scan showed 6 legacy mixed-case tags still present, incl. a split `Crosslake`/`crosslake` showing as two filter pills. **Reusable guardrail: a normalize-on-fold slice is NOT done at "merged + deployed" — new writes normalise but pre-existing projection rows do not until a rebuild re-folds them; verify by scanning the live projection (`aws dynamodb scan notetaker-proj-tagindex`), not by trusting green tests.** The auth on the rebuild endpoint (`.RequireAuthorization()`, Google bearer) means it can't be run headlessly from CI/CLI — it needs a signed-in user's token, which is exactly why it gets skipped. Two operational footguns hit while running it: (1) the curl header must be `Authorization: Bearer <token>` — a pasted value already starting with `Bearer` yields `Bearer Bearer …` → silent 401 empty body; (2) the id_token expires ~1h so copy a fresh one. Post-rebuild scan confirmed 0 mixed-case, `Crosslake`→`crosslake` merged (37→36 unique tags, rows unchanged = clean merge, no loss). | PRs #308/#309 |
| 2026-06-22 | 31-B pin-the-implicit-default | The Electron shell already captured system/loopback audio with no picker — but via **Electron 33's *implicit* default** for an unhandled `getDisplayMedia`, with no handler in our code (confirmed: silent-mic test → non-empty transcript). **Reusable guardrail: when a capability "just works" through an undocumented framework default, that is a latent regression, not a finished feature — pin it explicitly.** A default can tighten on any upgrade, and here the failure is *silent*: the renderer's `getDisplayMedia` catch (`useTranscription.ts:204`) swallows a denial and falls back to mic-only with no error and no prompt, so far-side meeting audio just vanishes. Fix = register `session.defaultSession.setDisplayMediaRequestHandler(..., { useSystemPicker: false })` returning `{ video: <primary screen via desktopCapturer+screen.getPrimaryDisplay>, audio: 'loopback' }`, and **log every grant/denial** (incl. matched-primary vs fell-back-to-first) so a future regression is *visible*, not silent. Keep the selection logic a pure function (no electron import) for headless unit tests — same seam as `server.spec.ts`. Re-run the Windows manual checks after any `electron` bump. Full design in `phases/phase-31.md` 31-B; manual checks in `desktop/MANUAL-VERIFICATION.md`. | PR #315 |
| 2026-06-22 | 31-C electron-builder installer | Packaged the Electron shell as an unsigned NSIS Windows installer (one-click, per-user, no admin) + made the build zero-config. **Three reusable points:** (1) **asar path resolution** — the main process reads `web-dist` via `path.join(__dirname, '..', 'web-dist')`; packed, `__dirname` = `app.asar/dist`, so `../web-dist` = `app.asar/web-dist`. Keep `web-dist` *inside* the asar (list it in `files` + `asar:true`) so that path resolves with **no main.ts change** — do NOT move it to `extraResources` (that lands it at `resources/web-dist`, breaking the `__dirname`-relative read). (2) **electron-builder footgun avoided** — `dependencies: {}` is correct here because every main/server/displayMedia import is electron, a node builtin, or relative; a real runtime dep left in `devDependencies` would be pruned out of the asar and crash at run time. (3) **A public OAuth client id can be a committed default** (`build-web.mjs` `process.env.X || DEFAULT`) — it already ships in every frontend bundle, so baking it makes a *packaged* build (no env at run time) and `npm run app`/`package` zero-config; env override preserved, nothing secret committed. The `.exe` only builds on Windows (`--win` needs Wine on Linux) → CI asserts the *config* (`tests/packaging.spec.ts`), the build is a documented manual step. **Completes the original 3-slice Phase 31 plan** (31-D later added auto-publish). Manual checks in `desktop/MANUAL-VERIFICATION.md` 31-C. | PR #316 |
| 2026-06-23 | 31-D CI-publish installer + pull-update | Turned desktop "update" into a one-command **pull** (`npm run update`) by having CI publish the installer to a rolling `desktop-latest` GitHub Release — no local rebuild. **Reusable points:** (1) **`workflow_run` after the deploy, not a `push` trigger** — keying the publish job on `Deploy` *success* (`conclusion == 'success'`) + checking out `workflow_run.head_sha` guarantees the artifact == a commit that **actually went live**, not main HEAD (which can be ahead / un-deployed). It's a *separate* workflow, so it runs after Deploy and can't gate/slow it (**deploy-time neutral** — the project's deploy-time guardrail is satisfied by construction). (2) **Gate the Windows-runner build on changed paths** (`git diff HEAD~1 HEAD` for `web/`|`desktop/`, `fetch-depth: 2`; squash-merge → one commit so `HEAD~1` is the right base) so backend/docs-only deploys don't burn a (2×-billed) Windows runner; force `build=true` on `workflow_dispatch` so manual runs always build. (3) **Rolling release is idempotent** via `gh release delete desktop-latest --cleanup-tag || true` then `create` (the `|| true` survives the first-ever run). (4) **No-space `artifactName`** (`AINoteTaker-Setup-${version}.${ext}`) makes the `gh release create release/*.exe` glob and the PowerShell `--pattern '*.exe'` robust. (5) **No secrets in the public artifact** — verified: only the public `VITE_GOOGLE_CLIENT_ID` is baked (already in the live site JS); client secret/AWS creds/user tokens are server-side or runtime-fetched. **Self-verified**: the merge's own deploy triggered publish run #1, which built on Windows and published the 82 MB installer. Full design in `phases/phase-31.md` 31-D; manual round-trip check in `desktop/MANUAL-VERIFICATION.md` 31-D. | PR #317 |
| 2026-06-23 | 32-B + Phase 32 activation | Phase 32 complete — Outlook calendar live in prod via `CALENDAR_PROVIDER=microsoft`; 32-B added `GetNextOccurrenceAsync` (Graph `/me/events/{id}/instances`). **Two reusable activation/verification traps surfaced turning the dark-shipped feature on:** (1) **A CDK slice can ship "complete" (props + `Program.cs` env reads + runtime + conditional IAM grant, all green incl. `Infrastructure.Assertions`) yet be impossible to activate, because the *workflow* never forwarded the secrets to `cdk deploy`.** The infra-assertion tests build the stack with props *directly*, so a missing `deploy.yml` `env:` line is invisible to them and to Hawk (reviews CDK, not the workflow). Fix #321 mirrored the `GOOGLE_*` lines into both `cdk deploy` steps. **Guardrail: when a CDK prop is fed from a GitHub secret, grep `deploy.yml` for the secret in *every* `cdk deploy` env block as part of the slice — the assertion tests will not catch its absence.** Secrets are repo-level, so activation also flips the *test* env to microsoft (no token there → `calendar_unavailable`); fine only because no E2E journey reads the calendar. (2) **Verifying the IAM grant in prod, the inline role policy showed only the *Google* SSM statement — the Microsoft grant was in an auto-generated CDK *overflow managed policy*** (`...ServiceRoleOverflowPolicy...`), which CDK spills statements into once the inline policy exceeds the 10 KB limit. **Guardrail: to confirm a Lambda IAM grant in prod, check `list-attached-role-policies` (overflow) too, not just `get-role-policy` (inline).** Confirmed working empirically (real MS meeting rendered) before the static read was reconciled. | PRs #321/#322 |
| 2026-06-23 | CHANGE-21 calendar-source label | Surfaced the active provider (`ICalendarClient.ProviderName` → `provider` on `GET /calendar/{date}`) + a small "Outlook"/"Google Calendar" header badge (stub/unknown → no label). **Reusable point: widening a discriminated-union API result with a new *required* field breaks every place that *constructs* the success shape, not just readers** — `tsc` caught the optimistic cache updater rebuilding `{ meetings }` without the new `provider` (fixed by spreading `{ ...old, meetings }`); without the spread the badge would also vanish mid-optimistic-update. Backwards-compatible across the deploy window: the label only renders for known providers, so a cached response with `provider: undefined` just hides the badge. | PR #323 |
| 2026-06-23 | CHANGE-22 analysis observability | Added `AnalysisDurationMs`/`AnalysisFailed` EMF metrics + `notetaker-analysis-failed` alarm + dashboard widget to the `AnalyseNote` path (mirrors the `ProjectionRebuild*` pattern exactly). **Reusable point: keep the entity id in the structured *log*, never as a metric dimension** — CloudWatch alarms reject a metric whose dimensions vary, so "which notes failed" is a Logs-Insights query on the failure log line, while the alarmable metric stays dimensionless (Service only). Mirrored the existing `SearchPerformed`/rebuild-metric convention. Verified live in prod by `describe-alarms --region eu-west-2` (8 notetaker alarms; empty result without the explicit region was a CLI default-region trap, not a missing resource). | PR #325 |
| 2026-06-23 | BUG-36 ASCII-only .ps1 | `npm run update` crashed on Windows with "the term 'silent' is not recognized" — `update.ps1` (added 31-E) carried UTF-8 em-dashes/arrows but **no BOM**. **Reusable guardrail: a `.ps1` consumed by Windows PowerShell 5.1 (`powershell.exe`, what `npm run` invokes) must be ASCII-only OR carry a UTF-8 BOM** — 5.1 decodes a no-BOM script as the system ANSI codepage (Windows-1252), where an em-dash's third byte `0x94` becomes a `"` that closes a string early and shifts the parser so a later `(silent)` is run as a command. **CI never caught it** because the Playwright suite runs on PowerShell *Core* (`pwsh` 7+), which defaults to UTF-8 — the bug is invisible to anything but real Windows PowerShell. Fix: rewrote the script ASCII-only (most durable — works on both 5.1 and 7+) and added a `publish.spec.ts` guard that fails on any non-ASCII byte in `update.ps1` (also incidentally catches a re-introduced BOM). | PR #319 |
| 2026-06-24 | 35 disable no-auth MCP (kill switch) | Disabled the 35-A no-auth MCP endpoint in prod (pending 35-E OAuth) via `MCP_ENABLED` (gated `app.MapMcp`, default true; CDK `MCP_ENABLED=false` on the shared Lambda env). **Reusable test-writing gotcha:** in CDK `Template` assertions, `FindResources(type, Match.ObjectLike(props))` does **not** apply the same partial-match semantics as `HasResourceProperties` — passing a `Match.*` filter to `FindResources` returned an **empty** dict even though `HasResourceProperties` matched. To assert that a *specific* function (not just "some function") carries an env var, walk the raw template JSON (`ToDict(TemplateJson()["Resources"])`) and filter by logical-id substring (`QueryFunction`) — the repo already has this helper pattern (`AuthTokensTableLogicalId`). Prod 404 verified live (`POST …/mcp` → 404, `/health` → 200) before marking done — kill switch confirmed, not just deployed. | PR #337 |
| 2026-06-24 | 34-D1 retire Google calendar SSM path | Google in-app connect verified in prod → removed the dead Google SSM fallback (`GoogleCalendarTokenSource` store-only; CDK env + conditional `ssm:GetParameter` grant dropped; mint script + guide removed; infra assertion repointed to the kept Microsoft grant). Re-sliced 34-D → 34-D1 (Google now) + 34-D2 (Microsoft, blocked on Outlook in-app verification) per the strangle guardrail — never retire a flow before its replacement is proven. **Reusable point (Hawk caught it): deleting a fallback branch can also delete the only exception absorber.** The old `LoadAsync` try/catch wrapped the store read *and* fell through to SSM on failure; removing the SSM fallback also removed the try/catch, so a transient DynamoDB `GetItem` throw would escape to a 500 instead of degrading to `calendar_unavailable` (the handler maps a `null` return gracefully but has no catch). When you strip a fallback, check whether it was load-bearing for error handling, not just for the happy path — keep the catch, return the safe sentinel. Prod verified: `GOOGLE_REFRESH_TOKEN_SSM_PATH` gone from the command Lambda, `MICROSOFT_REFRESH_TOKEN_SSM_PATH` retained. | PR #339 |
| 2026-06-25 | bug-37-heading-discussed-tick | The ✓ "Mark as discussed" tick fired `editor.commands.toggleStrike()` while the selection was a **collapsed caret** inside the heading (button renders on `isActive('heading')`). **Reusable guardrail:** a ProseMirror/TipTap `toggleMark`/`toggleStrike` on an *empty* selection only sets a **stored mark** (strikes the next-typed char) — it never marks the existing text. To toggle a mark across a block from a caret, **range-select first** (`$from.start()`→`$from.end()` bounds the parent textblock's text), then toggle. **Process lesson:** the bug was a 7-B feature whose acceptance ("renders with strikethrough") was **hand-checked, never automated** — a floating/caret-driven control needs a test that drives the *real selection it produces* (a headless `new Editor()` + `setTextSelection(pos)` reproduces a caret deterministically in jsdom, where the floating button itself isn't easily clickable). Reproduce-before-fix: caret-only `toggleStrike` → no `~~` in serialized markdown; range-select → `~~…~~`. Fix lives in `web/src/lib/headingDiscussed.ts` (+ `headingDiscussed.test.ts`). Full condensed entry in `phases/phase-bugs-archive.md` BUG-37. | PR #344 |
| 2026-06-25 | CHANGE-27 Tags+Actions Command Bar | Replaced the note-detail 320px right sidebar with a **full-width editor + a one-line Command Bar** under the title (inline tag chips + autocomplete; an `✓ Actions` pill opening a floating popover checklist that closes on outside-click/Esc and turns teal when all done). Extracted the tag combobox into `TagCombobox`, reused `ActionsSection` verbatim in the popover via a new optional `showHeading` prop, deleted `TagsSection`. **Reusable guardrail — relocating a UI control behind a disclosure (popover / click-to-reveal) breaks every E2E that drives the moved element, but the fix localizes to the page object, not the journeys:** keep the existing `data-testid`s on the moved elements (`tag-input`, `action-input`, `actions-list`, `actions-empty`, `tags-section`/`tag-pill-*`) and add an idempotent `OpenActionsPopoverAsync()` (no-op if already open) inside the `AppPage` helpers (`AddActionItemAsync`/`AssertActionsEmptyAsync` open the popover; `AddTagAsync` clicks the reveal); a reload closes the popover, so reload-loops must re-open it. Zero journey-logic change. **Process: prototype-first paid off for a UX-uncertain redesign** — 2 rounds × 3 divergent design subagents (sidebar-preserving rejected → full-width "Command Bar" chosen) settled the design before any production code; the locked prototype `REFERENCE.md` drove a one-shot build subagent (all 4 gates green). **Deploy-gate flake reconfirmed (no new fix):** #647 needed 3 attempts, failing on a *different* cold async-projector read-your-writes journey each time (ActionItem save-button via slow `useNoteDetail`; FilterBackNav empty cards) — the documented [deploy-gate-deflake-stacked-causes](deploy-gate-deflake-stacked-causes.md) pattern; a frontend-only relocation can't cause it. | PR #346 |
| 2026-06-25 | bug-37b-tick-zindex | The ✓ "Mark as discussed" tick did nothing because the editor content (`.contentInput`, `position:relative` for inline images) is a positioned sibling in the SAME stacking layer as the absolutely-positioned button and, coming later in the DOM, painted **over** it — so clicks landed on the heading, not the button. **Two compounding bugs:** the layering (this) + the caret-stored-mark (#344/BUG-37). **Reusable guardrails:** (1) an absolutely-positioned overlay control inside a `position:relative` editor/content area needs an explicit `z-index` — without it, DOM order wins and later content paints on top and eats the click. (2) **A click/hit-test bug is structurally invisible to jsdom** (no layout, no `elementFromPoint`); a unit test of the handler passes while the control is unclickable — exactly why #344 shipped green yet broke. A floating/overlay control MUST have a real-browser E2E (`DiscussedTickJourney`): a normal `ClickAsync` (no `force`) times out with "intercepts pointer events" on the bug and passes on the fix; `force:true` would have masked it. (3) **Don't trust a deploy on logic-only confidence** — verify the actual user gesture in a real browser before declaring a UI fix done. Debugged by extracting the missing Linux Chromium libs without root (`apt-get download` libnspr4/libnss3/libasound2 → `dpkg-deb -x` → `LD_LIBRARY_PATH`) to drive Playwright locally. Full chain in `phases/phase-bugs-archive.md` BUG-37. | PR #347 |
| 2026-06-25 | CHANGE-28 notes body text size | Shrank the editor body text — `.contentInput` `font-size` 1rem → 0.875rem (14px), `line-height` 1.75 → 1.7; font unchanged. **Reusable point: headings need no separate rule** — the editor's h1/h2/h3 carry only margin overrides (no explicit `px` font-size), so they inherit the UA `em`-relative sizes and scale down proportionally with the new base; a body-size change is genuinely a 2-value edit. Values chosen via a throwaway typography-playground prototype (live font/size/line-height + presets), which is the right tool for "find a comfortable size" vs the multi-agent divergent-ideation used for layout redesigns. | PR #348 |
| 2026-06-25 | OPS: scribe mass-delete of main + recovery | A *parallel session's* scribe commit (`996ab9d` "docs(scribe): BUG-37") **deleted the entire repo tree** (−1056 files / −105,601 lines, leaving 2 docs) and pushed it to `main` — almost certainly a `git add -A` run from a worktree whose tree was unexpectedly near-empty. Caught by *this* slice's merge gate: `git merge origin/main` reported `modify/delete` conflicts for core files (`NoteEditor.module.css`, `phase-minor-changes.md`), and `git ls-tree -r origin/main \| wc -l` = **2**. **Prod was unaffected** (the last deploy ran on the good parent `2d0530b`; the bad commit never deployed). **Reusable recovery (non-destructive, user-chosen over force-push):** `git revert --no-edit <bad-sha>` restores the whole tree, then `git checkout <bad-sha> -- <the surviving files>` re-instates the few *legitimate* edits the bad commit also carried (so the other session's real doc lines aren't lost); net diff vs the good parent = exactly those legit lines. Push fast-forwards `main` (no force). **Reusable guardrail (process-improvement, applied to CLAUDE.md):** Scribe must verify the working tree isn't catastrophically empty before committing to `main` — stage explicit paths, never blind `git add -A` in a checkout whose state you didn't establish, and sanity-check the file count. | (recovery, no PR) |
| 2026-06-26 | 41-C MCP edit_note (Phase 41 complete) | `edit_note(noteId, content)` replaces a note's body — final write tool of Phase 41 (the connector now exposes 10 tools: 5 read + create_note + add/complete/reopen_action_item + edit_note). **Reusable point: editing an existing aggregate via a non-create command can pass a null workspace safely — IF every projection preserves the view's WorkspaceId on that fold.** `edit_note` routes `EditContent` through the identity-explicit handler overload with `workspaceId: null`; verified (and Hawk re-verified against the code) that `NoteDetail`/`NoteCardList`/`NoteSearchView` all fold `ContentEdited`/`V2` as `with { Content/Body, LastModifiedAt }` and never read `envelope.Metadata.WorkspaceId` (workspace is sourced only from `NoteAssignedToWorkspace`). So the null can't drop the note from its workspace — but a *named-workspace* test (not just default, where `Matches` treats null/"" as default) is what actually proves it. **Auth: note-scoped writes (add_action_item, edit_note) share `AuthorizeOwnedNoteAsync` — `OwnsNoteAsync` against the event stream (BUG-30-safe); create_note authorizes the *workspace* since it has no note yet; complete/reopen authorize the *action's own owner* (41-B IDOR fix).** Error-map note writes that target an existing stream (`RunNoteWriteAsync`: WriteContentionException → retriable message, NoteNotFound → not-found) — create_note's fresh-GUID stream needs no wrapper. | PR #365 |
| 2026-06-30 | 43-B agenda tick/untick + coverage | Tick / untick an agenda item (2-state) + a "X / Y covered" header count, extending 43-A's composed-onto-`NoteDetailView` model. **Reusable points:** (1) when add-only ships first, the aggregate can track a bare `count` for position; the moment per-item ops (tick/edit/remove) arrive, switch to a keyed `Dictionary<Guid,_>` to validate item existence + no-op idempotent sets — and note that `Count`-based position derivation breaks once removal lands (flagged for 43-C). (2) A new event that only flips an **already-mapped** view field (`Discussed`, mapped+round-trip-tested in 43-A) needs no store change and no new `EventStore.Integration` test — the fold is exercised by the Api.Integration round-trip. (3) Per-item toggle 404 must be an accepted no-op on the client (covers both concurrent-removal and the temp-id-not-yet-reconciled window). **Deploy tail = shared cold-start flake, not the slice:** the merge deploy + the next commit both red-failed the E2E gate on *different* random journeys (BUG-38/BUG-43 SnapStart-500); one `gh run rerun --failed` went green and carried 43-B live — the documented [deploy-gate-deflake-stacked-causes](deploy-gate-deflake-stacked-causes.md) pattern, no slice-side fix. | PR #373 |
| 2026-06-30 | 43-C agenda edit/remove (agenda CRUD complete) | Inline edit + remove of agenda items, completing Phase 43's CRUD on the composed-onto-`NoteDetailView` model. **Three reusable points:** (1) **Monotonic position counter** — once removal exists, deriving a new item's `Position` from the live item *count* lets a post-remove add collide with a survivor; derive it from a counter incremented only on `AgendaItemAdded` (never on remove). For a no-removal stream it equals the count, so earlier slices don't regress. (2) **Inline-edit Esc-cancel is a real-browser bug jsdom can't catch** — removing the focused edit input fires `blur` on unmount in a browser (not in jsdom), so a naive `onBlur={commit}` commits the edit that Esc was meant to discard. Guard with an `editingRef` (mirror `ActionsSection`): `commit()` no-ops when the ref is already cleared; Enter commits once then clears it (unmount blur suppressed), Escape clears it first (blur commits nothing), blur-click-away still commits a genuine change. A jsdom unit test passes either way — the guard's correctness rests on mirroring the proven pattern, not on a test. (3) **Test QueryClient needs `staleTime: Infinity`** when a component reads a seeded `useQuery` cache and you drive async interactions — otherwise the on-mount refetch hits the default (no-agenda) MSW handler and race-wipes the seed before the user action. **Process: recurring CS8631 slip** — local `dotnet build` doesn't apply `-p:TreatWarningsAsErrors=true` (CI does), so a `string?` nullability warning on `Assert.Equal(span, array)` passed locally and red-failed CI (same as 43-A); build backend locally with that flag for backend slices (run-pipeline note added). | PR #374 |
| 2026-06-30 | 43-D collapsible agenda strip | The note-header agenda folds to a one-line summary (coverage + remaining-items peek) and expands again; frontend-only (no events/backend/CDK). **Reusable points:** (1) gate a new header control on `hasItems` so an empty entity doesn't add a focusable element — keeps existing tab-order tests valid (the empty-agenda new-note case is what NoteView's tab-order test exercises). (2) `aria-controls` must reference a MOUNTED id — when the controlled region unmounts on collapse, set `aria-controls` conditionally (omit while collapsed) rather than pointing at a missing id. (3) Stylist for a small addition = reuse the persisted `design-system/ai-note-taker/MASTER.md`, don't regenerate; here it was just focus-visible rings matching CommandBar (`prefers-reduced-motion` is already a global rule, so the caret transition needed no per-component guard). | PR #375 |
| 2026-06-30 | 43-E retire heading-✓ (Phase 43 complete) | Removed the legacy floating heading-✓ "mark as discussed" — superseded by Phase 43's separate first-class agenda. Full write-up (the heading-as-topic → BUG-37/37b → separate-agenda arc + clean-deletion technique) in [phase-43e-retire-heading-tick](phase-43e-retire-heading-tick.md). One-line reusable: **when a feature needs repeated bug-fixes to make a *conflated* model behave (here: heading doubling as topic), the model is the defect — separate the concepts rather than keep patching.** | PR #376 |
| 2026-07-01 | obs-review → BUG-31 gate cap + caller-identity logging | A 2026-06-30 `observability-review` sweep produced two shipped slices. **(1) BUG-31 cause-3 diagnosed + cheaply mitigated:** the long-standing "flaky reopen" was the cold-projector RYW lag — the stream-triggered Projector Lambda (no SnapStart/keep-warm) cold-starts ~7s while the `ConsistencyGate` cap was 2s, so a fresh read gave up (11 gate `STALE/ABSENT` timeouts/14d, all at `elapsedMs=2000`, `lastSeq=-1` at morning idle hours). Fix = raise the cap 2s→8s so the reader *tolerates* the cold start (one-off slow-success) — zero cost, vs keeping the projector warm (ping ~$0/mo or provisioned-concurrency ~$5/mo, deferred as [TI-52]). **Reusable: match resilience cost to scale — tolerate a rare cold start cheaply on the read side before paying to eliminate it.** BUG-31 stays *In Progress* (real-user symptom mitigated; re-enabling the disabled `NoteImageJourney.Remove` E2E + verifying is the remaining step — the E2E env's ~30s may still exceed 8s). PR #377, deploy #681. **(2) Un-attributable traffic → add caller-fingerprint fields, don't guess:** ~12 phantom command bursts/day (200 unique note-ids, exactly one Rename+SetDate+Content each, none ever created → correctly 404'd, no data loss) came over HTTP from a client the user didn't recognise; the `Command received` line carried no caller identity. Fix = append `user_agent`+`source_ip` (in `UseCorrelationId`, raw request) and `sub` (new `UseCallerIdentity`, post-auth) to the request log scope. **Reusable: `source_ip` = `Connection.RemoteIpAddress` not `X-Forwarded-For` (spoofable, phase-35a); never log the bearer token; register the `sub` middleware after `UseAuthentication` but BEFORE the allowlist so 403'd callers are still attributed.** Next sweep groups the phantom bursts by these fields to name the client. PR #378, deploy #683. | PR #377, #378 |
| 2026-07-01 | BUG-44 delete not consistency-gated (deleted note reappears) | A deleted note came back in the list and stayed openable → reopen+save 404'd and lost the user's text. Root cause: the **delete** was the one note write that never surfaced/threaded a read-your-writes token (`DeleteNote` emitted no `X-Consistency-Token`; `deleteNote` used plain `requestVoid`), so `useDeleteNote`'s `onSettled` `/notes/cards` refetch ran ungated against the async projector and returned the not-yet-`NoteDeleted` card, undoing the optimistic removal. Fix mirrors `DeleteTag`/`setNoteDate` exactly (handler `SetConsistencyToken`; api `requestVoidWithResponse` + `captureNoteToken`). **Reusable — the write-side analog of [BUG-30] (which was the read-side "authz on async projection"):** when reads move to the async projector, audit that **every write path threads its consistency token — not just the obvious content/rename/tag mutations.** The trap shape is `onMutate` optimistic-remove **+** `onSettled` `invalidate→refetch` on a write whose api fn discards the response: the ungated refetch silently *resurrects* the row. Hawk found the same shape still unfixed on the move paths → filed [BUG-45] (move-to-workspace reappears; folder-move flickers). Investigation-only diagnosis technique that nailed it: correlate the Command Lambda (`DeleteNote`→`NoteDeleted` vN, then `EditContent`→`NoteNotFoundException`), the Projector Lambda (applied vN *after* the delete returned), and browser RUM (`DELETE 204` → repeated `GET /notes/cards 200` → `PUT …/content 404`). | PR #382 |
| 2026-07-01 | BUG-45 move paths not consistency-gated (extends BUG-44) | Applied the [BUG-44] fix to the three note-move paths (`moveNoteToWorkspace`/`moveNoteToFolder`/`unfileNote`): handlers emit `X-Consistency-Token`, api fns thread it via `captureNoteToken`. **Reusable points:** (1) **scope matters** — a *note* move is a note-cards write, so capture with `captureNoteToken` (NOTE_CARDS_SCOPE, what `getNoteCards` gates on), **not** `captureFolderToken` (FOLDERS_SCOPE, the folder-tree read); the wrong scope silently leaves the reappearance ungated. No import cycle: `folders.ts`→`captureNoteToken` from `notes.ts` (notes.ts doesn't import folders.ts). (2) no-op moves (same ws/folder, already-unfiled) return `[]` but `HandleAsync` still returns the current version, so `SetConsistencyToken` emits a valid non-stale token — safe. (3) **the fix cascaded a THIRD instance** — Hawk (reviewing #384) found the folder-**delete** cascade (`UnfileNotesInFolderAsync` writes N `UnfileNote` events) has the same ungated-refetch shape → filed [BUG-46]; not a straight copy (N note-streams vs the single-token list gate, design #7). **Meta-lesson: a defect class rarely lives at one call site — when you fix an "async read wasn't gated" bug, grep every `onSettled: invalidate(sameList)` mutation for a response-discarding api fn.** | PR #384 |
| 2026-07-01 | 45-A event-store PITR (Phase 45 keystone) | Enabled DynamoDB Point-in-Time Recovery on `notetaker-events`, the source-of-truth event store — pure-infra slice, spec'd as `Infrastructure.Assertions` template checks (no Domain BDD spec). **Finding that motivated Phase 45: a durability-posture inversion** — a backup audit found the *only* PITR-enabled table was `notetaker-proj-calendarlinkindex`, a **rebuildable projection** (needs backup least), while the irreplaceable event store + the OAuth token tables (auth/calendar/MCP — losing them forces every user to re-consent) had **none**. `RemovalPolicy.RETAIN` only guards a `cdk destroy`, not a bad write / stray `DeleteTable`. **Two reusable points:** (1) **`cdk synth` needs ALL THREE Lambda projects published, not just Api** as CLAUDE.md "How to run" implies — `Api`, `Projector`, and `TranscribeCompletion` each `FromAsset` a `bin/Release/net10.0/publish` dir, and synth aborts with `Cannot find asset at …/src/<Proj>/bin/Release/net10.0/publish` for whichever is missing; publish all three before synth-gating an infra slice. (2) **Don't hand-roll a `DeletionPolicy: Retain` assertion for the events table — it already exists** (`EventsTable_HasRetainDeletionPolicy`); Hawk caught the exact duplicate. Grep the assertions file for the table name before adding a policy check. PITR verified live in prod (`describe-continuous-backups` → `ENABLED`, 35-day window). Remaining Phase 45: 45-B (token-table PITR + DeletionProtection), 45-C (S3 versioning), 45-D (projection PITR), 45-E (AWS Backup + DR runbook). | PR #380 |
| 2026-07-01 | 46-B interactive task-list checkboxes | Wired the already-installed `TaskList`/`TaskItem` into the Tiptap editor so `- [ ]`/`- [x]` render as clickable checkboxes. **Reusable:** tiptap-markdown serialises **taskList loose** (blank line between items) because it reuses bulletList's serializer but the taskList node carries no `tight` attr → `renderList` falls back to loose (and `Markdown.configure({tightLists:true})` doesn't reach it — that option isn't passed into the serializer state for taskList). Left as-is it silently double-spaces every checklist on the first edit-save (same class of silent-save-mutation as [46-A]'s dropped table alignment). Fix = give the node a `tight` attr (`default:true, rendered:false`) via `TaskList.extend({addAttributes})` — the existing serializer then emits tight, no serialize override needed (cleaner than 46-A's `getMarkdownSpec` override). Toggle *is* testable in jsdom: `fireEvent.click` the checkbox input flips the doc and fires onChange. | PR #383 |
| 2026-07-01 | 47-A list_folders + create_folder MCP tools | Phase 47's proving slice — Claude can list + create folders over `/mcp`. **Reusable #1 (cost a debug cycle):** an **optional MCP tool parameter must come AFTER `CancellationToken` and carry a C# default** (`string? parentId = null`). A nullable-without-default param is treated as **required** by the ModelContextProtocol binder, so omitting it fails during *binding* — before the method body runs — with a generic `"An error occurred invoking '<tool>'"` that surfaces **no server stack to the client** (widening the tool's `catch` to `Exception` never fired because the throw was pre-body; had to reason from the binding order). Convention is codified in a `list_meetings` comment but was re-learned — grep existing tools for the `CancellationToken ct,` -then-optional ordering before adding a tool. **Reusable #2:** the identity-explicit handler overload (33-B2) extends cleanly to a **3rd** handler (`IFolderCommandHandler`) by parameterizing only the envelope-stamping seam (`ToEnvelopes(streamId, events, userId, workspaceId)`, now `static`) — the route-coupled overload delegates with `currentUser`/`currentWorkspace`, every other route-coupled call site passes the scoped values unchanged. Add the overload only for the command the slice uses (`CreateFolder`); Rename/Delete/Move get theirs when 47-C/47-D need them. Tools live on `NoteMcpTools` to reuse `AuthorizeWriteAsync` (avoids duplicating auth — a BUG-41 risk). Deploy-time neutral. Clean E2E gate on deploy #689 (post-BUG-43 — cold-start flakes appear reduced). | PR #385 |
| 2026-07-01 | 46-C emoji shortcodes (Phase 46 complete) | `:shortcode:`→emoji in the Tiptap editor via a curated dependency-free map + (a) `emojifyMarkdown` load transform and (b) an InputRule for live typing. **Reusable — a raw-markdown-string transform must respect markdown syntax boundaries, not just code.** First cut protected only fenced/inline code (split on a capturing regex, emojify even/prose indices); Hawk caught that a `:word:` inside a **link/image URL or bare URL** (`[x](https://h/a:key:b)`, `see https://h/a:warning:b`) was still emojified → silently corrupts the URL on the next save (same silent-save-mutation class as 46-A alignment / 46-B loose lists). Fix = add `](url)`, `<autolink>`, and bare `https?://…` to the protected alternation (labels/alt-text still convert). Also protect an **unterminated** code fence (`\`\`\`[\s\S]*$`). Two gotchas: (1) `String.split` needs ONE outer **capturing** group or delimiters are dropped — build the alternation then wrap in `( … )`, keeping inner `\(`/`\)` escaped so there's exactly one group; (2) InputRule live-typing IS testable in jsdom — drive `view.someProp('handleTextInput', f=>f(view,from,to,ch))` char-by-char. Emoji persists as unicode (one-way, documented). Footnotes/math/definition-lists deferred to future-features (high-cost custom Tiptap nodes, not free plugins). | PR #386 |
