# Token Usage Log

Approximate tokens per slice. Recorded by Scribe at slice end; rounded to nearest 1 000. `Pip` folds Breaker + implementation + orchestration; `Hawk` folds all review rounds (the recurring spike). One row per slice; full per-agent breakdowns are in git history.

Recurring cost drivers and their pre-emptions live in [token-optimisation-playbook.md](token-optimisation-playbook.md) — add new avoidable costs there, not as prose here.

| Slice | Total | Pip | Hawk | Dominant cost driver |
|-------|------:|----:|-----:|----------------------|
| 3-A Add action items | 183k | 95k | 35k | First cross-aggregate slice; context compaction; 2 Hawk rounds |
| 3-B Complete/reopen | 86k | 55k | 8k | Pure extension; no Hawk rework |
| 3-C View open todos (home) | 113k | 62k | 6k | First cross-projection; layer-split kept Pip under 65k |
| 3-D Complete todos (home) | 40k | 20k | 4k | No new backend; 5 files |
| 3-E Delete action item | 55k | 28k | 5k | Extension; layer-split |
| 4-A Settable note date | 76k | 40k | 5k | First Phase 4; doc-fix pass + layer-split |
| 4-B Note layout redesign | 19k | 6k | 3k | Pure frontend |
| 4-C Implicit action add | 14k | 5k | 3k | Minimal frontend; lightest Phase 4 |
| 4-D Persistent sidebar | 18k | 8k | 3k | Pure frontend |
| 4-E Note summary cards | 82k | 53k | 6k | Largest Phase 4; new projection (9 handlers); 3 must-fix |
| 5-A B1 Add tags | 32k | 18k | 4k | In-flight domain; projection fixes |
| 5-A/B B2 Tags frontend | 11k | 3k | 1k | Pure frontend; pre-scaffolded |
| 5-C B1 TagIndex + GET /tags | 24k | 16k | — | Backend-only; followed FolderTree pattern |
| 5-C B2 Tag filter bar E2E | 43k | 40k | — | 3 deploy failures (Playwright race + eventual consistency) |
| 5-D B1 Create/browse folders | 29k | 18k | 2k | New aggregate; well-bounded |
| 5-D B2 Folders frontend | 23k | 12k | 2k | Pure frontend wire-up |
| 5-M Date defaults to today | 31k | 12k | 4k | Frontend-only |
| 5-EFGHIJKL Folder ops | 350k | 200k | 140k | 8 sub-slices in one PR; started on main not worktree; 2 Hawk rounds |
| 6 Upgrade to .NET 10 | 68k | 38k | 10k | CI failures (stale net8 refs); 2 Hawk rounds |
| 6.5-B Vitest scaffold | 67k | 20k | 35k | vitest lock-file Node-version desync |
| 6.5-C Home component tests | 70k | 55k | 10k | 2 blocking main-deploy failures (ConsistentRead, empty-string) |
| 6.5-D Note-view component tests | 50k | 35k | 10k | 2 Hawk rounds; stuck CI runner |
| 7-A Base editor + markdown | 147k | 45k | 77k | 2 Hawk rounds; stale-closure bug |
| 7-B Heading discussed + panel | 117k | 45k | 50k | 2 Hawk rounds |
| 7.5 Folder UX + Lambda perf (A–F) | 278k | 180k | 75k | 6 slices, 2 sessions; 7.5-F 2 Hawk rounds |
| 7.8-A Production pipeline | 5k | — | — | Manual setup; no agents |
| 7.8-G Domain event dispatcher | 129k | 20k | 104k | 2 Hawk rounds; soft-delete-timestamp bug |
| 7.8 Note-screen UX (B–F) | 320k | 215k | 95k | 5 slices one session; multiple 2-round reviews |
| 7.8-I Read-only smoke suite | 76k | 25k | 46k | Hawk read all 5 handlers for a 2-file PR |
| 7.8-H Human-readable URLs | 204k | 185k | 12k | 7 successive deploy failures |
| 8-A CDK + CORS wiring | 99k | 20k | 65k | 2 Hawk rounds; CORS assertion mismatch |
| 8-B Google Sign-In (frontend) | 155k | 65k | 70k | 2–3 Hawk rounds; OAuth state + E2E bypass |
| 8-B fixes Backend token + layout | 124k | 40k | 79k | 2 post-merge prod bugs (client_secret required) |
| 8-C/D JWT auth + isolation | 233k | 180k | 25k | IDOR gap post-merge (7 commits); compaction |
| Hotfix Auth token persistence | 21k | 18k | — | React effect-ordering race |
| 9-B Calendar API pass-through | 145k | 95k | 45k | 2 Hawk passes; Google SDK edge cases |
| 9-C NoteLinked + projection | 189k | 85k | 96k | 2 Hawk passes (Task.WhenAll swallow, ct) |
| 9-D One-click note from meeting | 180k | 85k | 75k | 2 Hawk rounds (user-isolation); compaction |
| 9-E Reminder hook + banner | 92k | 52k | 35k | 2 Hawk passes (timer churn) |
| 9-F Next-occurrence note | 135k | 50k | 80k | 2 Hawk rounds (6 findings) |
| 9-G CDK CalendarLinkIndex | 73k | 12k | 44k | Hawk 60% (SSM IAM negative test) |
| Hotfix TRANSCRIBE_ROLE_ARN 503 | 99k | 95k | — | 3 failed fix deploys; stale-alias root cause |
| Hotfix 9-F title + stub | 125k | 55k | 65k | 2 Hawk passes + 3 failed deploys (merge order) |
| 10-B Live transcript | 374k | 230k | 134k | 2 Hawk rounds + compaction + lint hotfix |
| 10-C Persist transcript | 222k | 120k | 98k | 2 Hawk passes + lint hotfix |
| 10-D Manual analysis (Bedrock) | 371k | 250k | 86k | Compaction mid-Pip + 2 Hawk rounds |
| 10-E Auto-analysis on stop | 144k | 85k | 47k | WSL suite + merge-gate incident |
| 10-F Capture remote participants | 172k | 90k | 73k | bin/obj context dump + 1 Hawk round-trip |
| 10-G Eval harness + prompts | 387k | 250k | 99k | Re-cut drifted scaffold; nightly-no-op bug caught |
| 10-I Record TagsSuggested | 125k | 70k | 46k | Clean backend; ref reads amortise across phase |
| 10-J Tag feedback projection | 188k | 120k | 57k | Meatier; dead-dispatcher discovery; parity fix |
| 10-K Record ActionItemsSuggested | 110k | 60k | 41k | Mechanical copy of 10-I |
| 10-L Action-item feedback projection | 207k | 130k | 65k | Cross-stream rebuild-ordering design |
| 10-M Stamp modelId/promptVersion | 273k | 80k | 120k | Event-versioning; Hawk traced every consumer ×2 |
| 10-N Migrate to Converse API | 224k | 70k | 109k | 2 Hawk rounds; behaviour-identical transport swap |
| MPI-2/10-P Ship analysis@v5 | 240k | — | 52k | eval-run; git-collision recovery (~55k) |
| 11-A Tag autocomplete | 137k | 55k | 73k | 2 Hawk passes; combobox a11y |
| 11-C Delete blank note on cancel | 103k | 25k | 75k | 2 Hawk passes; unawaited-Promise |
| 11-D Token expiry + silent refresh | 140k | 88k | 47k | Pip: fake-timer + findByRole iteration |
| 11-F Adaptive action buttons | 112k | 25k | 82k | 2 Hawk passes; predicate-arm test gap |
| 11-G Fix 401s in active sessions | 122k | 45k | 72k | 2 Hawk passes; jwtExpired inversion |
| 11-H Fix note-not-deleted on discard | 53k | 20k | 30k | Post-merge tsc error (prop signature) |
| 12-E + 12-H Alarms + unified errors | 372k | 137k | 95k | Parallel on shared file + deploy-break recovery |
| 12-F Frontend monitoring (RUM) | 196k | 95k | 45k | Plan agent pre-resolved Cognito → first-pass Hawk |
| 12-G Observability runbook + queries | 198k | 100k | 98k | Docs+queries verified vs real prod logs; 2 Hawk rounds |
| 16-A Browse meetings by date | 239k | 70k | 83k | End-to-end slice; Hawk traced full path |
| 17 Link note to meeting (A+B) | 335k | 125k | 95k | 2 slices one session; Scout docs landed mid-pipeline |
| 18-A Draft store + endpoints | 315k | 95k | 60k | Data-loss scare forensics + docs-to-main race |
| 18-B Frontend autosave + recovery | 215k | 70k | 80k | Concurrent 19-A api-split merge mid-build |
| 18-C Continue a transcript | 145k | 55k | 55k | Single-pass; Hawk caught stale-seed replace-on-2nd-Continue |
| 19-A Split api.ts into modules | 380k | 70k | 50k | Audit fan-out (~215k, amortises across Phase 19) |
| BUG-1 Blank screen on 401 | 163k | 110k | 44k | Merge/deploy orchestration vs fast-moving main |
| BUG-2 favicon 404 | 37k | (prior) | 28k | Recovery slice; trivial frontend fix |
| BUG-3/4/5 Backend defect sweep | 233k | 120k | 95k | 3 bugs one session; up-front backend explore |
| BUG-11 Session refresh-token flow | 214k | 55k | 62k | Cookie redesign; Hawk caught 30-day residual |
| CHANGE-4 To-do row wrapping | 105k | 60k | 37k | Recovery + 2 full-suite runs |
| CHANGE-5/6/7 Parallel minor batch | 536k | 244k | 112k | Parallel shared-file (App.css); orchestration ~180k |
| CHANGE-8/9 Sequential minor pair | 209k | 100k | 69k | Sequenced → zero conflict; cost was wall-clock |
| CHANGE-10 Home refinement | 164k | 70k | 40k | 3 prototype rounds (subjective brief) |
| CHANGE-11 Preview pull-out »↔« | 100k | 60k | 32k | Prop-threading + toggle |
| CHANGE-12 Notes divider/alignment | 70k | — | — | CSS tweak; backlog numbering collision |
| CHANGE-13 Next-occurrence control | 216k | 95k | 47k | Reuse-in-new-location; self-review caught 2 bugs |
| CHANGE-14 Rename audio toggle | 90k | 16k | 33k | One-line copy; smallest slice; Hawk is the floor |
| tech-remove-dead-dispatcher | 170k | 32k | 55k | Dead-dispatcher cleanup; Hawk coverage matrix |
| 20-A TanStack foundation + todos | 175k | 115k | 57k | Inherited a near-complete worktree (prior session); cost was verification + flaky vitest segfault retries, not authoring |
| 20-B Folders (tree) | 360k | 290k | 59k | Two mid-build rebases (21-A router, then 22-A/BUG-12) onto shared App.tsx + a `git rebase --quit` detached-HEAD recovery; explore+spec+impl in one session |
| 20-D Actions + tag index | 330k | 240k | 88k | Two domains, 5 components, 4 hooks; component-only (no App.tsx → one clean rebase); Hawk round on test coverage (rollback tests + prove temp-id swap) |
| 20-C Note cards / list | 470k | 330k | 95k | Biggest App.tsx consolidation (unify cards + useNotes, delete useNotes); post-merge E2E regression (tag-invalidation churn + fire-and-forget setNoteDate) → fix-forward PR #195 (red main). E2E only catches it after merge |
| 20-F Meetings | 250k | 175k | 61k | Two date-keyed queries cleanly preserve the Phase 16 reminders-vs-browsed decoupling; staleTime baked into the hook so the no-refetch test pins it; clean deploy+E2E (20-C lessons applied) |
| 21-A Router foundation + note/home URLs | ~340k | ~280k | 61k | Spike: two avoidable rounds — react-router-dom/Vitest vmThreads CJS-.mjs debug, then a mid-slice 20-A merge that broke PR-merge CI and forced a re-verify. Authoring itself was small. |
| 22-A Search read model + fuzzy endpoint | ~330k | ~255k | 75k | Backend slice; Explore map + single Pip pass (impl ~183k); Hawk caught O(user-notes) write-path scan → point-get. No spike. |
| 22-B Home search bar | ~220k | ~150k | 53k | Frontend; built in parallel w/ 22-A deploy; search kept in ListView (zero App.tsx edit) dodged the 20-B parallel conflict; merged main pre-PR; Hawk clean (3 nits). |
| 21-B Folder & sub-folder URLs | ~200k | ~150k | 47k | Clean single pass on top of 20-B's TanStack folders; Hawk approved first round (3 nits). One self-fixed regression (URL leak across tests → global reset) + a flaky eventstore CI rerun. No spike. |
| 21-C Deep-link edge cases | ~210k | ~165k | 44k | Closes Phase 21. Single pass; Hawk approved first round (3 optional nits, none applied). Auth-flow investigation (OAuth stash/restore) + new E2E journey. No spike. |
| 22-C Highlight matched terms | ~200k | ~150k | 43k | Backend (matchedTerms) + frontend (Highlight) single pass; Hawk approved first round (4 optional nits). Cost tail: NoteCard.tsx conflict with parallel 20-C → merge+resolve, and a false-green `gh pr checks --watch` (only CodeRabbit ran on the conflicting branch). |
| 20-E Note detail | ~640k | ~500k | 120k | **Spike — parallel-driver collision.** Built the slice twice: PR #198 against a stale local `phase-20.md` (seed-state approach), discovered 20-F + an authoritative draft-pattern 20-E spec were already on `origin/main`, threw it away, rebuilt as PR #199 off current main. Root cause: never `git fetch`ed before reading the plan. Both Pip passes were otherwise clean; Hawk approved each first round. See [phase-20e-note-detail](learnings/phase-20e-note-detail.md). |
| 24-A Bounded rebuild writes | ~280k | ~200k | ~80k | Backend single pass; Explore recon + one Pip pass (BoundedWrites helper + handler + 7 tests). Hawk REQUEST CHANGES on the under-tested OCE/timeout arm (the real 5s-timeout case) → fix → approve. No spike. See [phase-24a-bounded-rebuild-writes](learnings/phase-24a-bounded-rebuild-writes.md). |
| 19-D Context provider perf | ~170k | ~130k | ~40k | Small frontend memoisation slice; one clean Pip pass, Hawk approved first round (applied 1 of 2 optional nits). Cost tail = triaging 4 flaky `TagsJourney` E2E deploy re-runs (pre-existing flake, confirmed change-independent → routed to technical-improvements). No spike. See [phase-19d-context-provider-perf](learnings/phase-19d-context-provider-perf.md). |
| 20-G Cleanup + 19-H backoff | ~260k | ~190k | ~46k | Closes Phase 20. apiFetch transient-read backoff (subsumes 19-H) + dead `listNotes` removal; single Pip pass. Turning retry on for PUT/DELETE broke 6 optimistic-rollback tests → narrowed to GET/HEAD only (the correct line — see learnings); Hawk approved first round (4 optional nits). Cost tail = a lost-commit recovery (concurrent commit + push-waiter race → `git reset` reflog) and a main-deploy `TagsJourney` E2E flake gating the merge. No spike. See [phase-20g-cleanup](learnings/phase-20g-cleanup.md). |
| 24-B Upsert-and-reconcile | ~430k | ~290k | ~125k | Multi-store change: 5 new methods × (interface+Dynamo+double) + handler reconcile + 4 tests. Explore recon corrected one wrong conclusion (search tombstone direction). 2 Hawk rounds (~80k+43k): unpaginated reconcile scans + a tombstone test that passed trivially. No spike (breadth, not rework). See [phase-24b-upsert-reconcile](learnings/phase-24b-upsert-reconcile.md). |
| 23-A Workspace aggregate + CRUD | ~310k | ~245k | ~66k | Opens Phase 23. New aggregate + `WorkspaceList` projection + `/workspaces` CRUD + CDK table + tests + event-model/view-schema docs; single Pip pass off an upfront pattern-map read. Hawk approved first round, one Important nit (unused `CreateWorkspace.CreatedAt` contract lie → removed). Cost tail = one `TagsJourney` E2E deploy flake (change-independent, cleared on re-run). No spike. See [phase-23a-workspace-aggregate](learnings/phase-23a-workspace-aggregate.md). |
| 23-B Scope write path + note read models | ~520k | ~390k | ~115k | **Keystone — breadth, not rework.** ~40 files: new `NoteAssignedToWorkspace` event + aggregate fold + `EventMetadata.WorkspaceId`, `/w/{wsId}` route group + validation filter + `ICurrentWorkspace`, and `WorkspaceId` threaded through 5 read models (view+projection+store) + handler filters. Explore agent mapped all 6 read models up front (~70k); single long Pip pass (no compaction). Hawk approved first round but caught a real keystone bug — TagIndex live path stamped the *route's* workspace, rebuild the *note's* → fixed to derive from the note + regression test + a DynamoDB-Local search-GSI round-trip test. Cost tail = the same `TagsJourney` flake (2nd phase-23 occurrence) + fast-moving main (waited out a CI-workflow deploy). Large but expected for the keystone's surface area. See [phase-23b-scope-note-readmodels](learnings/phase-23b-scope-note-readmodels.md). |
| 23-C Scope folders + to-dos; delete-if-empty | ~300k | ~225k | ~64k | Reused the 23-B conventions across 2 read models (FolderTree, TodoList) + `EventEnvelopeFactory.WorkspaceId` stamping + dual-mapped `/folders`+`/todos` + `DeleteWorkspace` empty-check. Explore mapped folder/todo/delete wiring up front; single Pip pass. **Chosen specifically as Phase-25-disjoint work** (folders/todos vs media/tags) — kept off `Builder.cs`/`NoteEndpoints.cs`/`Program.cs`/`NoteTakerStack.cs`, so the merge was conflict-free against the fast-moving main. Hawk approved first round; 2 non-blocking comment fixes (TodoList rebuild back-fill is essential not incidental; flag 23-F move-coupling). Deploy green first try (no flake). No spike. See [phase-23c-scope-folders-todos](learnings/phase-23c-scope-folders-todos.md). |
| 23-D Frontend workspace routing + context | ~430k | ~330k | ~65k | Largest frontend slice of the phase: `/w/:wsId` routing, `WorkspaceProvider`/`useCurrentWorkspace`, module-global `workspaceStore` for the non-React api-client prefix, workspace-scoped query keys. Explore mapped the full FE wiring up front. **Parallelised the test repair** — the routing change broke 127 vitest tests; a pivot to "prefix-only-when-store-set" cut that to 23 (8 files), then 3 subagents fixed those disjoint files concurrently. Hawk approved first round (verified the render-time store-sync is correct, not a tearing bug). Deploy failed once — NOT a 23-D issue: another session's 26-C canary-deploy gated the Lambda alias on the `p99-latency` alarm and rolled back; 23-D rode to prod on 26-C's revert. See [phase-23d-frontend-routing](learnings/phase-23d-frontend-routing.md). |
| BUG-14 + E2E flake saga | ~470k | ~330k | ~85k | Two PRs on one thread: #203 (E2E tag-pill 15s→45s, **wrong** cold-start-latency diagnosis) then deploy #493 failed *with 45s applied* → re-diagnosed as a real optimistic-update race (multi-tag, note-in-flight) → #205 (reproduce-then-fix in `useTagMutations` + revert #203). Cost tail = the misdiagnosis loop + ~6 deploy/E2E watch cycles. Hawk approved both first round. Lesson: a near-deterministic "never appears" E2E timeout is a missing render, not latency — don't raise the timeout. See [phase-bugs#bug-14](phases/phase-bugs.md). |

| BUG-15 cold-start silent refresh | ~250k | ~195k | ~52k | Small frontend fix (one bootstrap effect + `authLoading` gate + spinner + 4 tests) on top of two Explore diagnosis passes (auth model + session-persistence). Hawk approved first round (only nits). Cost tail, not authoring: the **test-tsconfig CI miss** (local `tsc --noEmit` green, CI `tsc -p tsconfig.test.json` red on a stale `AuthState` literal → extra commit + full CI cycle), ~160s WSL vitest runs ×4, transient `gh` 401s under the sandbox, and a parallel-merge deploy-queue wait (#499 ahead of #500). No spike. See [phase-bug15-cold-start-silent-refresh](learnings/phase-bug15-cold-start-silent-refresh.md). |
| BUG-16 conditional consent prompt | ~300k | ~150k | ~101k | Frontend fix: `forceConsent` flag + lifecycle + 9 tests; backend regression test. Began with a design fork (refresh-token-vs-consent coupling → AskUserQuestion). **2 Hawk rounds (~54k+48k)** — round 1 REQUEST CHANGES caught the missing flag-clear on the 4th refresh-failure path (api-layer 401), a real ~1h-loop gap. Dominant cost was **orchestration waiting on the recurring `TagsJourney` E2E flake**, which red-gated main deploys #502/#503 (parallel merges) and #504 (this one) — three rerun cycles before the merge gate cleared. No authoring spike; the cost was the flake + the second review round. See [phase-bug16-consent-prompt-once](learnings/phase-bug16-consent-prompt-once.md). |
| 19-F1 a11y live regions | ~205k | ~150k | ~49k | Light authoring (role attrs + 10 tests across 8 components); a re-audit Explore subagent confirmed the 6-day-old finding list was ~half stale. **Dominant cost was orchestration gate-waiting**, not work: a parallel session was rapid-merging 26-x/23-D into main (overlapping/cancelled deploys + a 26-C revert), so the #229 merge gate took ~40min of deploy-monitor polling to clear a stable-green window — compounded by recurring GitHub API i/o timeouts. One clean Hawk round (APPROVE, no must-fix). |
| 19-F2 focus-visible | ~95k | ~70k | ~47k | Small slice: 6 CSS one-liners + one component refactor (MeetingPicker Esc → focus-trap onClose). Main value was the in-code scope correction (dropped the spec's SessionExpiredBanner Esc — blocking re-auth gate). One clean Hawk round (APPROVE; it re-ran lint/tsc/tests itself). Merge gate cleared on the first stable-green window — much cheaper than 19-F1. |
| 19-F3 jsx-a11y | ~165k | ~120k | ~65k | Scoped more than the spec implied: an ESLint-10 peer-range gap (solved with a scoped `overrides`, investigated empirically) + triaging a 12-violation backlog (7 files, mostly justified scoped disables). Cost drivers: the override investigation, the JSX-comment-placement iteration (disable directives in wrong position → re-lint), and a post-merge fix-forward (#525 deploy red on a parallel-merged `WorkspaceSwitcher` the gate retroactively caught). Hawk thorough (65k, ran npm ci + eslint --max-warnings=0 itself); APPROVE no must-fix. |
| 23-E Workspace switcher + CRUD UI | ~480k _(est.)_ | ~310k | ~105k | Prototype-first (Variant A dropdown + inline CRUD) → real impl: `WorkspaceSwitcher` + `useWorkspaces`/`useWorkspaceMutations` + global non-reset `workspaces` query key. 2 Hawk rounds (~61k+45k): round 1 REQUEST CHANGES caught two real UX gaps (deleting the *active* workspace stranded the route; outside-click leaked a stale 409 error) → fixed + 2 regression tests. Cost tail = two **user-reported live bugs** spun into hotfix #239 (popover text truncation + the `removeQueries` stuck-loading-on-switch). |
| 23-F Move a note to another workspace | ~560k _(est.)_ | ~390k | ~125k | Full-stack: `MoveNoteToWorkspace` (+`NoteUnfiled`) + endpoint w/ target-ownership check + live re-bucket of TodoList/TagIndex rows + card move menu (optimistic removal). 2 Hawk rounds (~73k+52k). **The convergence test (live==rebuild) earned its keep** — caught a real divergence Hawk's inspection missed: `TagIndexProjection` rebuild didn't re-stamp existing tag rows on reassignment → fixed (mirrors TodoList back-fill) + unit + integration regression. No authoring spike; breadth + the divergence fix. See [phase-23f-move-note-rebuild-divergence](learnings/phase-23f-move-note-rebuild-divergence.md). |
| 23-G Cleanup + backfill | ~470k _(est.)_ | ~330k | ~67k | Removed rootless content routes (+ fixed a latent inconsistency: transcription/analyse were never prefixed). Migrated 32 test files with **zero churn** via a test-server `IStartupFilter` (covers `WithWebHostBuilder` clients a per-client handler missed). One clean Hawk round (APPROVE, one test-hygiene fix). **Cost tail = a deploy-time miss**: `Api.Smoke` still hit rootless paths → `deploy-test` 404'd post-deploy, prod gated off, fixed-forward (#242). The in-process shim masked it — green PR CI ≠ safe when the live API surface changes. See [phase-23g-rootless-route-removal](learnings/phase-23g-rootless-route-removal.md). |
| 27-A Extract ProjectionUpdater | ~370k _(est.)_ | ~270k | ~82k | Opens Phase 27. Behaviour-neutral lift-and-shift of inline projection writes from all 5 handlers into one scoped `ProjectionUpdater` (the seam 27-B's async projector reuses). One Pip pass off an upfront read of all 5 handlers + the feedback stores. **That upfront read paid off as a Scout-time scope fix, not Pip rework**: the feedback counters are `ADD`-increments (not upserts), so redelivery-idempotency can't be cheap → descoped to 27-B's processed-position guard *in the phase doc before any code*. One clean Hawk round (APPROVE, no must-fix; verified all 5 aggregates byte-identical). Cost tail = the known `gh run watch <number>` 404 (use `databaseId`) faking a deploy-fail for one round. No spike. See [phase-27a-projection-updater](learnings/phase-27a-projection-updater.md). |
| CHANGE-15 folder-preview keyboard | ~120k _(est.)_ | ~75k | ~42k | Small single-component a11y fix run inline (no separate Breaker/Pip subagents): click/drag-only `<li>`→real `<button>`, drop the scoped jsx-a11y disable, +2 tests. One clean Hawk round (APPROVE, no must-fix; took suggestion #2 — a `draggable` drag-source assertion). Cost tail = a stray `pkill` killing my own shell mid-commit (re-ran) + the standard ~100s WSL vitest env startup ×2. No spike. See `_minor-log.md`. |
| 27-B Stream + Projector (shadow) | ~950k _(est.)_ ⚠️ **spike** | ~780k | ~150k | **Phase-sized infra slice; layer-split into 2 Pip passes.** Pass A (context-free `ProjectionUpdater` + `StreamProjector` engine + position guard + 7 tests), Pass B (CDK stream/projector Lambda/ESM/DLQ/least-priv IAM/alarms + 15 infra assertions + deploy-publish). Up-front Plan agent designed packaging + re-read-vs-NEW_IMAGE + guard table. **Spike drivers, NOT rework:** (1) breadth — a whole new Lambda project + CDK surface + a net-new ES concept; (2) the **Pass-A sub-agent was cut mid-run by a monthly spend limit** (52 tool-uses, no commit), so I verified its partial A1, committed it to lock it, and finished A2–A4 + tests by hand; (3) a 3-step package-downgrade chain (`Amazon.Lambda.Core` 2.5→3.1 to satisfy Powertools+Api). 2 Hawk rounds (101k+49k): round 1 REQUEST CHANGES caught a real CI-gate miss (`pr.yml` had no projector publish → the synth check would go red) + a missing failure-propagation test. See [phase-27b-stream-projector-shadow](learnings/phase-27b-stream-projector-shadow.md). |
| 27-C async cutover + 27-C2 + REVERT | ~1.7M _(est.)_ 🔴 **major spike** | ~1.2M | ~0.4M | **The phase's failure case — and most valuable lesson.** 27-C (backend append-only, Plan→Pip ~145k + Hawk ~93k) + 27-C2 (frontend optimistic propagation, Pip ~135k + 2 Hawk + 2 fix passes) both deployed RED (read-after-write broke across navigation). **Spike driver = an architectural misstep, not tooling:** the consistency-model flip was attempted *before* a client read-your-writes foundation existed, so it surfaced as production whack-a-mole. Large cost tail was a **cross-account production incident diagnosis** — I chased a "dead projector" red herring in the idle *prod* account before realising the failure was in the *test* account (separate creds), where the projector was healthy (~1s lag) and the real cause was the frontend's refetch-races-async. User chose to **revert** (keep the projector dormant as the ES artifact) and pursue read-your-writes (27-RYW) as the prerequisite. Revert itself was clean (git revert ×2 + ESM disable, Hawk APPROVE). See [phase-27c-async-cutover-reverted](learnings/phase-27c-async-cutover-reverted.md). **Playbook: gate a consistency-model flip on the client read-after-write contract; never flip async before read-your-writes exists.** |
| RYW-1 Todo read-your-writes spike | ~620k _(est.)_ | ~480k | ~115k | **Opens Phase 27-RYW the RIGHT way — a thin vertical proving the whole loop on one call (add-a-to-do).** Plan agent up front confirmed two gotchas (folder/workspace re-creation if the projector re-enables without the delete arms; the in-process host needs the projector-writer back). Two Pip passes (backend foundation: async Todo + gate + token + projector re-enable + the `SyncProjectingEventStore`/`MigratedPrefixes`/shared-position-store foundation; then frontend+E2E). **My review caught the load-bearing flaw the frontend agent missed:** the add path is optimistic-only (no refetch), so the gate/token were dead code and the E2E passed via the optimistic row — the 27-C2 cache-trap again. Fixed by persisting the token in `sessionStorage` + a reload-truth E2E (drop the cache, prove the server). One clean Hawk round (APPROVE, no must-fix — verified the off-by-one token arithmetic end-to-end). Merge gated on an explicit user go-ahead (it re-enables the prod projector). No spike. See [phase-27ryw1-todo-read-your-writes](learnings/phase-27ryw1-todo-read-your-writes.md). |

| RYW-2 Note read-your-writes | ~700k _(est.)_ | ~510k | ~145k | Resumed a VS-Code-crash-interrupted slice already implemented + locally green; verified all suites, ran Refactor (no smells), added the missing `NoteReadYourWritesJourney` E2E (RYW-1 had a Todo one), shipped #255. 2 Hawk rounds (~100k #255 + ~44k #256), both APPROVE first pass. **Cost tail = a post-deploy E2E failure + roll-forward, NOT authoring:** #255 passed all PR CI then red-gated the deploy on `TagsJourney.AddTag_PillAppearsOnHomeCard` — RYW-2 made the cards list async, but the *pre-existing* journey asserted a freshly-tagged card with no reload → cold-projector `stale` race. Fixed-forward #256 (centralised reload-tolerant cards-list reads + an in-process test proving the projector builds card tags). No authoring spike; the new playbook entry captures the generalisable trap. See [phase-27ryw2-note-read-your-writes](learnings/phase-27ryw2-note-read-your-writes.md). |
| BUG-21 note-title draft reconcile | ~285k _(est.)_ | ~220k | ~64k | Small, tightly-scoped fix run inline (no separate Breaker/Pip subagents): title migrated to the content/date draft pattern + new `useRenameNoteDetail` keystone mutation + `HandleRename` empty guard; 5 frontend + 2 domain tests. One clean Hawk round (APPROVE, 3 nits left as-is). **Cost tail = merge-gate wait, not authoring:** an *unrelated* parallel merge (#254) red-gated main's deploy #546 on the recurring `TagsJourney` async-projection E2E flake; `gh run rerun --failed` cleared it before BUG-21 could merge. Same flake family as BUG-14/RYW-2 — it taxes whoever holds the gate. No spike. See [phase-bug21-note-title-draft-reconcile](learnings/phase-bug21-note-title-draft-reconcile.md). |
| BUG-22 RYW token max-version | ~300k _(est.)_ | ~235k | ~52k | Started as a flaky-build **investigation** (deploy #546, attempts 1–2): traced the `TagsJourney` E2E failures to RYW-2's async cutover + a last-writer-wins consistency-token slot. Fix run inline (no Breaker/Pip subagents): 25-line guard in `consistencyTokens.ts` + 6 deterministic tests; reopened TI-19, logged BUG-22. One clean Hawk round (APPROVE, no blocking). **Cost tail = the same merge-gate whack-a-mole BUG-21 paid, worse:** main was red across #548/#549/#550 on *two* flakes (`NoteImageJourney` fixed in parallel by #264, plus the very `TagsJourney` flake this PR fixes); two rebases onto a moving main + reruns before #551 landed green (E2E 20/20 first-try). No authoring spike — the tax is the red-main serialisation, not tokens. See [phase-bug22-ryw-token-max-version](learnings/phase-bug22-ryw-token-max-version.md). |
| 28-A image preset sizes (close-out) | ~150k _(est.)_ | ~25k | ~55k | **Not authored this session — resumed an already-implemented, locally-green PR (#254) after a session crash.** Cost was orchestration: Hawk review (APPROVE, no must-fix), gate-wait, squash-merge, deploy-monitor. **Cost tail = merge-gate thrash, not authoring:** #254's deploy #546 red-gated twice on the recurring `TagsJourney` async flake (greened on attempt 3), after first holding the merge through a parallel session's red main. No authoring spike. Learnings folded into [phase-28-image-resize](learnings/phase-28-image-resize.md). |
| 28-B drag-resize handle | ~520k _(est.)_ | ~330k | ~150k | Authored Breaker→Pip→Refactor inline (pure `nextWidthFromDrag` reusing 28-A's `clampWidth`; corner handle; 13 unit + E2E; 534-test suite green; clean rebase onto a fast-moving main). One clean Hawk round (APPROVE). **Cost tail = a real regression + 3 failed deploys, NOT authoring:** the `opacity:0` handle still hit-tested and blanketed the remove test's **1×1** image → intercepted its hover (#548). Fix #261 (`pointer-events:none` when hidden) was *ineffective* — the hover the test performs is exactly what reveals the handle; fix #264 (remove journey uses a normal-sized image) closed it. Then the same `TagsJourney` async flake red-gated #550 (greened on re-run). Borderline spike, all in post-merge recovery + deploy serialization under heavy parallel-merge traffic. See [phase-28-image-resize](learnings/phase-28-image-resize.md). |
| MPI-5 grounded-entity allowlist | ~180k _(est.)_ | ~80k | ~54k | Small harness-only change authored Breaker→Pip→Refactor inline (no subagents): `GroundedEntities` helper + `BuildPrompt` allowlist block + `EvalRunner` wiring; 16 unit tests; full `Analysis.Eval` green; test-only so `paths-ignore`d (no deploy). One clean Hawk round (APPROVE, one doc-reconcile note actioned). **Cost tail = the same merge-gate starvation BUG-21/22/28-B paid today, ~2h:** a cascade of parallel-session merges (28-A/B flaky-E2E timeouts, BUG-21/22, TI-3) kept main's *latest* deploy perpetually in-progress or red, so the green-idle window the gate needs never opened; held + monitored + `git merge origin/main` to clear `mergeable:UNKNOWN`, merged in the first window a combined PR-∥-deploy poller caught. No authoring spike — the tax is red-main serialisation under heavy concurrent traffic. See [_minor-log](learnings/_minor-log.md) MPI-5. |
| TI-38 error-log-level | ~101k | ~88k | ~13k | Backend slice run by a **background subagent** (Breaker→Pip→Refactor→PR) from an observability-review brief. Replaced `UseExceptionHandler` with a try/catch mapping middleware; new in-process log-capture sink proves no-double-log. Self-Hawk APPROVE; folded its own 2 suggestions (HasStarted streaming-branch test + inverse-symmetry assertion) pre-merge → one CI rerun. Clean merge + deploy #556. |
| TI-37 frontend-error-capture | ~89k | ~80k | ~9k | Frontend+dashboard slice run by a **background subagent**, parallel with TI-38 (file-disjoint). Capture-phase window error listener → RUM `recordError`; 7 vitest cases + infra assertion. Self-Hawk APPROVE. **Cost tail = a transient Docker Hub outage** flaking the `eventstore` Testcontainers check across 3 reruns (zero eventstore code touched) — env, not authoring. Deploy #557. |
| BUG-23 rebuild-timeout | ~92k | ~84k | ~8k | Backend slice run by a **background subagent**, sequenced after TI-38 (shared `LoggingConfig.cs`). Generic `BoundedWrites.WithRetryAsync<T>` wrapping the rebuild reads + `Map` 503 arms; 4 integration tests (needed `[Collection]` to serialise the process-wide rebuild single-flight lock). Self-Hawk APPROVE. **Cost tail = the recurring flaky E2E:** deploy #558's E2E gate failed on `TagsJourney.AddMultipleTags_SpaceSeparated` + `ActionReadYourWrites` (neither in BUG-23's path) → one `gh run rerun --failed`, green. |
| CHANGE-16 pin @tiptap/extension-link | ~110k _(est.)_ | ~70k | ~36k | Manifest-only supply-chain fix run inline (no Breaker/Pip subagents): promote `@tiptap/extension-link` from transitive (via starter-kit) to a direct `^3.23.4` dep + incremental lockfile regen. Local npm==CI's 11.13.0, so the full frontend gate ran locally for real proof (npm ci/lint/tsc×2/578 tests/build/size). One clean Hawk round (APPROVE, no findings). No behaviour change → no BDD spec. No spike, no cost tail — clean PR CI, single green deploy #575. See `_minor-log.md` CHANGE-16. |
| BUG-24 image-403 (v1+v2) | ~348k _(est.)_ | ~310k | ~38k | Frontend slice run by **background subagents**. **v1 (#272) shipped on a unit-only-proven markdown-it rule, was merged, then the deploy-gate E2E proved the 403 still fired → reverted → re-driven as v2 (#273, resolve-before-parse).** ~150k (v1 impl+selfHawk) + ~198k (v2 impl+selfHawk) + orchestration. **Dominant cost = the verify-only-at-deploy-gate trap + chronic E2E flakiness:** the no-fetch property is unprovable in jsdom, so v1's mistake was invisible until the post-merge deploy gate; and reverting/re-merging serialised behind the same flaky `TagsJourney`/`NoteImage`/`ActionRYW` 30s-timeout flake that red-gated **~7 deploys today across all sessions** (#558/#559/#561/#562/#563 each needed 1–3 `gh run rerun`s). See [_minor-log](learnings/_minor-log.md) BUG-24 + new TI-39. |

| 19-I1 lazy-load editor + SDK | ~430k _(est.)_ | ~350k | ~81k | Frontend-only slice authored Breaker→Pip→Refactor **inline** (no subagents; Hawk was the one subagent). `LazyNoteEditor` (React.lazy + Suspense + ErrorBoundary→`lazyChunkError` RUM) + dynamic-`import()` the transcribe SDK + the chunk-reload-guard stability-delay fix. TDD: 6 tests red first, then green; build-verified the chunk split (entry has zero `@aws-sdk`/`smithy` tokens). One clean Hawk round (APPROVE, 3 non-blocking nits — 2 carried to TI-45 + a CLS-via-RUM check). **No spike, no cost tail:** all PR CI green first pass, single green deploy #602; the chronic E2E flake did not bite. Closes Phase 19. See [phase-19i1-lazy-load](learnings/phase-19i1-lazy-load.md). |

| 31-A electron-shell (resume only) | ~95k _(this session)_ | ~40k | ~52k | **Resume of an already-authored PR — not a fresh slice run.** Breaker/Pip/Stylist ran in prior sessions (counts unavailable). This session: rebase `origin/main` into the branch (one trivial `phase-bugs.md` Status conflict, BUG-34 Open→Done), one clean Hawk re-review subagent (~52k, APPROVE — confirmed the merge introduced no slice regressions + final desktop/ security pass), merge-gate, merge (#311), deploy #611 green, Scribe. No spike — the slice is purely additive under `desktop/`, conflict was one cell. See `_minor-log.md` 31-A. |
| minor-18-19 tag-search + auto-older (resume) | ~180k _(this session)_ | ~120k | ~59k | **Resume of a VS-Code-crash-interrupted slice — implementation already on disk (uncommitted), locally green.** This session: committed the dirty work after re-proving (29/29 + lint), merged `origin/main` into the branch, ran Refactor (no smells — code already clean, design-token CSS, explicit `olderAutoEnabled` state avoiding the set-state-in-effect lint gate). **Only real work = a pre-existing-test fix the full suite caught:** CHANGE-19's behavioural change (tag-select auto-enables older) broke the CHANGE-3 composition test in `CollapsibleFilters.test.tsx`, which the slice's own two test files did not cover — re-expressed it around a manual untick. One clean Hawk round (~59k, APPROVE — 5 nits, none gating). No spike; clean PR CI, single green deploy #613. See `_minor-log.md` minor-18-19. |

| 31-B pin loopback grant | ~140k _(est.)_ | ~75k | ~49k | Desktop-only slice driven **inline** (Breaker→Pip→Refactor; Hawk the one subagent ~49k). New pure `displayMedia.ts` + `setDisplayMediaRequestHandler` in `main.ts` + 4 headless unit tests; `build:main` tsc + tests green first pass. One clean Hawk round (APPROVE + 3 non-blocking nits, all adopted: single `loopback` literal, matched-primary log, `fakeSource` rename). No spike, no cost tail — PR CI green, single no-op deploy #615 (desktop not in `deploy.yml`). Origin of the slice: a user-driven manual finding that the implicit Electron default already worked → reframed as hardening. See `_minor-log.md` 31-B. |

| 31-C windows installer | ~150k _(est.)_ | ~80k | ~51k | Desktop-only slice driven **inline** (Breaker→Pip→Refactor; Hawk the one subagent ~51k). `electron-builder.json` (NSIS one-click, asar) + `package`/`app` scripts + baked public client-id default in `build-web.mjs` + 3 headless config-assertion tests; 7/7 specs green, `build:main` tsc + electron-builder binary verified. One clean Hawk round (APPROVE, only a default-icon doc nit, actioned). No spike, no cost tail — PR CI green, single no-op deploy #616 (desktop not in `deploy.yml`). **Completes the original 3-slice plan.** `.exe` build is a manual Windows step (Wine-on-Linux out of scope). See `_minor-log.md` 31-C. |
| 31-D CI-publish installer | ~165k _(est.)_ | ~85k | ~56k | CI/desktop slice driven **inline** (Breaker→Pip→Refactor; Hawk the one subagent ~56k). New `publish-desktop.yml` (workflow_run-after-Deploy, windows-latest, changed-paths gate, rolling `desktop-latest` Release) + `update.ps1`/`npm run update` + 3 headless wiring tests + `artifactName`; 6/6 specs green, YAML validated. One clean Hawk round (APPROVE + 2 nits actioned: dispatch-always-builds, name-coupling comment). No spike, no cost tail — PR CI green; **self-verified end-to-end** (the merge's own deploy #617 triggered publish run #1 → 82 MB installer published). Deploy-time neutral (separate post-deploy workflow). See `_minor-log.md` 31-D. |

| 32-A M365 calendar (resume) | ~430k _(est.)_ | ~205k | ~220k | **Resume of a WSL-crash-interrupted slice — Breaker's specs committed, Pip's impl uncommitted-but-~complete on disk.** This session: recovered the WIP, finished the gaps (mint tool, guide, startup provider-log, provider-agnostic stub/fake renames), and fixed a self-introduced infra-test race (2nd CDK-synth-in-static-ctor class → JSII asset-extraction `IOException` → `DisableTestParallelization`). **Hawk is the spike — ~220k across two rounds** (>Pip): round 1 REQUEST CHANGES found a real blocker (the Api.Integration calendar trio raced on process-wide env vars; my first fix only covered the *other* assembly) + tenant-default mismatch + AC6 divergence + graceful-JSON nits; round 2 APPROVE, all verified by execution. Cost driver = a thorough adversarial review of a security-sensitive external integration, justified. PR #320, deploy #620 green; ships dark (`CALENDAR_PROVIDER=google` default). See [phase-32a-outlook-calendar](learnings/phase-32a-outlook-calendar.md). |

| 32-B MS next-occurrence | ~190k _(est.)_ | ~120k | ~61k | Backend slice driven inline (Breaker→Pip→Refactor; Hawk the one subagent ~61k over 2 rounds). `GetNextOccurrenceAsync` via Graph `/me/events/{id}/instances` (400-day window, $orderby+$top, skip cancelled) + `TryMapEvent` refactor shared with the day view; source-verified the endpoint. One Hawk round APPROVE (only nits, applied: lock window/$orderby in the test). PR #322, deploy #622 — **this deploy carried the prod activation** (flipped CALENDAR_PROVIDER=microsoft). Prereq fix found first: deploy.yml never forwarded the MS secrets (#321). No cost tail — clean CI, single green deploy. |
| CHANGE-21 cal-source label | ~150k _(est.)_ | ~95k | ~49k | Backend+frontend minor change (user request after seeing Outlook live). `ICalendarClient.ProviderName` end-to-end + a header badge; tsc caught the optimistic-updater dropping the new required `provider` field (fixed with `{...old}`). One Hawk round APPROVE (nits, a11y aria-label applied). web 614 / Api.Integration 423 green. PR #323, deploy #623. No spike. |
| CHANGE-22 analysis observability | ~140k _(est.)_ | ~85k | ~55k | Backend+CDK observability slice driven **inline** (Breaker→Pip→Refactor; Hawk the one subagent ~55k). `AnalysisDurationMs`/`AnalysisFailed` EMF metrics + `notetaker-analysis-failed` alarm + dashboard widget on the `AnalyseNote` path, mirroring `ProjectionRebuild*`. Api 435 / infra 139 green. One clean Hawk round APPROVE (2 doc/style nits applied). Verified live in prod (`describe-alarms`, 8 alarms). No spike. PR #325, deploy #625. |
| 33-A save recording | ~340k _(est.)_ | ~250k | ~81k | Full-stack slice driven **inline** (Scout-drafted GWT scenarios into the bare phase doc, then Breaker→Pip→Refactor→Stylist; Hawk the one subagent ~81k). New `RecordingUploaded` event + `SaveRecording` + 3 presign/save endpoints + `notetaker-recordings` S3 bucket (DESTROY, 7-day expiry); frontend tees PCM→WAV→upload-on-Stop + optimistic Download link. Mirrored the image-upload pattern throughout. Domain 229 / Api 432 / infra 138 / web 618 green. One clean Hawk round APPROVE — **both should-fixes were docs/test completeness** (event-model+schemas entries, a scoped IAM-grant assertion), not code defects. No spike (size is the breadth of a genuine vertical, not waste). PR #324, deploy #624. See [phase-33a-save-recording](learnings/phase-33a-save-recording.md). |

| 34-A connect Google in-app | ~520k _(est.)_ | ~410k | ~112k | **Keystone of Phase 34** (graduates TI-47) authored **inline** (no Pip/Breaker subagents; Hawk the one subagent ~112k). New per-user `ICalendarTokenStore` (DynamoDB sub/provider) + in-app OAuth connect/connection/disconnect endpoints + `IGoogleCalendarTokenSource` (store-first/SSM-fallback) refactor of `GoogleCalendarClient` (singleton→scoped) + frontend connect/connected/reconnect UI + the delicate `AuthContext` calendar-connect callback (restore session → POST connect → reveal app). Largest slice of the session — big surface across backend+frontend+CDK+tests (Api.Integration 443, web 624, Infra 140). One clean Hawk round APPROVE (no blockers; 2 tracked follow-ups). No cost tail — clean PR CI, single green deploy #626; verified the prod table ACTIVE. Cost driver = breadth (security-sensitive OAuth + token store + auth-callback surgery), not rework. See [phase-34a-connect-google-calendar](learnings/phase-34a-connect-google-calendar.md). |

| 34-B key calendar by workspace | ~640k _(est.)_ | ~430k | ~85k | Large strangle-step slice authored **inline** (Breaker→Pip→Refactor; Hawk the one subagent ~85k, APPROVE no-blockers, 2 nits applied). Domain events + `(userId, workspaceId, provider)` token re-key + route relocation under `/w/{workspaceId}` + CDK route-pin move + frontend prefix/query-key/OAuth-workspace-round-trip. Tests: Domain 239, Api.Integration 452, Infra 32, web 624. **Cost tail = a two-stage deploy incident (~125k of the total), not authoring or review:** (1) deploy #627 flaked on un-`scoped()` calendar msw handlers (timing-dependent — local + PR CI passed); the web-only flake-fix (#329) then shipped the frontend without the backend (`detect-changes backend=false`) → prod 404 on `/w/{wsId}/calendar/*`; resolved by a backend push (#330→#629). Both incidents → [phase-34b-workspace-calendar](learnings/phase-34b-workspace-calendar.md) + the playbook. The authoring/review itself was clean and on-budget for the breadth. |

| 34-C MS provider per workspace | ~470k _(est.)_ | ~370k | ~87k | Large slice authored **inline** (Breaker→Pip→Refactor; Hawk the one subagent ~87k, APPROVE no-blockers, 1 nit applied — delete-other-before-upsert to avoid a stale-MS-token shadowing a new Google connection). Net-new Microsoft in-app OAuth (`IMicrosoftOAuthClient` + store-first token source) + `ICalendarClientFactory` replacing the `CALENDAR_PROVIDER` singleton + provider-choice frontend + `VITE_MS_CLIENT_ID` wiring. **No new domain events** (34-B's event already carries provider). Tests: Domain 239, Api.Integration 456, Infra 21, web 624. **Clean run, no cost tail** — one Hawk round, single green backend+frontend deploy #630 (the 34-B route-contract lesson held: both halves shipped together; new POST routes ride the generic proxy so no API-GW pin change). The one flake (Routing.test) was CPU contention from concurrent dotnet/eslint during a vitest run — passed clean when run alone; not a regression. See [phase-34c-ms-provider](learnings/phase-34c-ms-provider.md). |

| 33-B1 diarized transcript (async batch) | ~600k _(est.)_ | ~480k | ~123k | **Resumed after a laptop reboot** mid-slice (pass 1 — domain+projection — was already committed on the worktree branch). Drove passes 2–6 inline (trigger endpoint → new `TranscribeCompletion` Lambda → CDK infra → frontend chip/poll → observability+docs); Hawk the one subagent ~123k, **APPROVE no-blockers** (2 minors — empty-text-not-alarmed applied, speaker-`?` fallback a tested deliberate choice). Tests: Domain 245, Api.Integration 468, Infra 147, web 627. **Cost tail = CI-flake triage + a CONFLICTING merge (~150k):** the frontend flaked 2 ways (`TokenRefresh` fetch-spy race, `Auth` worker-teardown) — root-caused to 34-A's calendar MSW handlers registered rootless while the App calls them workspace-scoped; main's **34-B fixed it identically in parallel** so the merge collapsed both (and my redundant `TokenRefresh` hardening had to be reverted — it tripped `no-base-to-string`). Authoring/review itself clean and on-budget for a genuine full-stack vertical. The non-obvious IAM DefaultPolicy-overflow find (dedicated `Policy` vs `AddToRolePolicy`) was caught at infra-test time, not in review. PR #328, deploy #631; all 3 resources verified live in prod. See [phase-33b1-diarization-async-batch](learnings/phase-33b1-diarization-async-batch.md). |

| 33-B2 re-analysis on diarized transcript | ~720k _(est.)_ | ~470k | ~310k | Final Phase 33 slice, plan-mode → 4 passes inline (extract `INoteAnalysisService` + identity-explicit command-handler overloads → completion Lambda re-analyses on the winning transcript → CDK Bedrock/projection grants → frontend defer state-machine). Two correctness refinements found mid-impl beyond the plan: the analyse-intent must ride the job name (`-a`/`-n`) for auto-analyse-OFF correctness, and the on-Stop defer needs synchronous `'refining'` + a started-vs-failed split. **Cost tail = a 3-round Hawk cycle (~310k, >Pip)**: round 1 caught a real **Major** (headless re-analysis passed `userName:""` → broke action-item attribution); the fix (stamp `EventMetadata.UserName` → fold `NoteDetail.OwnerName` → service fallback) then tripped round 2's **Blocker** — `OwnerName` round-tripped in-memory but was **unmapped in `DynamoDbNoteDetailStore`**, so it was `""` in prod while tests stayed green (added the store mapping + a DynamoDB-Local round-trip test; round 3 APPROVE). Also rebased out a parallel session's unpushed Phase 35 doc commit my worktree was accidentally based on. Tests: Domain 245, Api.Integration 482, web 644, +EventStore.Integration. PR #336, deploy #637; B2 infra verified live. The in-memory-store-masks-DynamoDB-field lesson → a new CLAUDE.md guardrail. See [phase-33b2-reanalyse-diarized](learnings/phase-33b2-reanalyse-diarized.md). |

| 35-A MCP connect & list (no-auth) | ~760k _(est.)_ | ~430k | ~205k | First slice of Phase 35. Source-driven research subagent (~82k) pinned the MCP spec, then a **build subagent** (Breaker+Pip, ~131k) shipped the `ModelContextProtocol.AspNetCore` 1.4.0 server (`MapMcp("/w/{workspaceId}/mcp")`, stateless, `list_notes` over `NoteCardList`, Query-Lambda pin, config-driven IP allowlist). **Cost tail = a 3-round Hawk cycle (~205k)** on the no-auth IP allowlist: round 1 *mis*diagnosed `RemoteIpAddress` as the proxy IP → asked for an XFF fix; that fix was a **spoofable security regression** (API GW appends the peer, so left-most XFF is attacker-controlled); round 2 caught it → reverted to `RemoteIpAddress` (= AWS `sourceIp`, non-spoofable for a regional HTTP API); round 3 APPROVE. Lesson codified as a CLAUDE.md guardrail. Re-sliced before build: "reuse Google" = an OAuth AS-broker (deferred to 35-E), so 35-A shipped no-auth to prove the Cowork handshake. Api.Integration 476, Infra +1, build+synth green. PR #335, deploy #636; prod route verified live. See [phase-35a-mcp-connect-list](learnings/phase-35a-mcp-connect-list.md). |
| 35 disable no-auth MCP (kill switch) | ~95k _(est.)_ | ~55k | ~46k | Owner-requested security op after the handshake proved out: `MCP_ENABLED` kill switch (gated `MapMcp` default-true + CDK `MCP_ENABLED=false`) to close the no-auth window until 35-E. Driven inline (no build subagent); one Hawk round APPROVE (tightened the infra assertion to pin the **Query** function — `FindResources(Match)` returns empty, so walked the template JSON instead). Api 483 / Infra 150. PR #337, deploy #638; **prod 404 verified live**. Small, clean. See `_minor-log` 2026-06-24. |
| 34-D1 retire Google SSM path | ~330k _(est.)_ | ~210k | ~125k | Backend+infra strangle-cleanup authored **inline** (Breaker→Pip; Hawk the one subagent ~125k over 2 rounds). `GoogleCalendarTokenSource` → store-only; CDK drops the Google SSM env + conditional grant; Google mint script + guide removed; infra assertions repointed to the (kept) Microsoft grant. Re-sliced 34-D → 34-D1 (Google, proven) + 34-D2 (Microsoft, blocked on Outlook in-app verification) per the strangle guardrail. **Hawk round 1 caught a real regression**: removing the SSM fallback also removed the only try/catch absorbing a store-read failure → a transient DynamoDB error would 500 instead of `calendar_unavailable`; restored the catch + added a throwing-store test. Round 2 APPROVE. Deploy #640 first flaked on the unrelated TI-42 RYW E2E; `gh run rerun --failed` cleared it → prod verified (Google SSM env gone, MS kept). See [_minor-log](learnings/_minor-log.md). |
| 34-D2 retire MS SSM + CALENDAR_PROVIDER | ~290k _(est.)_ | ~190k | ~75k | **Completes Phase 34.** Outlook in-app verified in prod → removed the last SSM fallback: `MicrosoftCalendarTokenSource` store-only (kept the try/catch per the 34-D1 lesson), `SsmMicrosoftRefreshTokenSource` deleted, factory's `CALENDAR_PROVIDER` branch → new `UnavailableCalendarClient` (unconnected → calendar_unavailable → CHANGE-25 menu), CDK drops `CALENDAR_PROVIDER` + MS SSM env/grant, mint script + guide removed. Authored inline; one clean Hawk round APPROVE (2 nits applied — stale SSM/guide refs in MicrosoftCalendarClient logs+comments). Api 482 / Infra 148. PR #340, deploy #641 green first try; prod verified (no CALENDAR_PROVIDER/MS-SSM env, no ssm grant). No cost tail. |
| 35-E OAuth for MCP server | ~1.05M _(est.)_ | ~430k | ~410k | Largest, most security-critical slice of the phase. Source-driven research subagent (~63k) pinned the MCP OAuth spec; build subagent shipped RS (SDK `AddMcp`+`McpBearer`) + hand-rolled AS broker over Google (HS256 audience-bound, PKCE-S256, stores, secret) + `MCP_ENABLED` flip. **Cost driver = DUAL review (Hawk + security-auditor) over 2 audit rounds (~410k):** round 1 found a Critical prod-config break (`MCP_OAUTH_CLIENT_ID` never deployed) + 2 fail-open defaults (allowlist, signing key) + a confused-deputy gap; the fix's audience middleware then had a case-sensitivity bypass caught in round 2. Worth every token — it's an OAuth server. **Deploy tail:** #643 failed 3× (1 E2E flake + 2 orphaned-RETAIN resources from a parallel stale re-run clobbering the test stack; cleared the test-env orphans). Domain 245 / Api 509 / Infra 159. Prod verified (401 + 5 routes). Owner config pending. See [phase-35e-oauth-broker](learnings/phase-35e-oauth-broker.md). |

| 34-E ICS calendar feed provider | ~470k _(est.)_ | ~290k _(build subagent)_ | ~170k _(3 Hawk passes)_ | New `ics` calendar provider: connect a published ICS feed URL (e.g. Outlook "Publish a calendar") — bypasses the M365 admin-consent wall that blocked work accounts. Implementation **delegated to a build subagent** (~172k, one shot, all tests green: `IcsFeedCalendarClient` via Ical.Net occurrence-expansion + `IcsUrlValidator` SSRF guard + connect/ics endpoint + factory + frontend URL-paste menu + 30 backend/2 frontend tests). **Cost driver = Hawk security review (~170k over 2 substantive rounds):** round 1 REQUEST CHANGES found a *real* SSRF redirect bypass (HttpClient follows 3xx → could 302 to 169.254.169.254) + an unbounded-download OOM + a misleading rebinding comment; fixed (`AllowAutoRedirect=false`, 5 MB cap, documented TOCTOU residual + redirect test); round 2 APPROVE. Delegate-then-review-hard worked exactly as intended — the subagent shipped fast, Hawk caught the security gaps. PR #343, deploy #644; prod route verified. Adds `Ical.Net 5.2.3`. See [phase-34.md 34-E](phases/phase-34.md). |

## Scribe append format

Add one row above per completed slice. `Total` / `Pip` (Breaker+impl+orchestration) / `Hawk` (all rounds) / one-line dominant driver.

- A **spike** = any slice >2× the comparable recent slice, or a Hawk total >2× Pip. Flag it for `process-improvements`.
- If the slice surfaced a *new* avoidable cost, add a row to [token-optimisation-playbook.md](token-optimisation-playbook.md) — do not write a prose block here.
